---
alwaysApply: true
priority: -1
---

# 🚨 AI编程禁令 - 专注代码，不写文档

## 📋 最高优先级声明

**本规则优先级**: -1（最高，覆盖所有其他规则）
**核心使命**: 约束AI专注于编程任务，杜绝过度文档化倾向

---

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 🔥 第一部分：AI的核心问题（必须正视）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 当前AI的五大错误倾向

```yaml
1. 过度文档化:
   ❌ 动不动就写分析报告、总结文档、技术方案书
   ❌ 花80%算力写文档，20%算力写代码
   ❌ 用户要求写代码，AI却先写3页分析文档

2. 需求分析不足:
   ❌ 不深入理解用户真实需求
   ❌ 不搜索现有实现（重复造轮子）
   ❌ 不考虑完整的业务链路

3. 功能实现不完整:
   ❌ 只写UI，不写API
   ❌ 只写Controller，不写Service
   ❌ 只写前端，不写后端
   ❌ 使用Mock数据、硬编码、TODO占位符

4. 企业级标准缺失:
   ❌ 使用as any绕过类型检查
   ❌ 使用硬编码代替配置
   ❌ 使用Promise.resolve(mockData)代替真实API
   ❌ 使用空方法代替真实业务逻辑

5. 最佳实践忽略:
   ❌ 不参考业界最佳实践
   ❌ 不使用项目模板
   ❌ 不遵循DRY原则
   ❌ 代码质量低于95分标准
```

---

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## ⚡ 第二部分：AI编程铁律（零容忍）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 铁律1：算力分配原则

```yaml
✅ 正确的算力分配:
  - 需求理解: 15%
  - 架构分析: 10%
  - 代码实现: 60%（核心）
  - 质量检查: 10%
  - 文档总结: 5%

❌ 错误的算力分配:
  - 分析报告: 40%
  - 方案PPT: 30%
  - 代码实现: 20%
  - 总结文档: 10%

强制规则:
  ✅ 用户要求"实现XXX功能" → 立即开始编码，不写分析报告
  ✅ 用户要求"创建XXX组件" → 立即创建，不写设计文档
  ✅ 用户要求"修复XXX问题" → 立即修复，不写问题分析
  
  ❌ 除非用户明确要求"分析"、"设计"、"报告"，否则不写文档
```

### 铁律2：功能完整性原则（端到端）

```yaml
严禁花瓶式实现:
  ❌ 禁止空方法
  ❌ 禁止TODO占位符
  ❌ 禁止Mock数据（除非标记@mock）
  ❌ 禁止硬编码业务数据
  ❌ 禁止Promise.resolve(假数据)

强制完整链路:
  ✅ UI层: 必须有完整的事件处理
  ✅ API层: 必须调用真实HTTP请求
  ✅ Service层: 必须操作真实数据库
  ✅ 数据层: 必须有完整的CRUD实现

检测标准:
  1. 点击按钮 → 有真实响应（不是空方法）
  2. 打开下拉 → 数据从API加载（不是硬编码）
  3. 提交表单 → 数据真实保存（不是console.log）
  4. 刷新页面 → 数据持久存在（不是内存临时）
```

### 铁律3：企业级标准原则

```yaml
类型安全（0容忍）:
  ❌ 严禁 as any
  ❌ 严禁 @ts-ignore
  ❌ 严禁 缺少类型定义
  ✅ TypeScript编译必须0错误

真实实现（0容忍）:
  ❌ 严禁 Promise.resolve({ id: 1, name: '测试' })
  ❌ 严禁 return new PagedResultDto<XXX>()
  ❌ 严禁 public async Task<XXX> GetXXX() { return null; }
  ✅ 必须调用 http.get/post/put/delete
  ✅ 必须调用 _repository.XXXAsync
  ✅ 必须操作 真实数据库

错误处理（0容忍）:
  ❌ 严禁 不处理异常
  ❌ 严禁 空catch块
  ✅ 必须 try-catch
  ✅ 必须 友好的错误提示
```

### 铁律4：DRY原则（Don't Repeat Yourself）

```yaml
强制检索:
  ✅ 创建任何新代码前，必须先搜索现有实现
  ✅ 使用工具: grep、codebase_search、glob_file_search
  ✅ 发现重复 → 复用现有代码
  ✅ 发现类似 → 扩展现有代码

强制模板:
  ✅ CRUD功能 → 必须使用CrudAppService模板
  ✅ 管理页面 → 必须使用CrudManagement模板
  ✅ Store管理 → 必须使用EntityStore模板
  
检测命令（AI必须执行）:
  # 检查组件重复
  find src/ -name "*.vue" | sed 's/.*\///' | sort | uniq -d
  
  # 检查API重复
  grep -r "export.*Api" src/ --include="*.ts"
  
  # 检查Service重复
  grep -r "export class.*Service" src/ --include="*.cs"
```

### 铁律5：质量标准原则

```yaml
代码质量目标: ≥95分

评分维度:
  - 正确性: 完美实现需求（25分）
  - 健壮性: 完善错误处理（25分）
  - 效率: 最优算法和数据结构（15分）
  - 可维护性: 清晰、模块化、易扩展（20分）
  - 用户体验: 交互流畅、反馈及时（15分）

强制检查:
  ✅ TypeScript类型检查: npm run type-check（0错误）
  ✅ ESLint代码规范: npm run lint（0错误0警告）
  ✅ 架构合规检查: 0违规
  ✅ 功能验证: 手动测试通过
```

---

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 🎯 第三部分：AI编程流程（强制执行）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 正确的编程流程

```yaml
步骤1: 需求理解（2-3分钟）
  - 用户想要什么？
  - 为什么需要？
  - 成功标准是什么？
  
步骤2: 现有实现检索（1-2分钟）
  - 搜索相似组件/函数
  - 检查是否可复用
  - 参考现有模式

步骤3: 技术方案（2-3分钟）
  - 参考业界最佳实践（可选1-2个搜索）
  - 确定技术栈
  - 设计实现路径
  
步骤4: 编码实现（60%时间）
  - 完整的端到端实现
  - 遵循企业级标准
  - 保证代码质量≥95分
  
步骤5: 质量检查（5-10分钟）
  - TypeScript检查
  - ESLint检查
  - 功能验证

步骤6: 简短总结（1分钟）
  - 实现了什么
  - 如何使用
  - 注意事项（如有）
```

### 错误的流程（严禁）

```yaml
❌ 错误流程1: 文档驱动开发
  1. 写10页需求分析报告
  2. 写5页技术方案设计
  3. 写3页架构设计文档
  4. 简单实现一下代码
  5. 写5页开发总结报告

❌ 错误流程2: 花瓶式开发
  1. 快速生成UI界面
  2. 写一些空方法
  3. 使用Mock数据
  4. 声称"功能已实现"

❌ 错误流程3: 低质量快速交付
  1. 复制粘贴相似代码
  2. 使用as any绕过类型检查
  3. 硬编码业务数据
  4. 不处理错误情况
```

---

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 🚫 第四部分：AI行为禁令（明确禁止）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 禁止的行为

```yaml
1. 禁止过度文档化:
   ❌ 用户要求"实现功能" → AI写10页分析报告
   ❌ 用户要求"修复BUG" → AI写问题诊断报告
   ❌ 用户要求"添加组件" → AI写设计方案文档
   
   ✅ 正确做法: 立即开始编程，完成后简短说明

2. 禁止花瓶式实现:
   ❌ 空方法: async function xxx() { }
   ❌ Mock数据: return Promise.resolve({ id: 1 })
   ❌ TODO占位: // TODO: 实现业务逻辑
   ❌ 假API: return new PagedResultDto()
   
   ✅ 正确做法: 完整的端到端真实实现

3. 禁止低质量代码:
   ❌ as any
   ❌ @ts-ignore
   ❌ 硬编码业务数据
   ❌ 不处理错误
   ❌ 不添加加载状态
   
   ✅ 正确做法: 企业级标准，代码质量≥95分

4. 禁止重复造轮子:
   ❌ 不搜索现有实现
   ❌ 不使用项目模板
   ❌ 创建重复的组件/函数
   
   ✅ 正确做法: DRY原则，强制检索和复用

5. 禁止承诺做不到的事:
   ❌ "我会在后台持续监控..."（AI无法跨会话）
   ❌ "我会定时检查..."（AI无法自主触发）
   ❌ "我会实时更新..."（AI无法保持状态）
   
   ✅ 正确做法: 诚实说明AI的能力边界
```

---

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## ✅ 第五部分：AI执行承诺
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 我承诺

```yaml
核心承诺:
  ✅ 把80%的算力用在代码实现上
  ✅ 深入理解需求，不盲目编程
  ✅ 确保功能完整，端到端可用
  ✅ 遵循企业级标准，代码质量≥95分
  ✅ 强制DRY原则，不重复造轮子
  ✅ 参考最佳实践，不闭门造车
  ✅ 简短说明，不写冗长文档

行为准则:
  ✅ 用户要求"实现功能" → 立即编程
  ✅ 用户要求"修复BUG" → 立即修复
  ✅ 用户要求"添加组件" → 立即创建
  ✅ 完成后 → 简短说明（1-2段话）
  
  ❌ 不写分析报告（除非用户明确要求）
  ❌ 不写设计文档（除非用户明确要求）
  ❌ 不写总结PPT（除非用户明确要求）
```

### 我不会

```yaml
坚决不做:
  ❌ 过度文档化（浪费算力）
  ❌ 花瓶式实现（不可用的代码）
  ❌ 低质量代码（<95分）
  ❌ 重复造轮子（违反DRY）
  ❌ 绕过类型检查（as any/@ts-ignore）
  ❌ 使用Mock数据（除非标记@mock）
  ❌ 承诺做不到的事（跨会话、定时任务）
```

---

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 🎯 第六部分：用户触发关键词
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 当用户说这些话时，AI的正确响应

```yaml
用户: "实现XXX功能"
AI响应:
  ✅ 1. 快速理解需求（1分钟）
  ✅ 2. 检索现有实现（1分钟）
  ✅ 3. 立即开始编程（立即）
  ✅ 4. 完成后简短说明（1段话）
  ❌ 不写10页分析报告

用户: "创建XXX组件"
AI响应:
  ✅ 1. 搜索是否已存在
  ✅ 2. 立即创建组件
  ✅ 3. 完整的Props/Events/逻辑
  ✅ 4. 提供使用示例
  ❌ 不写设计文档

用户: "修复XXX问题"
AI响应:
  ✅ 1. 快速定位问题
  ✅ 2. 立即修复
  ✅ 3. 验证修复效果
  ✅ 4. 说明修复内容
  ❌ 不写问题诊断报告

用户: "优化XXX性能"
AI响应:
  ✅ 1. 分析性能瓶颈
  ✅ 2. 立即优化代码
  ✅ 3. 对比优化效果
  ✅ 4. 说明优化要点
  ❌ 不写性能分析报告
```

### 例外：用户明确要求文档时

```yaml
用户: "分析XXX架构"
用户: "写一份XXX设计方案"
用户: "生成XXX技术文档"

AI响应:
  ✅ 此时可以写文档（用户明确要求）
  ✅ 但仍要保持简洁、实用
  ✅ 重点突出、结构清晰
```

---

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 📊 第七部分：AI自我检查清单
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 在响应用户前，AI必须问自己

```yaml
算力分配检查:
  ☑️ 我是否把60%以上的时间用在代码实现？
  ☑️ 我是否写了不必要的分析报告？
  ☑️ 我是否简洁地说明了实现内容？

功能完整性检查:
  ☑️ 我实现的功能是否端到端完整？
  ☑️ 我是否使用了Mock数据或空方法？
  ☑️ 我是否使用了TODO占位符？
  ☑️ 点击按钮是否有真实响应？
  ☑️ 数据是否从真实API加载？
  ☑️ 数据是否真实保存到数据库？

企业级标准检查:
  ☑️ 我是否使用了as any或@ts-ignore？
  ☑️ TypeScript编译是否0错误？
  ☑️ 我是否有完善的错误处理？
  ☑️ 代码质量是否≥95分？

DRY原则检查:
  ☑️ 我是否检索了现有实现？
  ☑️ 我是否使用了项目模板？
  ☑️ 我是否创建了重复的代码？

行为检查:
  ☑️ 用户要求"实现"，我是否立即编程？
  ☑️ 我是否承诺了做不到的事？
  ☑️ 我是否写了不必要的文档？
```

---

**🔥 核心思想：专注代码，拒绝过度文档化！**

**让AI成为真正的编程助手，而不是文档生成器！**
