---
alwaysApply: true
---

# AI编程铁律自动执行引擎 (AI Programming Iron Law Auto-Execution Engine)

## 🎯 核心使命

**问题**: 现有铁律规则完善，但AI大模型无法自动识别、学习和执行，导致规则形同虚设。

**目标**: 建立**可被AI自动识别、强制学习、自动执行**的完整闭环体系。

**原则**: 保留立体同心圆模型精髓（L0-L7核心理念层到架构保护层），增强AI自动执行能力。

---

## 🔥 第一层：AI编程前强制学习系统 (Pre-Coding Mandatory Learning)

### 触发条件检测 (Trigger Detection)

**🚨 AI必须在以下情况下自动触发学习流程：**

```typescript
触发关键词 = [
  // 直接触发
  "专家模式", "开始编程", "开始开发", "新功能", "新建",
  // 代码操作触发
  "创建组件", "创建服务", "创建Store", "创建API",
  "修改代码", "添加功能", "实现功能",
  // 修复触发
  "修复BUG", "修复错误", "解决问题",
  // 架构触发
  "重构", "优化", "架构调整"
]

文件操作触发 = [
  '创建*.vue', '创建*.ts', '创建*.tsx',
  '修改packages/*', '修改src/*'
]
```

### 强制学习检查清单 (Mandatory Learning Checklist)

**AI在编程前必须执行以下步骤，并在响应中明确列出：**

#### ✅ 1. 项目架构扫描 (Architecture Scanning)

```bash
# AI必须执行的命令
📁 扫描packages目录结构
ls -la src/SmartAbp.Vue/packages/

📁 识别核心package
lowcode-shared → 基础层(Layer 0)
lowcode-core → 核心层(Layer 1)
lowcode-api → API层(Layer 1)
lowcode-tools → 工具层(Layer 1)
lowcode-designer → 应用层(Layer 2)

📁 扫描组件库
find src/SmartAbp.Vue/packages -name "*.vue" -o -name "*.ts"
```

**AI响应格式**:
```
✅ 架构扫描完成
📦 发现5个核心package
📄 发现XX个组件，XX个工具函数
🔍 依赖层级: 正确
```

#### ✅ 2. 代码库功能检索 (Codebase Feature Search)

```bash
# AI必须搜索的位置
🔍 lowcode-shared/src/types/ → 查找已有类型定义
🔍 lowcode-shared/src/utils/ → 查找已有工具函数
🔍 lowcode-shared/src/error/ → 查找错误处理
🔍 lowcode-core/src/components/ → 查找核心组件
🔍 lowcode-designer/src/components/ → 查找设计器组件
```

**AI响应格式**:
```
✅ 代码库检索完成
📋 已有类型: BaseComponentProps, ValidationRule等
🔧 已有工具: string.ts(14函数), object.ts(13函数)
🎨 已有组件: XX个可复用组件
⚠️ 发现相似功能: [列出]
```

#### ✅ 3. ADR架构决策学习 (ADR Learning)

```bash
# AI必须阅读的ADR
📚 docs/architecture/adr/0001-technology-stack-selection.md
📚 docs/architecture/adr/0005-lowcode-engine-architecture.md
📚 docs/architecture/SmartAbp企业级低代码引擎系统架构说明书.md
```

**AI响应格式**:
```
✅ ADR学习完成
📖 技术栈: ABP vNext + Vue3 + TypeScript
📖 架构原则: packages黑盒原则，统一编译架构
📖 约束条件: [列出相关约束]
```

#### ✅ 4. 模板库匹配 (Template Matching)

```bash
# AI必须检查的模板
🎯 glob_file_search("templates/**/*.template.*")
🎯 检查是否有匹配的代码生成模板
```

**AI响应格式**:
```
✅ 模板库检查完成
📝 发现匹配模板: [模板名称]
📝 无匹配模板: 需要手动编写
💡 建议使用: [模板路径]
```

#### ✅ 5. 公共组件库识别 (Common Components)

```bash
# AI必须识别的公共组件
🔍 lowcode-shared/src/components/BaseComponent.ts
🔍 lowcode-shared/src/components/ComponentRegistry.ts
🔍 packages/*/src/components/*.vue
```

**AI响应格式**:
```
✅ 公共组件识别完成
🎨 可复用组件: [列出]
🔧 可复用Composables: [列出]
⚠️ 避免重复创建
```

---

## 🔥 第二层：增量迭代编程执行系统 (Incremental Programming Execution)

### 编程执行规范 (Coding Standards)

**AI在编程时必须：**

#### ✅ 1. 代码行数追踪 (Line Counter)

```typescript
// AI内部状态管理
let currentSessionLines = 0;

function trackCodeLines(newCode: string) {
  const lines = newCode.split('\n').length;
  currentSessionLines += lines;
  
  if (currentSessionLines >= 280) {
    console.warn("⚠️ 警告：已编写280行，接近300行限制");
  }
  
  if (currentSessionLines >= 300) {
    console.error("🚨 强制停止：已达到300行，必须执行质量检查");
    triggerQualityGate();
  }
}
```

**AI响应格式**:
```
📊 当前会话代码行数: XX/300行
⚠️ [280行时] 即将达到限制，准备质量检查
🚨 [300行时] 已达限制，立即停止编码，执行质量检查
```

#### ✅ 2. 架构合规实时检查 (Real-time Compliance)

```typescript
// 每次编写代码后立即检查
function checkArchitectureCompliance(code: string, filePath: string) {
  // 检查1: 相对路径违规
  if (filePath.includes('packages/') && code.includes("'../")) {
    throw new Error("🚨 架构违规: packages中禁止使用相对路径");
  }
  
  // 检查2: 主应用引用违规
  if (filePath.includes('packages/') && code.includes("'@/")) {
    throw new Error("🚨 架构违规: packages中禁止引用主应用");
  }
  
  // 检查3: 类型安全绕过
  if (code.includes('as any') || code.includes('@ts-ignore')) {
    throw new Error("🚨 类型违规: 禁止使用as any或@ts-ignore");
  }
}
```

**AI响应格式**:
```
✅ 实时架构检查通过
🔍 相对路径: 0违规
🔍 主应用引用: 0违规
🔍 类型安全: 0绕过
```

#### ✅ 3. 代码重复检测 (Duplication Detection)

```typescript
// 编写前检查是否重复
function checkDuplication(componentName: string, functionName: string) {
  // 搜索现有组件
  const existingComponents = searchComponents(componentName);
  if (existingComponents.length > 0) {
    return {
      duplicate: true,
      suggestion: `已存在组件: ${existingComponents.join(', ')}`,
      action: '建议复用而非重新创建'
    };
  }
  
  // 搜索现有函数
  const existingFunctions = searchFunctions(functionName);
  if (existingFunctions.length > 0) {
    return {
      duplicate: true,
      suggestion: `已存在函数: ${existingFunctions.join(', ')}`,
      action: '建议导入使用而非重新实现'
    };
  }
  
  return { duplicate: false };
}
```

**AI响应格式**:
```
✅ 代码重复检测完成
🔍 组件重复: 未发现
🔍 函数重复: 未发现
💡 [如发现] 建议复用: [路径]
```

---

## 🔥 第三层：质量门禁自动触发系统 (Quality Gate Auto-Trigger)

### 触发条件 (Trigger Conditions)

**🚨 以下情况AI必须立即停止编码，执行质量门禁：**

```typescript
触发条件 = {
  // 条件1: 代码行数达到300行
  codeLines: currentSessionLines >= 300,
  
  // 条件2: 完成开发阶段
  stagCompleted: [
    '完成功能模块', '完成组件开发', '完成API实现',
    '完成Store编写', '完成工具函数', '完成类型定义',
    '修复重要BUG', '完成重构任务'
  ],
  
  // 条件3: 用户明确要求
  userRequest: ['提交代码', '质量检查', '代码检查']
}
```

### 五重质量门禁执行 (Five-Gate Execution)

**AI必须按顺序执行，任何一关失败立即停止：**

#### 🏗️ 第一关：架构完整性检查 (0违规)

```bash
echo "🏗️ 第一关：架构完整性检查..."

# 检查1: 相对路径违规
RELATIVE_VIOLATIONS=$(grep -r "'../'" src/SmartAbp.Vue/packages/ 2>/dev/null | wc -l)

# 检查2: 主应用引用违规
MAIN_APP_VIOLATIONS=$(grep -r "@/" src/SmartAbp.Vue/packages/ 2>/dev/null | wc -l)

# 检查3: 类型安全绕过
AS_ANY=$(grep -r "as any" src/ 2>/dev/null | wc -l)
TS_IGNORE=$(grep -r "@ts-ignore" src/ 2>/dev/null | wc -l)

TOTAL=$((RELATIVE_VIOLATIONS + MAIN_APP_VIOLATIONS + AS_ANY + TS_IGNORE))

if [ $TOTAL -eq 0 ]; then
  echo "✅ 第一关通过 (0违规)"
else
  echo "❌ 第一关失败: 发现${TOTAL}个违规"
  exit 1
fi
```

**AI响应格式**:
```
🏗️ 第一关：架构完整性检查
  • 相对路径违规: 0个 ✅
  • 主应用引用违规: 0个 ✅
  • as any使用: 0个 ✅
  • @ts-ignore使用: 0个 ✅
✅ 第一关通过
```

#### 🔄 第二关：代码重复度检查 (0重复)

```bash
echo "🔄 第二关：代码重复度检查..."

# 检查重复组件
DUP_COMPONENTS=$(find src/ -name "*.vue" | sed 's/.*\///' | sort | uniq -d | wc -l)

# 检查重复函数
DUP_FUNCTIONS=$(grep -rh "export function\|export const" src/ --include="*.ts" | sort | uniq -d | wc -l)

TOTAL=$((DUP_COMPONENTS + DUP_FUNCTIONS))

if [ $TOTAL -eq 0 ]; then
  echo "✅ 第二关通过 (0重复)"
else
  echo "❌ 第二关失败: 发现${TOTAL}个重复"
  exit 1
fi
```

**AI响应格式**:
```
🔄 第二关：代码重复度检查
  • 重复组件: 0个 ✅
  • 重复函数: 0个 ✅
  • 重复类: 0个 ✅
✅ 第二关通过
```

#### ⚡ 第三关：编译与静态检查 (0错误)

```bash
echo "⚡ 第三关：编译与静态检查..."

cd src/SmartAbp.Vue

# TypeScript类型检查
npm run type-check
TYPE_CHECK=$?

# ESLint检查
npm run lint
LINT=$?

if [ $TYPE_CHECK -eq 0 ] && [ $LINT -eq 0 ]; then
  echo "✅ 第三关通过 (0错误)"
else
  echo "❌ 第三关失败"
  exit 1
fi
```

**AI响应格式**:
```
⚡ 第三关：编译与静态检查
  • TypeScript类型检查: ✅ 通过
  • ESLint代码规范: ✅ 通过
✅ 第三关通过
```

#### 🎯 第四关：低代码生成器专项检查 (100%质量)

```bash
echo "🎯 第四关：低代码生成器专项检查..."

cd src/SmartAbp.Vue

# packages TypeScript编译
npx tsc --build tsconfig.references.json --force
PACKAGES_BUILD=$?

# packages ESLint检查
npm run lint -- "packages/*/src/**/*.{ts,vue}" --fix
PACKAGES_LINT=$?

if [ $PACKAGES_BUILD -eq 0 ] && [ $PACKAGES_LINT -eq 0 ]; then
  echo "✅ 第四关通过 (100%质量)"
else
  echo "❌ 第四关失败"
  exit 1
fi
```

**AI响应格式**:
```
🎯 第四关：低代码生成器专项检查
  • packages编译: ✅ 通过
  • packages规范: ✅ 通过
  • 依赖层级: ✅ 正确
✅ 第四关通过
```

#### 🚀 第五关：技术债务监控 (≥85分)

```bash
echo "🚀 第五关：技术债务监控..."

# 计算质量评分
QUALITY_SCORE=100

# 复杂文件数
COMPLEX_FILES=$(find src/ -name "*.ts" -o -name "*.vue" | xargs wc -l | awk '$1 > 200 {count++} END {print count+0}')
[ $COMPLEX_FILES -gt 10 ] && QUALITY_SCORE=$((QUALITY_SCORE - 5))

# TODO标记数
TODO_COUNT=$(grep -r "TODO\|FIXME" src/ 2>/dev/null | wc -l)
[ $TODO_COUNT -gt 20 ] && QUALITY_SCORE=$((QUALITY_SCORE - 5))

echo "📊 质量评分: ${QUALITY_SCORE}/100"

if [ $QUALITY_SCORE -ge 85 ]; then
  echo "✅ 第五关通过 (评分≥85)"
else
  echo "⚠️ 第五关警告 (评分<85)"
fi
```

**AI响应格式**:
```
🚀 第五关：技术债务监控
  • 复杂文件数: XX个
  • TODO标记: XX个
  • 质量评分: XX/100
✅ 第五关通过 (或 ⚠️ 警告但不阻止)
```

---

## 🔥 第四层：Git版本管理自动执行系统 (Git Auto-Execution)

### Git六步强制流程 (Six-Step Mandatory Process)

**质量门禁通过后，AI必须自动执行：**

#### 📋 第一步：检测代码变更

```bash
git status --porcelain
```

**AI响应**:
```
📋 第一步：代码变更检测
  • 修改文件: XX个
  • 新增文件: XX个
  • 删除文件: XX个
```

#### 📦 第二步：暂存所有修改

```bash
git add .
```

**AI响应**:
```
📦 第二步：暂存修改
  • 已暂存: XX个文件
```

#### 📝 第三步：生成标准化提交信息

```bash
# AI生成的提交信息模板
COMMIT_MSG="feat: 完成[功能描述] - [阶段名称]

🔧 核心实现:
- 🎯 [具体实现1]
- 🔧 [具体实现2]

📊 质量验证:
✅ 架构完整性: 0违规
✅ 代码重复度: 0重复
✅ 编译静态检查: 0错误
✅ 低代码生成器: 100%质量
✅ 技术债务: ${QUALITY_SCORE}/100

📦 修改统计:
- 文件数: XX个
- 新增代码: XX行
- 删除代码: XX行"
```

**AI响应**:
```
📝 第三步：生成提交信息
  • 类型: feat/fix/refactor
  • 描述: [核心成果]
  • 质量验证: ✅ 全部通过
```

#### 🚀 第四步：执行本地提交

```bash
git commit -m "$COMMIT_MSG"
```

**AI响应**:
```
🚀 第四步：本地提交
  • 提交哈希: abc123...
  • 提交成功: ✅
```

#### 🔄 第五步：拉取远程并合并

```bash
git pull --rebase origin main
```

**AI响应**:
```
🔄 第五步：拉取远程合并
  • 合并方式: rebase/fast-forward
  • 冲突: 无 ✅ (或需要解决)
```

#### 🌐 第六步：推送到远程

```bash
git push origin main
```

**AI响应**:
```
🌐 第六步：推送远程
  • 推送结果: ✅ 成功
  • 本地哈希: abc123...
  • 远程哈希: abc123...
  • 同步状态: ✅ 完全一致
```

---

## 🔥 第五层：AI自动识别与响应系统 (AI Auto-Recognition)

### 强制响应模板 (Mandatory Response Template)

**AI在每次编程任务时必须使用此模板：**

```markdown
🔥 **AI编程铁律自动执行引擎已启动！**

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 第一阶段：编程前强制学习（Pre-Coding Learning）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 1. 架构扫描完成
   📦 packages目录: 5个核心package已识别
   📄 组件库: XX个组件，XX个工具函数
   🔍 依赖层级: 正确

✅ 2. 代码库检索完成
   📋 已有类型: [列出]
   🔧 已有工具: [列出]
   🎨 已有组件: [列出]
   ⚠️ 相似功能: [列出或无]

✅ 3. ADR学习完成
   📖 技术栈: ABP vNext + Vue3 + TS
   📖 架构原则: packages黑盒原则
   📖 约束条件: [列出]

✅ 4. 模板库检查完成
   📝 匹配模板: [有/无]
   💡 建议使用: [模板路径或手动编写]

✅ 5. 公共组件识别完成
   🎨 可复用: [列出]
   ⚠️ 避免重复

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔧 第二阶段：增量迭代编程（Incremental Programming）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 代码行数追踪: 0/300行
✅ 架构合规检查: 实时监控中
✅ 代码重复检测: 已激活

[开始编程...]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚨 第三阶段：质量门禁检查（Quality Gate）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 触发条件: [完成开发阶段/300行代码]

🏗️ 第一关：架构完整性 - ✅ 通过 (0违规)
🔄 第二关：代码重复度 - ✅ 通过 (0重复)
⚡ 第三关：编译静态检查 - ✅ 通过 (0错误)
🎯 第四关：低代码生成器 - ✅ 通过 (100%质量)
🚀 第五关：技术债务 - ✅ 通过 (XX/100分)

✅ 五重质量门禁全部通过！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔄 第四阶段：Git版本管理（Git Management）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 第一步：代码变更检测 - ✅ XX个文件
📦 第二步：暂存修改 - ✅ 完成
📝 第三步：生成提交信息 - ✅ 完成
🚀 第四步：本地提交 - ✅ abc123...
🔄 第五步：拉取合并 - ✅ 无冲突
🌐 第六步：推送远程 - ✅ 同步完成

✅ Git版本管理完成！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎉 AI编程铁律闭环执行完成！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 执行摘要:
  ✅ 编程前学习: 5项全部完成
  ✅ 增量编程: XX行代码，0违规
  ✅ 质量门禁: 5关全部通过
  ✅ Git管理: 6步完整执行

🚀 代码已同步到远程仓库，可继续下一阶段开发！
```

---

## 🎯 关键成功因素 (Critical Success Factors)

### 1. AI必须在响应开头立即声明

```
🔥 **AI编程铁律自动执行引擎已启动！**
```

### 2. AI必须完整执行所有检查清单

- ✅ 不能跳过任何步骤
- ✅ 不能简化响应
- ✅ 必须明确列出结果

### 3. AI必须在达到触发条件时立即停止

```
🚨 已达到300行代码限制
⏸️ 立即停止编码
🔍 开始执行质量门禁检查
```

### 4. AI必须使用标准化响应格式

- ✅ 使用emoji标识
- ✅ 使用分隔线区分阶段
- ✅ 使用勾选框表示完成状态

---

## 🔄 闭环完整性验证 (Loop Integrity Verification)

### 闭环检查清单

**确保以下流程完整闭合：**

```
编程触发
  ↓
✅ 第一层：编程前学习 (5项检查)
  ↓
✅ 第二层：增量编程 (行数追踪+实时检查)
  ↓
🚨 触发条件: 300行 OR 阶段完成
  ↓
✅ 第三层：质量门禁 (5关检查)
  ↓
✅ 第四层：Git管理 (6步执行)
  ↓
✅ 第五层：自动响应 (标准模板)
  ↓
🔄 返回第二层 (继续下一阶段)
```

---

## 🛡️ 立体同心圆模型集成 (Concentric Circle Model Integration)

### 保留原有L0-L7层级结构

```
L0: 核心理念层 ← 质量至上/架构先行/独立决策
L1: 代码形态层 ← 命名规范/样式指南
L2: 工作流层 ← 标准开发流程
L3: 质量守护层 ← 质量门禁
L4: 代码质量禁止层 ← 类型安全/架构违规
L5: 增量迭代质量门禁 ← 300行节奏律
L6: 低代码生成器质量 ← packages专项
L7: AI架构保护 ← 自动识别保护

新增: AI自动执行引擎层 ← 贯穿L0-L7的自动化执行系统
```

### 与现有铁律的协同

- **00_core_philosophy.mdc**: 提供核心理念
- **01_code_standards.mdc**: 提供编码标准
- **02_development_process.mdc**: 提供开发流程
- **03_quality_guardian.mdc**: 提供质量检查
- **05_增量迭代开发质量门禁与GIT版本管理铁律.mdc**: 提供门禁和Git流程
- **07_AI编程架构自动识别保护铁律.mdc**: 提供架构保护

**本文件**: 将所有铁律串联，形成AI可自动执行的完整闭环

---

## 🚀 立即生效 (Immediate Effectiveness)

**本执行引擎立即生效！**

所有AI编程任务必须：
1. ✅ 启动时声明引擎已激动
2. ✅ 执行前完成5项学习
3. ✅ 编程中实时监控
4. ✅ 触发时执行质量门禁
5. ✅ 完成后执行Git管理
6. ✅ 使用标准响应模板

**违规后果**: AI响应将被视为不完整，必须重新执行。

**执行承诺**: 确保项目开发规则铁律100%被AI识别和执行！

---

🛡️ **SmartAbp AI编程铁律闭环 - 完整、自动、强制执行！**