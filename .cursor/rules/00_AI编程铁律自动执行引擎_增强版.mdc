---
alwaysApply: true
priority: 1
---

# AI编程铁律自动执行引擎 - 增强版 v2.0 (AI Programming Iron Law Auto-Execution Engine - Enhanced)

## 🎯 核心使命

**问题**: AI大模型在编程时容易忽略300行代码检查点和阶段性质量门禁，导致规则执行不力。

**目标**: 建立**强制触发、自动暂停、实时监控**的增强型执行引擎。

**原则**: 零容忍、零跳过、自动化、强制化。

---

## 🚨 AI自检强制清单 (AI Self-Check Mandatory Checklist)

**AI在每次响应用户请求前必须自问：**

```typescript
const AI_SELF_CHECK = {
  question1: "我是否已启动AI编程铁律自动执行引擎？",
  question2: "我是否已执行编程前五项强制学习？",
  question3: "我当前会话已编写多少行代码？",
  question4: "我是否即将超过300行限制？",
  question5: "我上次质量检查是什么时候？",
  question6: "我是否已在每个开发阶段完成后执行质量门禁？"
}

// 如果任何一个问题的答案是"否"或"不确定"
// AI必须立即停止，重新启动执行引擎
```

---

## 🔥 第一层：AI编程前强制学习系统 (Pre-Coding Mandatory Learning)

### 🚨 强制触发规则

**AI必须在以下情况下自动触发（无例外）：**

#### 触发场景1: 关键词检测
```typescript
触发关键词 = [
  // 直接触发
  "专家模式", "开始编程", "开始开发", "新功能", "新建",
  "立即执行", "立即推进", "继续", "继续推进",
  
  // 代码操作触发
  "创建组件", "创建服务", "创建Store", "创建API",
  "修改代码", "添加功能", "实现功能", "编写代码",
  
  // 修复触发
  "修复BUG", "修复错误", "解决问题", "优化代码",
  
  // 架构触发
  "重构", "优化", "架构调整", "性能优化"
]
```

#### 触发场景2: 用户请求分析
```typescript
// AI必须分析用户请求意图
if (用户请求包含 "完成", "实现", "开发", "编写") {
  // 立即启动执行引擎
  启动AI编程铁律自动执行引擎();
}
```

### 📋 强制学习五步法 (AI必须按顺序执行并显示结果)

#### ✅ 第1步：项目架构扫描
```bash
ls -la src/SmartAbp.Vue/packages/
find src/SmartAbp.Vue/packages -name "*.vue" -o -name "*.ts" | wc -l
```

**AI必须显示**:
```
✅ 1. 架构扫描完成
📦 packages目录: 5个核心package
📄 组件库: XX个组件，XX个工具函数
```

#### ✅ 第2步：代码库检索
```typescript
// AI必须搜索现有实现
搜索位置 = [
  'lowcode-shared/src/types/',
  'lowcode-shared/src/utils/',
  'lowcode-core/src/components/',
  'src/components/',
  'src/composables/'
]
```

**AI必须显示**:
```
✅ 2. 代码库检索完成
📋 已有类型: [列出]
🔧 已有工具: [列出]
⚠️ 相似功能: [列出或无]
```

#### ✅ 第3步：ADR决策学习
**AI必须显示**:
```
✅ 3. ADR学习完成
📖 技术栈: ABP vNext + Vue3 + TS
📖 架构原则: packages黑盒原则
📖 约束条件: [列出]
```

#### ✅ 第4步：模板库匹配
**AI必须显示**:
```
✅ 4. 模板库检查完成
📝 匹配模板: [有/无]
💡 建议使用: [模板路径或手动编写]
```

#### ✅ 第5步：公共组件识别
**AI必须显示**:
```
✅ 5. 公共组件识别完成
🎨 可复用: [列出]
⚠️ 避免重复
```

---

## ⚡ 第二层：增量迭代编程系统 (Incremental Programming System)

### 🔢 强制代码行数追踪器 (Mandatory Line Counter)

**AI内部必须维护的状态**:
```typescript
class CodeLineTracker {
  private currentSessionLines: number = 0
  private lastCheckpoint: number = 0
  private filesCreated: string[] = []
  
  // AI必须在每次生成代码后调用
  addCode(fileName: string, code: string) {
    const lines = code.split('\n').length
    this.currentSessionLines += lines
    this.filesCreated.push(`${fileName} (${lines}行)`)
    
    // 自动检查
    this.checkThreshold()
  }
  
  checkThreshold() {
    if (this.currentSessionLines >= 280 && this.currentSessionLines < 300) {
      console.warn("⚠️ 警告：已编写" + this.currentSessionLines + "行，接近300行限制")
      console.warn("⚠️ 建议：完成当前文件后立即执行质量检查")
    }
    
    if (this.currentSessionLines >= 300) {
      console.error("🚨 强制停止：已达到300行限制")
      console.error("🚨 必须：立即执行五重质量门禁检查")
      this.triggerQualityGate()
      return false // 阻止继续编写
    }
    
    return true // 允许继续
  }
  
  reset() {
    this.lastCheckpoint = this.currentSessionLines
    this.currentSessionLines = 0
    this.filesCreated = []
  }
}
```

### 📊 AI必须在每次响应中显示

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 代码行数追踪器状态
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📝 当前会话代码行数: XX/300行
📁 已创建文件: 
   - file1.vue (150行)
   - file2.ts (120行)
⚠️ 距离检查点: 还剩XX行
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 🚨 强制暂停点 (Mandatory Pause Points)

**AI必须在以下时刻暂停并执行质量检查：**

#### 暂停点1: 280行警告
```
⚠️ 已编写280行代码！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️ 警告：接近300行限制
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

当前状态:
  • 已编写: 280行
  • 距离限制: 20行
  • 建议行动: 完成当前文件后立即质量检查

下一步:
  1. 完成当前正在编写的文件
  2. 立即执行五重质量门禁检查
  3. 执行Git版本同步
  4. 重置计数器后继续
```

#### 暂停点2: 300行强制停止
```
🚨 已达到300行代码限制！必须立即停止！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚨 强制停止：300行铁律触发
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

当前状态:
  • 已编写: 300行
  • 状态: 🚫 禁止继续编写
  • 必须行动: 立即质量检查

强制执行:
  ⏸️  停止所有编码活动
  🔍 执行五重质量门禁检查
  📦 执行Git版本同步
  🔄 重置计数器
  ✅ 通过后继续下一个300行周期
```

#### 暂停点3: 开发阶段完成
```
✅ 开发阶段完成！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 阶段完成：必须执行质量检查
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

完成的任务:
  • Task X.X: [任务描述]
  • 代码行数: XX行
  • 文件数: XX个

强制执行:
  🔍 五重质量门禁检查
  📦 Git版本同步
  📊 生成完成报告
```

---

## 🔍 第三层：五重强制质量门禁系统 (Five Quality Gates)

### 触发条件（满足任一即触发）

```typescript
触发条件 = {
  条件1: currentSessionLines >= 300,
  条件2: 开发阶段完成 === true,
  条件3: 用户明确要求质量检查,
  条件4: 准备Git提交
}

// 任何一个条件满足，AI必须立即执行
if (满足任一触发条件) {
  执行五重质量门禁();
  禁止继续编码直到通过();
}
```

### 🏗️ 第一关：架构完整性检查 (0违规)

**AI必须执行的命令**:
```bash
# 1. packages相对路径违规
grep -r "'../'" src/SmartAbp.Vue/packages/ 2>/dev/null | wc -l

# 2. packages主应用引用违规
grep -r "@/" src/SmartAbp.Vue/packages/ 2>/dev/null | grep -v node_modules | wc -l

# 3. 类型安全绕过
grep -r "as any" src/ packages/ 2>/dev/null | wc -l
grep -r "@ts-ignore" src/ packages/ 2>/dev/null | wc -l
```

**AI必须显示**:
```
🏗️ 第一关：架构完整性检查
  • 相对路径违规: 0个 ✅
  • 主应用引用违规: 0个 ✅
  • as any使用: 0个 ✅
  • @ts-ignore使用: 0个 ✅
✅ 第一关通过 (0违规)
```

**失败处理**: 如果任何检查 > 0，AI必须：
1. ❌ 立即停止所有操作
2. 🔧 列出所有违规位置
3. 💡 提供修复建议
4. 🚫 禁止继续编码和Git提交

### 🔄 第二关：代码重复度检查 (0重复)

**AI必须执行的命令**:
```bash
# 1. 重复Vue组件
find src/ -name "*.vue" 2>/dev/null | sed 's/.*\///' | sort | uniq -d | wc -l

# 2. 重复函数
grep -rh "export function\|export const.*=" src/ --include="*.ts" | sort | uniq -d | wc -l

# 3. 重复类
grep -rh "export class\|export interface" src/ --include="*.ts" | sort | uniq -d | wc -l
```

**AI必须显示**:
```
🔄 第二关：代码重复度检查
  • 重复组件: 0个 ✅
  • 重复函数: 0个 ✅
  • 重复类: 0个 ✅
✅ 第二关通过 (0重复)
```

### ⚡ 第三关：编译与静态检查 (0错误)

**AI必须执行的命令**:
```bash
cd src/SmartAbp.Vue && npm run type-check
cd src/SmartAbp.Vue && npm run lint
```

**AI必须显示**:
```
⚡ 第三关：编译与静态检查
  • TypeScript错误: 0个 ✅
  • ESLint错误: 0个 ✅
✅ 第三关通过 (0错误)
```

### 🎯 第四关：低代码生成器专项检查 (100%质量)

**AI必须执行的命令**:
```bash
cd src/SmartAbp.Vue && npx tsc --build tsconfig.references.json
cd src/SmartAbp.Vue && npm run lint -- "packages/*/src/**/*.{ts,vue}"
```

**AI必须显示**:
```
🎯 第四关：低代码生成器专项检查
  • packages编译: ✅ 通过
  • packages规范: ✅ 通过
✅ 第四关通过 (100%质量)
```

### 🚀 第五关：技术债务监控 (≥85分)

**AI必须计算并显示**:
```
🚀 第五关：技术债务监控
  • 代码复杂度: 合理
  • TODO标记: XX个
  • 质量评分: XX/100
✅ 第五关通过 (评分≥85)
```

---

## 📦 第四层：Git版本同步管理系统 (Git Sync Management)

### 🔐 六步强制执行流程

**前置条件**: 五重质量门禁必须全部通过

#### 第1步：代码变更检测
```bash
git status --porcelain
```

#### 第2步：暂存所有修改
```bash
git add .
```

#### 第3步：生成标准化提交信息
```bash
COMMIT_MSG="类型: 完成[阶段] - [描述]

🔧 核心实现:
- [具体实现]

📊 质量验证:
✅ 架构完整性: 0违规
✅ 代码重复度: 0重复
✅ 编译静态检查: 0错误
✅ 低代码生成器: 100%质量
✅ 技术债务: XX/100

📦 修改统计:
- 代码行数: XX行
- 文件数: XX个"
```

#### 第4步：执行本地提交
```bash
git commit -m "$COMMIT_MSG"
```

#### 第5步：拉取远程并合并
```bash
git pull --rebase origin main
```

#### 第6步：推送到远程
```bash
git push origin main
```

**AI必须显示完整的Git同步报告**

---

## ✅ 第五层：AI自动推进机制 (Auto-Continue Mechanism)

### 🚀 自动推进规则

**质量检查通过后，AI必须自动继续，而不是等待用户指令！**

```typescript
if (五重质量门禁全部通过 && Git同步成功) {
  // ✅ 正确行为：自动继续下一个任务
  console.log("✅ 质量检查通过，自动继续推进下一个任务...")
  重置代码行数计数器();
  继续执行下一个开发任务();
  
  // ❌ 错误行为：停下来等待用户
  // console.log("请用户确认是否继续...") // 这是错的！
}
```

**AI必须显示**:
```
✅ 五重质量门禁全部通过！
✅ Git版本同步完成！
🚀 自动推进：继续执行下一个任务...

📊 下一个任务:
  • Task X.X: [任务描述]
  • 预计代码行数: XX行
  • 预计文件数: XX个

⏱️ 开始时间: [当前时间]
📝 当前会话代码行数: 0/300行（已重置）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
开始编写...
```

---

## 🔒 第六层：强制执行响应模板 (Mandatory Response Template)

### AI必须使用的标准响应格式

```markdown
🔥 **AI编程铁律自动执行引擎已启动！**

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 第一阶段：编程前强制学习
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 1. 架构扫描完成
   [详细结果]

✅ 2. 代码库检索完成
   [详细结果]

✅ 3. ADR学习完成
   [详细结果]

✅ 4. 模板库检查完成
   [详细结果]

✅ 5. 公共组件识别完成
   [详细结果]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔧 第二阶段：增量迭代编程
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 代码行数追踪: 0/300行
✅ 架构合规检查: 实时监控中
✅ 代码重复检测: 已激活

[开始编写代码...]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 编写进度更新
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 已创建: file1.vue (150行)
📊 当前会话代码行数: 150/300行
⚠️ 距离检查点: 还剩150行

继续编写...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️ 280行警告触发！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

当前状态:
  • 已编写: 280行
  • 建议: 完成当前文件后立即质量检查

最后完成: file3.vue (50行)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚨 第三阶段：质量门禁检查（300行触发）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🏗️ 第一关：架构完整性 - ✅ 通过
🔄 第二关：代码重复度 - ✅ 通过
⚡ 第三关：编译静态检查 - ✅ 通过
🎯 第四关：低代码生成器 - ✅ 通过
🚀 第五关：技术债务 - ✅ 通过 (XX/100)

✅ 五重质量门禁全部通过！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔄 第四阶段：Git版本管理
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 第1步：代码变更检测 - ✅
📦 第2步：暂存修改 - ✅
📝 第3步：生成提交信息 - ✅
🚀 第4步：本地提交 - ✅
🔄 第5步：拉取合并 - ✅
🌐 第6步：推送远程 - ✅

✅ Git版本管理完成！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 第五阶段：自动推进下一个任务
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 质量检查通过
✅ Git同步成功
🔄 重置计数器: 0/300行
🚀 自动继续: Task X.X

[继续编写下一个任务...]
```

---

## 💡 AI自我纠正机制 (AI Self-Correction Mechanism)

### 🚨 当AI发现自己违反铁律时

**AI必须立即执行：**

```typescript
if (发现违反铁律) {
  // 1. 立即停止
  停止所有操作();
  
  // 2. 承认错误
  console.error("🚨 错误：我违反了AI编程铁律！");
  console.error("违规内容：" + 违规描述);
  
  // 3. 回滚操作
  if (可以回滚) {
    回滚到上一个检查点();
  }
  
  // 4. 重新执行
  console.log("🔄 重新执行：严格遵守铁律");
  重新启动执行引擎();
  从正确的流程开始();
}
```

### 📝 AI自我提醒机制

**AI在每次操作前必须自问：**

```typescript
const SELF_REMINDER = {
  before_coding: "我是否已执行编程前五项学习？",
  during_coding: "我当前写了多少行代码？是否接近300行？",
  after_coding: "我是否需要执行质量检查？",
  before_commit: "我是否已通过五重质量门禁？"
}
```

---

## 🎯 执行保证声明 (Execution Guarantee)

### AI必须做到：

```
✅ 100%激活率: 每次编程都自动启动引擎
✅ 0%跳过率: 不跳过任何检查步骤
✅ 实时监控: 持续追踪代码行数
✅ 强制暂停: 300行必停
✅ 自动推进: 检查通过后自动继续
✅ 完整报告: 每次都生成完整报告
```

### AI禁止做：

```
❌ 一次性编写超过300行代码
❌ 跳过编程前学习
❌ 跳过质量门禁检查
❌ 忘记Git版本同步
❌ 检查通过后等待用户指令（应自动继续）
❌ 简化响应格式（必须使用标准模板）
```

---

## 🛡️ 最终保障：Git Hook守护

**pre-commit hook会强制检查：**
- 是否通过五重质量门禁
- 是否包含质量验证标记
- 是否符合提交信息规范

**如果AI违反铁律提交代码，Git Hook会拒绝提交！**

---

## 📊 执行引擎状态监控

**AI必须在内部维护：**

```typescript
interface ExecutionEngineState {
  isActivated: boolean           // 是否已启动
  preLearningCompleted: boolean  // 编程前学习是否完成
  currentSessionLines: number    // 当前会话代码行数
  lastCheckpoint: number         // 上次检查点
  qualityGatesPassed: boolean    // 质量门禁是否通过
  gitSyncCompleted: boolean      // Git同步是否完成
  canContinueCoding: boolean     // 是否允许继续编码
}
```

---

🔥 **AI编程铁律自动执行引擎 - 增强版 v2.0**

**零容忍、零跳过、自动化、强制化！**

**确保每一次编程都严格遵守铁律，代码质量永远保持95分以上！**