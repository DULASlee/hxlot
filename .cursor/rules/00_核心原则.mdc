---
alwaysApply: true
priority: 0
---

# AI编程核心原则（12条铁律）

## 📋 文档说明

**版本**: v4.0 极简版
**核心**: 12条铁律（从23条精简52%）
**理念**: 只保留绝对不能违反的底线
**更新日期**: 2025-10-13

---

## 🚨 四条新增铁律（优先级-3，最高）

### 第一条 禁止创建文档和测试代码

```yaml
没有用户明确要求，严禁创建任何文档和测试代码
AI的核心职责是编写生产代码
```

### 第二条 禁止自动Git提交

```yaml
严禁未经用户明确授权就提交代码到Git远程仓库
Git提交必须由用户主动触发或明确授权
```

### 第三条 鼓励主动优化建议

```yaml
主动提出架构优化、代码重构、性能优化建议
发现代码问题时，主动指出并提供改进方案
```

### 第四条 鼓励主动测试代码

```yaml
编程完成后主动测试代码功能
主动验证正确性、性能和用户体验，发现问题立即修复
```

---

## 🏛️ 架构三大铁律（优先级-2，格杀勿论）

### 第五条 统一类型系统（SSOT）

```yaml
⭐ v20.0更新（2025-10-16）：
  - metadata-core已废弃 ❌
  - lowcode-shared为唯一真实来源（SSOT）✅

所有类型必须在 @smartabp/lowcode-shared 定义：
  - 统一Schema: unified-schema.ts
  - 枚举定义: enums.ts（25个枚举）⭐NEW⭐
  - 验证系统: validation/
  - 版本管理: version/
  - 国际化错误消息: error-messages.ts ⭐NEW⭐

必须使用 @smartabp/* 别名（别名已包含src/，不再写/src/）:
  ✅ 正确：import { DatabaseType } from '@smartabp/lowcode-shared'
  ✅ 正确：import { UnifiedEntityDefinition } from '@smartabp/lowcode-shared'
  ❌ 错误：import { ... } from '@smartabp/lowcode-shared/src/types/...'
  ❌ 错误：import { ... } from '@smartabp/metadata-core'（已废弃）

补充（已实施）：核心业务规则类型（RuleNodeData 等）统一由 `@smartabp/lowcode-core` 根导出，设计器节点等禁止跨包使用 `/src` 路径。
```

### 第六条 组件注册系统

```yaml
所有组件必须注册到 ComponentRegistry
必须提供完整 ComponentMetadata
严禁跳过注册直接使用组件
```

### 第七条 架构层级依赖

```yaml
⭐ v20.0更新（2025-10-16）：
  - metadata-core已从依赖链中移除 ❌
  - lowcode-shared为Layer 0（最底层）✅

依赖层级（只能向下依赖）:
  Layer 2: lowcode-designer（设计器）
  Layer 1: lowcode-core, lowcode-api, lowcode-tools（核心/API/工具）
  Layer 0: lowcode-shared（共享层，SSOT）✅

严禁:
  ❌ 逆向依赖（shared依赖core）
  ❌ 循环依赖（A→B→A）
  ❌ 相对路径跨包引用（'../'）
  ❌ @别名在packages中使用（@/）
  ❌ 引用metadata-core（已废弃）

必须使用 @smartabp/* 别名进行包间通信:
  ✅ 正确：import { DatabaseType } from '@smartabp/lowcode-shared'
  ✅ 正确：import { xxx } from '@smartabp/lowcode-core/components/...'
  ❌ 错误：import { xxx } from '@smartabp/lowcode-core/src/components/...'
  ❌ 错误：import { xxx } from '../../packages/lowcode-core/src/...'

补充（已实施）：`@smartabp/lowcode-designer`、`@smartabp/lowcode-core` 在 tsconfig 与 Vite 中统一指向包根与 src 目录的顶层别名；禁止任何 `packages/**` 相对路径跨包导入。
```

---

## 🛡️ P0阻断性规则（优先级0，零容忍）

> **违反任何一条 = 禁止提交代码**

### 第八条 类型安全铁律

```yaml
TypeScript编译必须0错误
严禁使用 as any 和 @ts-ignore
所有函数必须有类型定义

检测: cd src/SmartAbp.Vue && npm run type-check
```

### 第九条 架构完整性铁律

```yaml
packages必须黑盒独立（无../、无@/引用）
依赖层级正确（shared→core→designer，只能向下依赖）
使用@smartabp/别名进行包间通信

检测:
  grep -r "'../'" src/SmartAbp.Vue/packages/
  grep -r "@/" src/SmartAbp.Vue/packages/
```

### 第十条 功能完整性铁律

```yaml
完整链路: 菜单→UI→API→Service→DB，9层完整不可缺
真实事件: 所有控件必须有真实事件绑定，严禁空方法
真实数据: 所有数据来自真实API，严禁Mock数据
类型完整: 100%类型安全，DTO前后端一致
后端完整: Controller+Service+Repository+DB迁移
验收标准: 点击能打开、按钮有响应、数据能保存（95分质量）
淘汰机制: 花瓶式实现（空方法、TODO、Mock）= 不合格
```

### 第十一条 Git安全铁律

```yaml
Git操作必须先pull后push
使用标准化脚本（git-safe-sync.ps1）
严禁直接push（跳过pull）
严禁force push到main/master

执行: pwsh -File scripts/git/git-safe-sync.ps1 -AutoCommit
```

### 第十二条 代码保护铁律

```yaml
AI权限: 只能创建、修改、重构、注释代码，严禁删除任何代码和文件
优先注释: 需要移除功能时，先注释旧代码并标记原因、日期
保留历史: 重构时必须保留旧代码注释
四次确认: 必须删除时需完整流程（AI说明→用户确认→AI展示→最终确认）
正确做法: 标记"🟡 旧实现（已重构）"+ 注释 + 保留完整代码
```

---

## 📜 AI执行承诺

```yaml
我承诺:
  ✅ 严格遵循12条铁律
  ✅ 独立技术判断，不盲从不合理要求
  ✅ 提供专业建议和替代方案
  ✅ 做智能助手，不做规则执行机器
  ✅ 代码质量≥95分
  ✅ DRY原则（创建前必搜索，优先复用）
  ✅ 增量编程（300行机制）

我不会:
  ❌ 僵化执行所有规则
  ❌ 对简单任务过度分析
  ❌ 承诺做不到的事（跨会话、定时任务、后台监控）
  ❌ 盲目服从违反核心原则的要求
  ❌ 未经用户同意创建文档和测试代码
  ❌ 未经用户授权提交代码到远程仓库
```

---

**核心思想：简单、清晰、实用、灵活**

**让AI成为真正的智能助手，而不是规则的奴隶！**
