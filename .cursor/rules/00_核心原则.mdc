---
alwaysApply: true
priority: 0
---

# AI编程核心原则（14条铁律）

## 📋 文档说明

**版本**: v4.2 终端优化版
**核心**: 14条铁律（新增终端命令优化规则）
**理念**: 只保留绝对不能违反的底线
**更新日期**: 2025-10-18

---

## 🚨 六条新增铁律（优先级-3，最高）

### 第一条 禁止创建文档和测试代码

```yaml
没有用户明确要求，严禁创建任何文档和测试代码
AI的核心职责是编写生产代码
```

### 第二条 禁止自动Git提交

```yaml
严禁未经用户明确授权就提交代码到Git远程仓库
Git提交必须由用户主动触发或明确授权
```

### 第三条 鼓励主动优化建议

```yaml
主动提出架构优化、代码重构、性能优化建议
发现代码问题时，主动指出并提供改进方案
```

### 第四条 鼓励主动测试代码

```yaml
编程完成后主动测试代码功能
主动验证正确性、性能和用户体验，发现问题立即修复
```

### 第五条 文档增量编写（防止超时）

```yaml
编写大型文档时必须增量编写，每次不超过1000行
避免上下文长度限制和超时断线
分批编写，确保每次写入成功
用户确认后再继续下一批次

✅ 强制规则:
  1. 单次写入文档不超过1000行
  2. 写入前预估总行数，规划批次
  3. 每个批次写入后等待用户确认
  4. 明确告知用户当前进度（如：第1/3批次）
  5. 保持批次间的内容连贯性

📊 执行检查清单:
  ☑️ 文档总行数是否>1000？（需要分批）
  ☑️ 是否告知用户总批次数？
  ☑️ 是否在批次结束后暂停等待确认？
  ☑️ 是否标记了当前进度？
  ☑️ 批次间是否保持了上下文连贯？

💡 分批策略:
  • 1-1000行：一次完成
  • 1001-3000行：分3批（每批约1000行）
  • 3001-5000行：分5批（每批约1000行）
  • >5000行：建议拆分为多个文档

⚠️ 注意事项:
  - 在代码块、列表、表格等结构中间不要切断
  - 每批次结束位置选择章节或小节边界
  - 最后一批次可以少于1000行
  - 出现超时时立即停止，下次从断点继续
```

### 第六条 终端命令优化（防止卡死） ⭐NEW⭐

```yaml
AI执行终端命令时必须优化参数，避免长时间卡死：

✅ 强制规则:
  1. 编译命令必须限时限量（避免长时间输出）
  2. 使用 --verbosity quiet/minimal 减少输出
  3. 分离编译和运行步骤（先compile后run）
  4. 长时间命令必须后台执行或分批处理
  5. 检查命令是否必要（能用文件工具就不用terminal）

❌ 禁止行为:
  - 直接执行可能长时间运行的命令（dotnet build不带quiet）
  - 不优化输出参数导致大量日志阻塞
  - 等待命令完成超过30秒不做任何处理
  - 重复执行相同的长时间命令

🔧 具体优化策略:

  后端编译:
    ✅ dotnet build --verbosity quiet --nologo
    ✅ dotnet build --verbosity minimal --no-incremental
    ❌ dotnet build（输出过多）

  前端构建:
    ✅ npm run build --silent
    ✅ pnpm build --silent
    ❌ npm run build（输出过多）

  Git操作:
    ✅ git status --short
    ✅ git commit --quiet
    ✅ git push --quiet
    ❌ git push --verbose（输出过多）

  长时间命令:
    1. 提前告知用户"正在执行，请稍候"
    2. 使用 --quiet/--silent 参数
    3. 只显示最终结果
    4. 错误时显示完整输出

  超时处理:
    - 命令执行前预估时间
    - 超过30秒未完成时主动汇报进度
    - 提供取消选项或重试策略
    - 记录卡死原因，下次避免

📊 执行检查清单:
  ☑️ 命令是否必要？（能用文件工具吗？）
  ☑️ 输出是否会很多？（需要优化参数吗？）
  ☑️ 是否使用了 --quiet/--silent/--verbosity minimal？
  ☑️ 是否告知用户正在执行？
  ☑️ 是否有超时处理机制？
```

---

## 🏛️ 架构三大铁律（优先级-2，格杀勿论）

### 第七条 后端SSOT驱动的契约类型系统

```yaml
⭐ Phase 3C架构重构（2025-10-18）：
  - metadata-core已废弃 ❌
  - 后端C# DTO为唯一真实来源（SSOT）✅
  - packages使用独立契约类型（backend-contracts.ts）✅

类型定义三层架构:

  Layer 1 - 后端SSOT（ABP vNext DDD - 98/100分）:
    位置: src/SmartAbp.Domain/Entities/LowCode/*.cs
    标记: [GenerateSwaggerSchema]
    评分: 100/100（完全符合ABP vNext + DDD最佳实践）

  Layer 2 - 前端契约层（31级AlphaGO最优解 - 95/100分）:
    位置: packages/lowcode-shared/src/types/backend-contracts.ts
    内容: 45个独立契约类型（EntityDefinitionDto, ModuleDto等）
    特点: 零外部依赖，100%后端DTO一致性

  Layer 3 - 主应用生成层（NSwag自动化 - 100/100分）:
    位置: src/SmartAbp.Vue/src/api/generated/
    工具: openapi-typescript-codegen
    用途: 仅主应用API调用

必须使用 @smartabp/* 别名:
  ✅ 正确：import { ModuleDto, EntityDefinitionDto } from '@smartabp/lowcode-shared'
  ✅ 正确：import type { ValidationRule } from '@smartabp/lowcode-shared'
  ❌ 错误：import { ModuleDto } from '@/api/generated/models'（packages中）
  ❌ 错误：import { ... } from '@smartabp/metadata-core'（已废弃）
  ❌ 错误：import { ... } from '../../../src/api/generated'（破坏黑盒）

SSOT流程保证:
  后端C# DTO → NSwag扫描 → Swagger JSON →
  openapi-typescript-codegen → TS类型生成 →
  packages契约层精确映射 → 100%类型一致性
```

### 第八条 组件注册系统

```yaml
所有组件必须注册到 ComponentRegistry
必须提供完整 ComponentMetadata
严禁跳过注册直接使用组件
```

### 第九条 前后端分离的架构层级体系

```yaml
⭐ Phase 3C架构重构（2025-10-18）：
  - metadata-core已从依赖链中移除 ❌
  - 后端SSOT + 前端契约层双重架构 ✅
  - packages完全黑盒独立（零主应用依赖）✅

后端架构层级（ABP vNext DDD - 98/100分）:
  Layer 4: HttpApi（控制器层）
  Layer 3: Application（应用服务层）
  Layer 2: Domain（领域层 - 核心SSOT）
  Layer 1: Domain.Shared（共享基础）
  Layer 0: EntityFrameworkCore（基础设施层）

  ✅ 完全符合DDD六边形架构
  ✅ 依赖反转原则（Domain不依赖EF Core）
  ✅ Repository仓储模式（100%接口驱动）

前端packages层级（31级AlphaGO最优解 - 95/100分）:
  Layer 2: lowcode-designer（设计器UI层）
  Layer 1: lowcode-core, lowcode-api, lowcode-tools（核心逻辑层）
  Layer 0: lowcode-shared（契约类型层）

  ✅ 契约驱动架构（backend-contracts.ts）
  ✅ 完全黑盒独立（零src/依赖）
  ✅ 100%后端DTO一致性

严禁（格杀勿论）:
  ❌ packages引用src/api/generated（破坏黑盒独立）
  ❌ 逆向依赖（shared依赖core）
  ❌ 循环依赖（A→B→A）
  ❌ 相对路径跨包引用（'../'）
  ❌ 主应用别名在packages中（@/）
  ❌ 引用metadata-core（已废弃）
  ❌ 契约类型与后端DTO不一致

必须使用 @smartabp/* 别名:
  ✅ 正确：import { ModuleDto } from '@smartabp/lowcode-shared'
  ✅ 正确：import { SmartForm } from '@smartabp/lowcode-core'
  ❌ 错误：import { ModuleDto } from '@/api/generated/models'（packages中）
  ❌ 错误：import { xxx } from '../../packages/lowcode-core/src/'
```

---

## 🛡️ P0阻断性规则（优先级0，零容忍）

> **违反任何一条 = 禁止提交代码**

### 第十条 类型安全铁律

```yaml
TypeScript编译必须0错误
严禁使用 as any 和 @ts-ignore
所有函数必须有类型定义

检测: cd src/SmartAbp.Vue && npm run type-check
```

### 第十一条 架构完整性铁律（Phase 3C契约系统标准）

```yaml
后端ABP vNext架构（98/100分，业界顶级）:
  ✅ DDD分层架构（Domain为核心）
  ✅ Repository仓储模式（100%接口驱动）
  ✅ AutoMapper配置完整（DTO↔Entity映射）
  ✅ CQRS查询分离（GetList分页+筛选+排序）
  ✅ 单元工作模式（UnitOfWork事务管理）

前端packages架构（95/100分，31级AlphaGO最优解）:
  ✅ 完全黑盒独立（零src/依赖）
  ✅ 契约类型系统（backend-contracts.ts）
  ✅ 依赖层级正确（shared→core→designer）
  ✅ 使用@smartabp/别名通信
  ✅ 100%后端DTO一致性

强制检测（五关门禁）:
  第一关: grep -r "@/api/generated" src/SmartAbp.Vue/packages/  # 必须为0
  第二关: grep -r "@smartabp/metadata-core" packages/  # 必须为0
  第三关: grep -r "'../'" src/SmartAbp.Vue/packages/  # 必须为0
  第四关: npx tsc --build tsconfig.references.json  # 0错误
  第五关: dotnet build src/SmartAbp.sln --verbosity quiet  # 0错误
```

### 第十二条 功能完整性铁律

```yaml
完整链路: 菜单→UI→API→Service→DB，9层完整不可缺
真实事件: 所有控件必须有真实事件绑定，严禁空方法
真实数据: 所有数据来自真实API，严禁Mock数据
类型完整: 100%类型安全，DTO前后端一致
后端完整: Controller+Service+Repository+DB迁移
验收标准: 点击能打开、按钮有响应、数据能保存（95分质量）
淘汰机制: 花瓶式实现（空方法、TODO、Mock）= 不合格
```

### 第十三条 Git安全铁律

```yaml
Git操作必须先pull后push
使用标准化脚本（git-safe-sync.ps1）
严禁直接push（跳过pull）
严禁force push到main/master

执行: pwsh -File scripts/git/git-safe-sync.ps1 -AutoCommit
```

### 第十四条 代码保护铁律

```yaml
AI权限: 只能创建、修改、重构、注释代码，严禁删除任何代码和文件
优先注释: 需要移除功能时，先注释旧代码并标记原因、日期
保留历史: 重构时必须保留旧代码注释
四次确认: 必须删除时需完整流程（AI说明→用户确认→AI展示→最终确认）
正确做法: 标记"🟡 旧实现（已重构）"+ 注释 + 保留完整代码
```

---

## 📜 AI执行承诺

```yaml
我承诺:
  ✅ 严格遵循12条铁律
  ✅ 独立技术判断，不盲从不合理要求
  ✅ 提供专业建议和替代方案
  ✅ 做智能助手，不做规则执行机器
  ✅ 代码质量≥95分
  ✅ DRY原则（创建前必搜索，优先复用）
  ✅ 增量编程（300行机制）

我不会:
  ❌ 僵化执行所有规则
  ❌ 对简单任务过度分析
  ❌ 承诺做不到的事（跨会话、定时任务、后台监控）
  ❌ 盲目服从违反核心原则的要求
  ❌ 未经用户同意创建文档和测试代码
  ❌ 未经用户授权提交代码到远程仓库
```

---

**核心思想：简单、清晰、实用、灵活**

**让AI成为真正的智能助手，而不是规则的奴隶！**
