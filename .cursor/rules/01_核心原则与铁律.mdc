---
alwaysApply: true
priority: -2
description: 核心原则与铁律 - P0最高优先级，格杀勿论（整合版）
---

# 🔥 核心原则与铁律 - P0最高优先级

**版本**: v5.0 统一整合版
**整合日期**: 2025-10-24
**优先级**: -2（最高，覆盖所有规则）
**执行级别**: 格杀勿论，零容忍
**适用范围**: 全项目所有代码

---

## 📋 文档结构说明

```yaml
第一部分: 六条新增铁律（优先级-3，最高）
第二部分: 架构三大铁律（优先级-2，格杀勿论）
第三部分: 设计系统四大铁律（前端专用，零容忍）
第四部分: P0阻断性规则（优先级0，零容忍）
```

---

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 🚨 第一部分：六条新增铁律（优先级-3，最高）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 铁律1.1：禁止创建文档和测试代码

```yaml
核心原则:
  没有用户明确要求，严禁创建任何文档和测试代码
  AI的核心职责是编写生产代码
```

### 铁律1.2：禁止自动Git提交

```yaml
核心原则:
  严禁未经用户明确授权就提交代码到Git远程仓库
  Git提交必须由用户主动触发或明确授权
```

### 铁律1.3：鼓励主动优化建议

```yaml
核心原则:
  主动提出架构优化、代码重构、性能优化建议
  发现代码问题时，主动指出并提供改进方案
```

### 铁律1.4：鼓励主动测试代码

```yaml
核心原则:
  编程完成后主动测试代码功能
  主动验证正确性、性能和用户体验
```

### 铁律1.5：文档增量编写（防止超时）

```yaml
强制规则:
  1. 单次写入文档不超过1000行
  2. 写入前预估总行数，规划批次
  3. 每批次写入后等待用户确认
  4. 明确告知用户当前进度
  5. 保持批次间内容连贯性

分批策略:
  • 1-1000行：一次完成
  • 1001-3000行：分3批
  • 3001-5000行：分5批
  • >5000行：拆分为多个文档
```

### 铁律1.6：终端命令优化（防止卡死）

```yaml
强制规则:
  1. 编译命令必须限时限量
  2. 使用 --verbosity quiet/minimal
  3. 分离编译和运行步骤
  4. 长时间命令后台执行
  5. 优先使用文件工具

优化策略:
  后端: dotnet build --verbosity quiet --nologo
  前端: npm run build --silent
  Git: git status --short

检查清单:
  ☑️ 命令是否必要？
  ☑️ 是否优化了输出参数？
  ☑️ 是否有超时处理？
```

---

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 🔥 第二部分：架构三大铁律（优先级-2，格杀勿论）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 铁律2.1：后端SSOT驱动的契约类型系统

#### 核心原则（Phase 3C架构重构）

```yaml
类型定义三层架构:

  Layer 1 - 后端SSOT层:
    位置: src/SmartAbp.Domain/Entities/LowCode/*.cs
    标记: [GenerateSwaggerSchema]
    评分: 100/100（ABP vNext + DDD最佳实践）

  Layer 2 - 前端契约层:
    位置: packages/lowcode-shared/src/types/backend-contracts.ts
    内容: 45个独立契约类型
    特点: 零外部依赖，100%后端DTO一致性
    评分: 95/100（31级AlphaGO最优解）

  Layer 3 - 主应用生成层:
    位置: src/SmartAbp.Vue/src/api/generated/
    工具: NSwag + openapi-typescript-codegen
    用途: 仅主应用API调用
    评分: 100/100（完全自动化）

SSOT流程:
  后端C# DTO → NSwag → Swagger JSON →
  TS生成 → packages契约层 → 100%一致性
```

#### 正确做法

```typescript
// ✅ 后端定义（SSOT层）
[GenerateSwaggerSchema]
public class ModuleDto { ... }

// ✅ 前端契约层（packages独立）
// packages/lowcode-shared/src/types/backend-contracts.ts
export interface ModuleDto { ... }

// ✅ packages使用契约类型
import type { ModuleDto } from '@smartabp/lowcode-shared'

// ✅ 主应用使用生成类型
import type { ModuleDto } from '@/api/generated/models'
```

#### 错误做法（格杀勿论）

```typescript
// ❌ packages引用生成API
import type { ModuleDto } from '@/api/generated/models'  // 💀

// ❌ 引用废弃的metadata-core
import { xxx } from '@smartabp/metadata-core'  // 💀

// ❌ 相对路径跨包引用
import type { xxx } from '../../../src/api/generated'  // 💀

// ❌ 主应用定义底层契约
// src/types/lowcode-contracts.ts
export interface EntityDefinitionDto { ... }  // 💀

// ❌ 契约类型与后端不一致
export interface ModuleDto {
  name: string;  // 后端是 string | null  // 💀
}
```

#### 强制检查（五关门禁）

```bash
# 第一关：packages违规引用
grep -r "@/api/generated" packages/  # 必须为0
grep -r "src/api/generated" packages/  # 必须为0

# 第二关：废弃包引用
grep -r "@smartabp/metadata-core" packages/  # 必须为0

# 第三关：相对路径跨包
grep -r "'\.\./" packages/ | grep -v node_modules  # 必须为0

# 第四关：TypeScript编译
cd src/SmartAbp.Vue && npx tsc --build tsconfig.references.json  # 0错误

# 第五关：后端编译
dotnet build src/SmartAbp.sln --verbosity quiet  # 0错误
```

---

### 铁律2.2：强制使用组件注册系统

#### 核心原则

```yaml
所有低代码组件必须通过 ComponentRegistry 注册

强制要求:
  1. 组件创建后立即注册
  2. 提供完整 ComponentMetadata
  3. 声明所有依赖关系
  4. 指定 category 和 bundle
  5. 集中注册函数
  6. 主应用按顺序调用

严禁:
  ❌ 创建组件不注册
  ❌ 直接 import 使用
  ❌ 跳过 ComponentRegistry
```

#### 正确做法

```typescript
// ✅ 注册组件
export function registerCoreComponents() {
  registerComponent({
    name: 'SmartForm',
    displayName: '智能表单',
    category: 'form',
    priority: 'high',
    dependencies: ['BaseComponent'],
    bundle: '@smartabp/lowcode-core',
    version: '1.0.0',
    tags: ['form', 'smart']
  })
}

// ✅ 主应用启动
import { registerSharedComponents } from '@smartabp/lowcode-shared'
import { registerCoreComponents } from '@smartabp/lowcode-core'

registerSharedComponents()
registerCoreComponents()
```

#### 强制检查

```bash
# 检查组件数量匹配
find packages/*/src/components -name "*.vue"
grep -r "registerComponent" packages/*/src/index.ts

# 运行检查器
npm run check:config
npm run check:components
```

---

### 铁律2.3：前后端分离的架构层级体系

#### 后端架构（ABP vNext DDD - 98/100分）

```
Layer 4: SmartAbp.HttpApi（HTTP端点）
  ↓
Layer 3: SmartAbp.Application（应用服务）
  ↓
Layer 2: SmartAbp.Domain（领域层 - 核心SSOT）
  ↓
Layer 1: SmartAbp.Domain.Shared（共享基础）
  ↓
Layer 0: SmartAbp.EntityFrameworkCore（基础设施）

核心评分:
  ✅ DDD分层架构：100/100
  ✅ Repository模式：100/100
  ✅ CQRS查询分离：100/100
```

#### 前端架构（packages黑盒独立 - 95/100分）

```
Layer 2: lowcode-designer（设计器UI）
  ↓
Layer 1: lowcode-core, lowcode-api, lowcode-tools（核心逻辑）
  ↓
Layer 0: lowcode-shared（契约类型 - SSOT映射）

主应用 (src/):
  ✅ 使用NSwag生成的API类型
  ❌ 不能定义packages契约类型
```

#### 严禁行为

```yaml
严禁:
  ❌ packages引用 src/api/generated
  ❌ 逆向依赖（shared → core）
  ❌ 循环依赖（A→B→A）
  ❌ 引用metadata-core（已废弃）
  ❌ 契约类型与后端DTO不一致
```

---

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 🎨 第三部分：设计系统四大铁律（前端专用，零容忍）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 铁律3.1：禁止硬编码颜色

#### 错误做法

```vue
<!-- ❌ 硬编码颜色 -->
<div style="color: #409EFF">文本</div>

<style scoped>
.header { background-color: #409EFF; }
</style>
```

#### 正确做法

```vue
<!-- ✅ 使用设计令牌 -->
<div :style="{ color: 'var(--color-primary-500)' }">文本</div>

<style scoped>
.header { background-color: var(--color-primary-500); }
</style>
```

#### 检测

```bash
grep -rE "#[0-9A-Fa-f]{3,8}|rgb\(|rgba\(" src/views/ | wc -l  # 必须为0
```

---

### 铁律3.2：禁止硬编码间距

#### 8px栅格系统

```yaml
--spacing-1: 4px    # 微小间距
--spacing-2: 8px    # 小间距
--spacing-3: 12px   # 组件间距
--spacing-4: 16px   # 卡片内边距（最常用）✅
--spacing-5: 20px   # 中等间距
--spacing-6: 24px   # 区块间距
--spacing-8: 32px   # 页面边距
--spacing-12: 48px  # 超大间距
```

#### 正确做法

```vue
<style scoped>
.container {
  padding: var(--spacing-4);
  margin: var(--spacing-3);
}
</style>
```

#### 检测

```bash
grep -rE "padding:|margin:" src/views/ | grep -E "[0-9]+px" | wc -l  # 必须为0
```

---

### 铁律3.3：强制使用SmartComponents

#### 组件映射

```yaml
<el-button> → <SmartButton>
<el-card> → <SmartCard>
<el-input> → <SmartInput>
<el-icon> → <SmartIcon>
<el-form> → <SmartForm>
<el-table> → <SmartTable>
```

#### 正确做法

```vue
<template>
  <SmartButton variant="primary" size="md">按钮</SmartButton>
  <SmartCard elevation="sm">卡片</SmartCard>
</template>

<script setup lang="ts">
import { SmartButton, SmartCard } from '@/components/design-system'
</script>
```

#### 检测

```bash
grep -rE "<el-(button|card|input|icon)" src/views/ | wc -l  # 必须为0
```

---

### 铁律3.4：统一图标系统（Carbon Design）

#### 正确做法

```vue
<template>
  <SmartIcon icon="carbon:edit" size="md" />
  <SmartIcon icon="carbon:add" size="lg" />
</template>
```

#### 常用图标

```yaml
操作: carbon:add, carbon:edit, carbon:delete, carbon:save
导航: carbon:home, carbon:menu, carbon:chevron-left
状态: carbon:checkmark, carbon:error, carbon:warning
```

#### 检测

```bash
grep -rE "<el-icon|<i class=\"(fa|el-icon)" src/views/ | wc -l  # 必须为0
```

---

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 🛡️ 第四部分：P0阻断性规则（优先级0，零容忍）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 铁律4.1：类型安全铁律

```yaml
强制要求:
  ✅ TypeScript编译0错误
  ✅ 严禁 as any 和 @ts-ignore
  ✅ 所有函数有类型定义

检测:
  cd src/SmartAbp.Vue && npm run type-check  # 0错误
```

### 铁律4.2：架构完整性铁律

```yaml
后端架构（98/100分）:
  ✅ DDD分层架构
  ✅ Repository模式
  ✅ AutoMapper配置
  ✅ CQRS查询分离

前端架构（95/100分）:
  ✅ packages黑盒独立
  ✅ 契约类型系统
  ✅ 依赖层级正确
  ✅ 100%DTO一致性
```

### 铁律4.3：功能完整性铁律

```yaml
完整链路: 菜单→UI→API→Service→DB
真实事件: 所有控件真实绑定
真实数据: 来自真实API，禁Mock
类型完整: 100%类型安全
验收标准: 95分质量

淘汰: 花瓶式实现（空方法、TODO、Mock）
```

### 铁律4.4：Git安全铁律

```yaml
强制流程:
  1. 先pull后push
  2. 使用git-safe-sync.ps1
  3. 禁直接push
  4. 禁force push到main

执行:
  pwsh -File scripts/git/git-safe-sync.ps1 -AutoCommit
```

### 铁律4.5：代码保护铁律

```yaml
AI权限:
  ✅ 创建、修改、重构、注释
  ❌ 严禁删除代码和文件

优先注释:
  移除功能时先注释并标记原因、日期

四次确认:
  AI说明 → 用户确认 → AI展示 → 最终确认

正确做法:
  "🟡 旧实现（已重构）" + 注释 + 保留代码
```

---

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## ✅ AI执行承诺
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```yaml
架构层面:
  ✅ 遵守后端SSOT驱动的契约类型系统
  ✅ 遵守packages黑盒独立原则
  ✅ 遵守架构层级依赖关系
  ❌ 禁止packages引用src/api/generated
  ❌ 禁止引用metadata-core
  ❌ 禁止相对路径跨包引用

设计系统层面:
  ✅ 使用SmartComponents
  ✅ 使用设计令牌（禁硬编码）
  ✅ 使用Carbon图标
  ✅ 执行四关门禁

质量层面:
  ✅ TypeScript编译0错误
  ✅ ESLint 0错误0警告
  ✅ 后端编译0错误
  ✅ 代码质量≥95分

执行层面:
  ✅ 编程前15节点分析
  ✅ 遵循DRY原则
  ✅ 增量编程（300行机制）
  ✅ 五关质量门禁
  ✅ 终端命令优化

我不会:
  ❌ 未授权创建文档和测试
  ❌ 未授权Git提交
  ❌ 删除任何代码和文件
  ❌ 违反架构铁律
  ❌ 违反设计系统铁律
  ❌ 降低质量标准
```

---

**🔥 这是项目最高优先级规则！**
**🔥 任何违反都格杀勿论！**
**🔥 质量标准不容妥协！**
