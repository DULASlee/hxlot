# AI架构铁律 - L3: 质量守护协议

## 核心原则: 零失败、零跳过、自动化、强制化

---

## 🚀 七关强制质量门禁 (Pre-Delivery Quality Gates)

**任何代码交付前，必须自动、依次、强制执行。任何一关失败，严禁进入下一关或Git提交。**

### 🏗️ **第一关：架构符合性 (0违规)**
```bash
grep -r "'../'" src/SmartAbp.Vue/packages/
grep -r "@/" src/SmartAbp.Vue/packages/
grep -r "as any\|@ts-ignore" src/
```

### 🔄 **第二关：代码重复度 (0重复)**
```bash
find src/ -name "*.vue" | sed 's/.*\/\///' | sort | uniq -d
grep -r "function\s+\w+\|const\s+\w+\s*=" src/ --include="*.ts,*.vue" | sed -e 's/.*function\s\+\(\w\+\).*/\1/' -e 's/.*const\s\+\(\w\+\).*/\1/' | sort | uniq -d
```

### ⚡ **第三关：编译与静态检查 (0错误)**
```bash
# 前端
npm run type-check --silent
npm run lint --silent

# 后端 (如修改)
# 【集成自06】必须使用优化参数进行增量编译
dotnet build <project_name>.csproj --no-dependencies --verbosity minimal
```

### 🎯 **第四关：低代码生成器专项 (100%质量)**
```bash
# packages TypeScript项目引用构建
cd src/SmartAbp.Vue && npx tsc --build tsconfig.references.json

# packages ESLint专项检查
cd src/SmartAbp.Vue && npm run lint -- "packages/*/src/**/*.{ts,vue}"
```

###  debt **第五关：技术债务监控 (≥85分)**
```bash
# 统计大文件数量 (>200行)
find src/ -name "*.ts" -o -name "*.vue" | xargs wc -l | awk '$1 > 200 {count++} END {print count+0}'

# 统计TODO/FIXME标记
grep -r "TODO\|FIXME" src/ --exclude-dir=node_modules | wc -l

# 统计类型安全问题
grep -r "as any\|@ts-ignore" src/ | wc -l
```

### 🏆 **第六关：卓越工程评分 (≥90分)**
```yaml
# 此关为AI基于以下维度的综合评估:
评估维度:
  - 代码质量: 可读性、可维护性
  - 架构设计: 是否遵循最佳实践
  - 性能考量: 是否存在明显性能问题
  - 测试覆盖: (建议)
  - 文档完整性: (建议)
最终评分: "≥90分"
```

###  Git **第七关：Git版本同步 (100%成功)**
```bash
# 强制使用企业级Git安全同步脚本
# Windows
pwsh -File scripts/git/git-safe-sync.ps1 -AutoCommit

# Linux/Mac
bash scripts/git/git-safe-sync.sh --auto-commit

# 核心验证：本地哈希 == 远程哈希
LOCAL_HASH=$(git rev-parse HEAD)
REMOTE_HASH=$(git rev-parse origin/main)
if [ "$LOCAL_HASH" != "$REMOTE_HASH" ]; then exit 1; fi
```