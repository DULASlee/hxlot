# AI架构铁律同心圆模型 - L5: 核心功能保护层 (Core Function Protection)

## 文件说明
- **文件标识**: `05_核心功能保护铁律.mdc`
- **层级**: L5 - 核心功能保护层
- **作用**: 防止AI在修复类型错误、代码重构时破坏已完成的核心业务功能
- **优先级**: 🔥🔥🔥 **最高优先级** - 高于代码质量检查



## 🚨 核心功能保护铁律 (Core Function Protection Tenet)

### 核心原则

**在修复任何代码问题（类型错误、Lint警告、架构问题）时，必须遵循以下铁律：**

1. **严禁删除核心业务逻辑** - 无论代码看起来多"复杂"或"冗余"
2. **严禁简化已完成功能** - 核心功能完整性优先于代码"优雅"
3. **严禁注释掉核心代码** - 注释核心代码等同于删除
4. **严禁用空实现替代** - throw Error或空函数体是严重违规
5. **类型修复不能改变业务逻辑** - 只修复类型定义，不改变实现

---

## 📋 核心功能清单 (已完成功能保护列表)

### 🔥 前端核心Store (严禁简化)

#### 1. enhancedStateMachine.ts (874行)

**保护等级**: 🔥🔥🔥🔥🔥 **核心中的核心**

**核心功能列表**:
```typescript
// ✅ 必须保护的核心功能
export const useEnhancedStateMachineStore = defineStore("enhancedStateMachine", () => {
  // 🔥 核心状态 - 严禁删除或简化
  const states = ref<EnhancedState[]>([])
  const transitions = ref<StateTransition[]>([])
  const businessRules = ref<BusinessRule[]>([])
  const workflowMetadata = ref<WorkflowMetadata>({...})
  const workflowTemplates = ref<WorkflowTemplate[]>([])
  const executionErrors = ref<ExecutionError[]>([])
  
  // 🔥 核心CRUD方法 - 严禁删除
  const addState = (state: EnhancedState) => { /* 实现逻辑 */ }
  const updateState = (id: string, updates: Partial<EnhancedState>) => { /* 实现逻辑 */ }
  const deleteState = (id: string) => { /* 实现逻辑 */ }
  const addTransition = (transition: StateTransition) => { /* 实现逻辑 */ }
  const updateTransition = (id: string, updates: Partial<StateTransition>) => { /* 实现逻辑 */ }
  const deleteTransition = (id: string) => { /* 实现逻辑 */ }
  
  // 🔥 规则执行引擎 - 严禁简化
  const executeBusinessRules = async (context: Record<string, any>) => { /* 完整实现 */ }
  
  // 🔥 代码生成 - 严禁删除
  const generateCode = (options: CodeGenerationOptions) => { /* 完整实现 */ }
  
  // 🔥 验证机制 - 严禁删除
  const validateWorkflow = (): ValidationResult => { /* 完整实现 */ }
  
  return {
    // ⚠️ 所有导出必须保留，严禁删除任何一个
    states, transitions, businessRules, workflowMetadata,
    addState, updateState, deleteState,
    addTransition, updateTransition, deleteTransition,
    executeBusinessRules, generateCode, validateWorkflow,
    // ... 874行所有功能
  }
})
```

**修复类型错误时的正确做法**:
```typescript
// ❌ 错误做法：删除复杂逻辑
const executeBusinessRules = async (context: any) => {
  return {} // 简化了核心逻辑！！！
}

// ✅ 正确做法：修复类型定义
const executeBusinessRules = async (
  context: Record<string, any>
): Promise<RuleExecutionResult> => {
  // 保留完整的业务逻辑实现
  // 只修复类型注解
}
```

#### 2. codeGeneration.ts (1176行)

**保护等级**: 🔥🔥🔥🔥🔥 **核心中的核心**

**核心功能列表**:
```typescript
export const useCodeGenerationStore = defineStore("codeGeneration", () => {
  // 🔥 代码生成主方法 - 严禁简化
  const generateCode = async (
    config: CodeGenerationConfig,
    onProgress?: (progress: GenerationProgress) => void
  ): Promise<GenerationResult> => {
    // ⚠️ 这里有复杂的代码生成逻辑，严禁删除
    // ⚠️ 包含模板处理、文件生成、进度回调等
    // ⚠️ 即使类型错误，也要保留逻辑，只修复类型
  }
  
  // 🔥 模板管理 - 严禁删除
  const loadTemplates = async () => { /* 完整实现 */ }
  const applyTemplate = (template: CodeTemplate, data: any) => { /* 完整实现 */ }
  
  // 🔥 文件生成 - 严禁简化
  const generateFiles = (config: CodeGenerationConfig) => { /* 完整实现 */ }
  
  return {
    generateCode, loadTemplates, applyTemplate, generateFiles,
    // ... 1176行所有功能
  }
})
```

#### 3. entityModeling.ts

**保护等级**: 🔥🔥🔥🔥

**核心功能**:
- 实体设计器状态管理
- 字段定义和验证
- 关联关系配置
- 实体导入导出

**严禁操作**:
- 删除字段验证逻辑
- 简化关联关系处理
- 删除导入导出功能

#### 4. pageDesign.ts

**保护等级**: 🔥🔥🔥🔥

**核心功能**:
- 页面组件树管理
- 组件属性配置
- 页面布局计算
- 响应式设计支持

---

### 🔥 后端核心服务 (严禁简化)

#### 1. CodeGenerationAppService.cs (1973行)

**保护等级**: 🔥🔥🔥🔥🔥 **核心中的核心**

**核心方法列表**:
```csharp
public class CodeGenerationAppService : ApplicationService, ICodeGenerationAppService
{
    // 🔥 核心生成方法 - 严禁删除或简化
    public async Task<GeneratedModuleDto> GenerateModuleAsync(ModuleMetadataDto input) 
    {
        // ⚠️ 完整的模块生成逻辑，严禁简化
        // ⚠️ 包含验证、生成、集成、事件发布等
    }
    
    // 🔥 DDD生成 - 严禁删除
    public async Task<GeneratedDddSolutionDto> GenerateDddDomainAsync(DddDefinitionDto input)
    {
        // ⚠️ 完整的DDD生成逻辑
    }
    
    // 🔥 CQRS生成 - 严禁删除
    public async Task<GeneratedCqrsSolutionDto> GenerateCqrsAsync(CqrsDefinitionDto input)
    {
        // ⚠️ 完整的CQRS生成逻辑
    }
    
    // 🔥 微服务生成 - 严禁删除
    public async Task<GeneratedAspireSolutionDto> GenerateAspireSolutionAsync(AspireSolutionDefinitionDto input)
    {
        // ⚠️ 完整的微服务生成逻辑
    }
    
    // ... 1973行所有方法必须保留
}
```

**修复编译错误时的正确做法**:
```csharp
// ❌ 错误做法：删除复杂逻辑
public async Task<GeneratedModuleDto> GenerateModuleAsync(ModuleMetadataDto input)
{
    return new GeneratedModuleDto(); // 简化了核心逻辑！！！
}

// ✅ 正确做法：修复类型定义或引用
public async Task<GeneratedModuleDto> GenerateModuleAsync(ModuleMetadataDto input)
{
    Check.NotNull(input, nameof(input));
    
    // 保留完整的生成逻辑
    // 只修复编译错误，如：
    // - 添加缺失的using
    // - 修复命名空间引用
    // - 修复类型转换
}
```

---

## 🛡️ 类型错误修复规范

### 原则1: 只修复类型，不改变逻辑

**错误的修复方式**:
```typescript
// ❌ 删除复杂逻辑来消除类型错误
const processData = (data: any) => {
  return data // 简化了所有处理逻辑
}

// ❌ 用any绕过类型检查
const complexFunction = (input: any): any => {
  // 原本有完整的类型定义
}

// ❌ 注释掉"有问题"的代码
const importantFeature = () => {
  // const result = complexLogic()
  // return result
  return {} // 空实现
}
```

**正确的修复方式**:
```typescript
// ✅ 添加正确的类型定义
interface ProcessedData {
  id: string
  name: string
  value: number
}

const processData = (data: RawData): ProcessedData => {
  // 保留完整的处理逻辑
  // 只添加类型注解
  return {
    id: data.id,
    name: transformName(data.rawName),
    value: calculateValue(data.rawValue)
  }
}

// ✅ 定义完整的接口
interface ComplexInput {
  field1: string
  field2: number
}

interface ComplexOutput {
  result: string
  metadata: Record<string, any>
}

const complexFunction = (input: ComplexInput): ComplexOutput => {
  // 保留所有业务逻辑
}

// ✅ 修复类型错误而非删除代码
const importantFeature = (): FeatureResult => {
  const result = complexLogic() // 保留逻辑
  return result // 添加正确的类型
}
```

### 原则2: 类型错误修复优先级

**修复顺序**:
1. **添加缺失的import** - 最常见的问题
2. **定义缺失的interface/type** - 创建正确的类型定义
3. **修复类型注解** - 纠正错误的类型标注
4. **添加类型断言** - 仅在确定类型时使用
5. **重构代码结构** - 最后手段，必须保留所有功能

**严禁的修复方式**:
- ❌ 删除代码
- ❌ 简化逻辑
- ❌ 注释代码
- ❌ 使用`any`绕过
- ❌ 使用`@ts-ignore`

### 原则3: 验证修复结果

**修复后必须验证**:
```typescript
// 修复前：类型错误
const getData = async (id) => {
  const result = await api.get('/data/' + id)
  return result.data.items.map(item => ({
    ...item,
    formatted: formatData(item)
  }))
}

// 修复后：类型完整 + 功能保留
interface DataItem {
  id: string
  name: string
  value: number
}

interface FormattedItem extends DataItem {
  formatted: string
}

const getData = async (id: string): Promise<FormattedItem[]> => {
  const result = await api.get<{ data: { items: DataItem[] } }>('/data/' + id)
  return result.data.items.map(item => ({
    ...item,
    formatted: formatData(item) // ✅ 功能完全保留
  }))
}

// ✅ 验证清单：
// 1. 类型错误已修复？
// 2. 所有业务逻辑保留？
// 3. 函数功能未改变？
// 4. 返回值类型正确？
```

---

## 🔍 核心功能保护检查清单

### 修复前强制检查

在修复任何类型错误或代码问题前，AI必须回答：

```typescript
const CORE_PROTECTION_CHECKLIST = {
  question1: "这个文件是否包含核心业务功能？",
  question2: "我的修复是否会删除或简化任何业务逻辑？",
  question3: "是否可以只修复类型定义而不改变实现？",
  question4: "修复后的代码功能是否与修复前完全一致？",
  question5: "我是否需要查看完整的文件内容以理解上下文？",
  question6: "是否存在更好的类型定义方式而非简化代码？"
}

// 如果question2的答案是"是"，必须立即停止，寻找其他修复方案
```

### 修复中实时验证

```typescript
class CoreProtectionValidator {
  // 在每次代码修改前验证
  validateBeforeChange(file: string, oldCode: string, newCode: string): ValidationResult {
    // 1. 检查代码行数变化
    const oldLines = oldCode.split('\n').length
    const newLines = newCode.split('\n').length
    
    if (newLines < oldLines * 0.8) {
      return {
        valid: false,
        reason: "代码行数减少超过20%，可能删除了核心功能",
        suggestion: "只修复类型，不删除逻辑"
      }
    }
    
    // 2. 检查关键字消失
    const criticalKeywords = [
      'async', 'await', 'return', 'if', 'for', 'map', 'filter',
      'validate', 'generate', 'execute', 'process'
    ]
    
    for (const keyword of criticalKeywords) {
      const oldCount = (oldCode.match(new RegExp(keyword, 'g')) || []).length
      const newCount = (newCode.match(new RegExp(keyword, 'g')) || []).length
      
      if (newCount < oldCount * 0.5) {
        return {
          valid: false,
          reason: `关键字'${keyword}'出现次数大幅减少，可能简化了逻辑`,
          suggestion: "保留所有业务逻辑"
        }
      }
    }
    
    // 3. 检查return语句
    const oldReturns = (oldCode.match(/return\s+/g) || []).length
    const newReturns = (newCode.match(/return\s+/g) || []).length
    
    if (newReturns < oldReturns) {
      return {
        valid: false,
        reason: "return语句减少，可能删除了分支逻辑",
        suggestion: "保留所有return语句"
      }
    }
    
    return { valid: true }
  }
}
```

### 修复后强制验证

```typescript
const POST_FIX_VALIDATION = {
  // 1. 功能完整性验证
  functionalityIntact: () => {
    // 所有核心方法仍然存在？
    // 所有核心状态仍然存在？
    // 所有导出仍然存在？
  },
  
  // 2. 类型安全性验证
  typeSafety: () => {
    // TypeScript编译通过？
    // 无any类型？
    // 无@ts-ignore？
  },
  
  // 3. 代码行数验证
  codeLineCount: () => {
    // 代码行数不能减少超过10%
    // 核心文件行数必须保持在80%以上
  },
  
  // 4. 单元测试验证
  unitTests: () => {
    // 所有单元测试通过？
    // 测试覆盖率未降低？
  }
}
```

---

## 📚 常见类型错误的正确修复示例

### 示例1: 缺少import导致的类型错误

**错误**:
```typescript
// ❌ 类型错误：找不到'EntityDto'
const processEntity = (entity: EntityDto) => {
  // ...
}
```

**错误的修复**:
```typescript
// ❌ 删除类型注解
const processEntity = (entity: any) => {
  // ...
}
```

**正确的修复**:
```typescript
// ✅ 添加正确的import
import type { EntityDto } from '@smartabp/lowcode-shared'

const processEntity = (entity: EntityDto) => {
  // 业务逻辑完全保留
}
```

### 示例2: 接口定义缺失

**错误**:
```typescript
// ❌ 类型错误：找不到'UserConfig'
const configureUser = (config: UserConfig) => {
  return {
    id: config.userId,
    name: config.userName,
    roles: config.userRoles.map(r => r.toUpperCase())
  }
}
```

**错误的修复**:
```typescript
// ❌ 简化逻辑
const configureUser = (config: any) => {
  return config // 删除了所有处理逻辑！
}
```

**正确的修复**:
```typescript
// ✅ 定义完整的接口
interface UserConfig {
  userId: string
  userName: string
  userRoles: string[]
}

interface ConfiguredUser {
  id: string
  name: string
  roles: string[]
}

const configureUser = (config: UserConfig): ConfiguredUser => {
  // 业务逻辑完全保留
  return {
    id: config.userId,
    name: config.userName,
    roles: config.userRoles.map(r => r.toUpperCase())
  }
}
```

### 示例3: 复杂类型推导错误

**错误**:
```typescript
// ❌ 类型错误：无法推导返回类型
const processData = async (items) => {
  const results = []
  for (const item of items) {
    const processed = await complexProcessing(item)
    if (processed.isValid) {
      results.push({
        ...processed,
        metadata: generateMetadata(processed)
      })
    }
  }
  return results
}
```

**错误的修复**:
```typescript
// ❌ 简化逻辑
const processData = async (items: any[]): Promise<any[]> => {
  return items // 删除了所有处理逻辑！
}
```

**正确的修复**:
```typescript
// ✅ 定义完整的类型
interface InputItem {
  id: string
  data: any
}

interface ProcessedItem {
  id: string
  data: any
  isValid: boolean
  value: number
}

interface ResultItem extends ProcessedItem {
  metadata: {
    processedAt: Date
    version: string
  }
}

const processData = async (items: InputItem[]): Promise<ResultItem[]> => {
  // 业务逻辑完全保留
  const results: ResultItem[] = []
  for (const item of items) {
    const processed = await complexProcessing(item)
    if (processed.isValid) {
      results.push({
        ...processed,
        metadata: generateMetadata(processed)
      })
    }
  }
  return results
}
```

---

## 🚨 AI修复代码时的强制规则

### 规则1: 必须先阅读完整文件

**在修复任何类型错误前，AI必须**:
```typescript
1. 读取完整的文件内容（不能只看报错的几行）
2. 理解文件的整体功能和业务逻辑
3. 识别哪些是核心功能（参考本文档的核心功能清单）
4. 确定最小化的修复方案
```

### 规则2: 优先级顺序

**修复优先级**:
```
1. 添加缺失的import           (最高优先级)
2. 定义缺失的interface/type   (高优先级)
3. 修复类型注解               (中优先级)
4. 添加类型守卫               (低优先级)
5. 重构代码结构               (最低优先级，需要用户确认)

❌ 绝不允许：删除代码、简化逻辑、注释代码
```

### 规则3: 修复前提问机制

**AI在不确定时必须提问**:
```
Q: "这段代码的业务逻辑是什么？"
Q: "这个函数在系统中的作用是什么？"
Q: "是否可以只修复类型而不改变实现？"
Q: "有没有其他不影响逻辑的修复方案？"

而不是：
❌ 直接删除"看起来有问题"的代码
❌ 简化"看起来复杂"的逻辑
❌ 注释掉"无法理解"的代码
```

### 规则4: 修复后验证机制

**AI必须自动执行**:
```typescript
afterFix() {
  // 1. 代码行数检查
  if (newLines < oldLines * 0.9) {
    throw Error("代码行数减少超过10%，可能删除了核心功能")
  }
  
  // 2. 关键方法检查
  const criticalMethods = ['generateCode', 'executeRules', 'validateWorkflow']
  for (const method of criticalMethods) {
    if (!newCode.includes(method)) {
      throw Error(`核心方法'${method}'消失了！`)
    }
  }
  
  // 3. 导出检查
  const oldExports = extractExports(oldCode)
  const newExports = extractExports(newCode)
  if (newExports.length < oldExports.length) {
    throw Error("导出项减少了！")
  }
}
```

---

## 🎯 核心功能保护的成功标准

### 修复成功的标志

✅ **类型错误已修复** - TypeScript编译通过  
✅ **功能完全保留** - 所有业务逻辑未改变  
✅ **代码行数基本一致** - 变化不超过±10%  
✅ **单元测试通过** - 所有测试用例通过  
✅ **无类型绕过** - 无any、无@ts-ignore  
✅ **接口完整** - 所有导出保持不变  

### 修复失败的标志

❌ **代码行数大幅减少** - 减少超过20%  
❌ **核心方法消失** - 任何核心功能被删除  
❌ **逻辑被简化** - 业务逻辑被简单实现替代  
❌ **用any绕过** - 使用any类型逃避问题  
❌ **导出减少** - 公共API被删除  

---

## 📞 遇到困难时的处理方式

### AI应该做的

✅ **向用户说明问题**: "这个类型错误涉及核心业务逻辑，我需要更多信息"  
✅ **提供多个方案**: "有三种修复方式，请选择最合适的"  
✅ **保守修复**: "我只添加了类型定义，没有改变任何逻辑"  
✅ **详细说明**: "我修复了X个import，定义了Y个interface，保留了所有业务逻辑"  

### AI不应该做的

❌ **擅自删除代码**: "这段代码看起来不需要，我删掉了"  
❌ **简化逻辑**: "这个函数太复杂了，我简化了"  
❌ **注释代码**: "这段代码有问题，我先注释掉"  
❌ **使用any**: "用any先绕过，以后再说"  

---

**制定者**: SmartAbp世界顶尖架构师团队  
**优先级**: 🔥🔥🔥 最高优先级  
**执行原则**: 核心功能保护 > 代码质量 > 代码优雅