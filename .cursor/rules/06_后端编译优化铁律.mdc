# AI编程铁律 - L6: 后端编译优化铁律 (Backend Compilation Optimization)

## 文件说明
- **文件标识**: `06_后端编译优化铁律.mdc`
- **层级**: L6 - 后端编译优化层
- **作用**: 规定AI在执行后端C#项目编译时的强制优化规则,避免终端卡死和长时间等待
- **优先级**: 最高优先级 - 每次后端编译都必须遵守

---

## 🚨 核心原则（零容忍）

**基本真理**: 完整编译耗时长(30-60s)且容易超时,增量编译快速高效(3-5s)

**强制要求**: 
- ✅ **必须使用增量编译**
- ✅ **必须使用最小详细度**
- ✅ **必须限时保护(10s超时)**
- ❌ **严禁完整编译**
- ❌ **严禁详细输出**

---

## 📋 强制执行规则

### 规则1: 增量编译优先 ⚡

**正确做法** (✅ 必须使用):
```bash
# 编译单个项目（推荐）
dotnet build src/项目名/项目名.csproj \
  --no-dependencies \
  --verbosity minimal

# 示例: 编译Application层
dotnet build src/SmartAbp.Application/SmartAbp.Application.csproj \
  --no-dependencies \
  --verbosity minimal
```

**错误做法** (❌ 严禁使用):
```bash
# ❌ 错误1: 完整编译（耗时30-60s，容易超时）
dotnet build src/SmartAbp.sln

# ❌ 错误2: 缺少--no-dependencies（会编译所有依赖）
dotnet build src/SmartAbp.Application/SmartAbp.Application.csproj

# ❌ 错误3: 使用详细输出（输出过多，阻塞终端）
dotnet build --verbosity detailed
```

---

### 规则2: 最小详细度 📊

**verbosity参数强制设置**:
```bash
--verbosity minimal  # ✅ 必须使用
--verbosity quiet    # ✅ 可选（更安静）

# ❌ 严禁使用
--verbosity normal   # 输出过多
--verbosity detailed # 输出过多
--verbosity diagnostic # 输出过多
```

**输出对比**:
```yaml
minimal输出（✅ 推荐）:
  ✓ 编译结果（成功/失败）
  ✓ 错误信息
  ✓ 警告数量
  ✗ 详细编译步骤
  ✗ 文件列表
  
detailed输出（❌ 禁止）:
  ✓ 所有文件路径
  ✓ 所有编译步骤
  ✓ 所有依赖分析
  → 导致终端阻塞！
```

---

### 规则3: 限时保护机制 ⏱️

**超时检测**:
```bash
# 使用timeout命令（Linux/Mac）
timeout 10s dotnet build src/项目.csproj \
  --no-dependencies \
  --verbosity minimal

# 或使用| tail限制输出
dotnet build src/项目.csproj \
  --no-dependencies \
  --verbosity minimal 2>&1 | tail -20
```

**超时处理**:
```yaml
如果编译超过10秒:
  1. 立即终止编译
  2. 检查是否误用完整编译
  3. 检查是否网络问题（NuGet还原）
  4. 使用更精确的项目路径
```

---

## 🎯 编译场景分类

### 场景1: 修改Application层 ✅

```bash
# 只编译Application项目
dotnet build src/SmartAbp.Application/SmartAbp.Application.csproj \
  --no-dependencies \
  --verbosity minimal \
  2>&1 | tail -20

# 预期耗时: 3-5秒
```

### 场景2: 修改Domain层 ✅

```bash
# Domain改动会影响Application,需要编译两个
dotnet build src/SmartAbp.Domain/SmartAbp.Domain.csproj \
  --no-dependencies \
  --verbosity minimal

dotnet build src/SmartAbp.Application/SmartAbp.Application.csproj \
  --no-dependencies \
  --verbosity minimal

# 预期耗时: 5-8秒
```

### 场景3: 修改Contracts层 ✅

```bash
# Contracts改动影响Application
dotnet build src/SmartAbp.Application.Contracts/SmartAbp.Application.Contracts.csproj \
  --no-dependencies \
  --verbosity minimal

dotnet build src/SmartAbp.Application/SmartAbp.Application.csproj \
  --no-dependencies \
  --verbosity minimal

# 预期耗时: 5-8秒
```

### 场景4: 完整验证（仅CI/CD） ⚠️

```bash
# 仅在最终验证时使用
dotnet build src/SmartAbp.sln \
  --verbosity minimal \
  2>&1 | tail -50

# 预期耗时: 30-60秒
# ⚠️ 仅用于: 最终提交前、CI/CD流水线
```

---

## 📊 性能对比

| 编译方式 | 耗时 | 输出行数 | 风险 | 适用场景 |
|---------|------|---------|------|---------|
| **增量+minimal** | 3-5s | <20行 | ✅ 无 | ✅ 日常开发 |
| **增量+normal** | 5-8s | 100+行 | ⚠️ 可能卡顿 | ⚠️ 调试时 |
| **完整+minimal** | 30-60s | 50-100行 | ⚠️ 容易超时 | ⚠️ 最终验证 |
| **完整+detailed** | 60-120s | 1000+行 | 🚨 必卡死 | 🚨 严禁使用 |

---

## 🔍 AI执行前检查清单

**每次执行dotnet build前,AI必须自问**:

```yaml
☑️ 我是否使用了增量编译（--no-dependencies）？
☑️ 我是否使用了最小详细度（--verbosity minimal）？
☑️ 我是否指定了具体项目（.csproj）而不是解决方案（.sln）？
☑️ 我是否使用了tail限制输出（2>&1 | tail -20）？
☑️ 我是否在修复编译错误，而不是首次编译？
☑️ 编译时间是否在10秒内？
```

**如果任何一项为"否"，立即停止并修正！**

---

## 🚨 常见错误与修正

### 错误1: 使用解决方案编译

```bash
# ❌ 错误
dotnet build src/SmartAbp.sln

# ✅ 修正
dotnet build src/SmartAbp.Application/SmartAbp.Application.csproj \
  --no-dependencies \
  --verbosity minimal
```

### 错误2: 忘记--no-dependencies

```bash
# ❌ 错误（会编译所有依赖，耗时长）
dotnet build src/SmartAbp.Application/SmartAbp.Application.csproj

# ✅ 修正
dotnet build src/SmartAbp.Application/SmartAbp.Application.csproj \
  --no-dependencies \
  --verbosity minimal
```

### 错误3: 使用详细输出

```bash
# ❌ 错误（输出过多，终端阻塞）
dotnet build src/项目.csproj --verbosity detailed

# ✅ 修正
dotnet build src/项目.csproj --verbosity minimal 2>&1 | tail -20
```

---

## 🎯 标准化编译模板

**AI必须使用以下模板**:

```bash
# ═══════════════════════════════════════════════════════════════════════
# 标准化后端编译模板（AI强制使用）
# ═══════════════════════════════════════════════════════════════════════

# Application层编译
dotnet build src/SmartAbp.Application/SmartAbp.Application.csproj \
  --no-dependencies \
  --verbosity minimal \
  2>&1 | tail -20

# Domain层编译
dotnet build src/SmartAbp.Domain/SmartAbp.Domain.csproj \
  --no-dependencies \
  --verbosity minimal \
  2>&1 | tail -20

# Contracts层编译
dotnet build src/SmartAbp.Application.Contracts/SmartAbp.Application.Contracts.csproj \
  --no-dependencies \
  --verbosity minimal \
  2>&1 | tail -20

# CodeGenerator编译
dotnet build src/SmartAbp.CodeGenerator/SmartAbp.CodeGenerator.csproj \
  --no-dependencies \
  --verbosity minimal \
  2>&1 | tail -20
```

---

## 📈 执行效果验证

**成功标准**:
```yaml
✅ 编译时间: <10秒
✅ 输出行数: <30行
✅ 终端响应: 无卡顿
✅ 编译结果: 清晰可见
✅ 错误信息: 完整展示
```

**失败标准** (需要优化):
```yaml
❌ 编译时间: >30秒
❌ 输出行数: >100行
❌ 终端响应: 卡顿/无响应
❌ 看不到编译结果
```

---

## 🔄 集成到执行引擎

**在`.cursor/rules/00_执行引擎.mdc`的阶段E（质量门禁）中集成**:

```yaml
阶段E - 持续质量监控:
  第三关: 编译与静态检查
    前端检查:
      ✅ TypeScript类型检查
      ✅ ESLint检查
    
    后端检查:  # 🔥 新增强制规则
      ✅ 增量编译（--no-dependencies）
      ✅ 最小详细度（--verbosity minimal）
      ✅ 限时保护（10s超时）
      ✅ 输出限制（| tail -20）
      
      命令模板:
      dotnet build src/SmartAbp.Application/SmartAbp.Application.csproj \
        --no-dependencies \
        --verbosity minimal \
        2>&1 | tail -20
```

---

## 💡 AI自我提醒

**每次看到"dotnet build"时,AI必须立即想起**:

```
1. 🚨 增量编译！（--no-dependencies）
2. 📊 最小输出！（--verbosity minimal）
3. ⏱️ 限时保护！（| tail -20）
4. 🎯 具体项目！（.csproj而非.sln）
```

---

## ✅ 验收标准

- ✅ 每次后端编译必须使用增量编译
- ✅ 每次后端编译必须使用最小详细度
- ✅ 每次后端编译必须限制输出
- ✅ 编译时间必须<10秒
- ✅ 终端必须保持响应
- ❌ 任何违反此规则的编译都必须立即重新执行

---

**最终原则**: 
```
快速编译 = 增量 + 最小详细度 + 输出限制
完整编译 = 仅用于最终验证
AI必须记住 = 每次编译都检查此规则
```

**🔥 这是AI的后端编译圣经！每次编译前必读！**
