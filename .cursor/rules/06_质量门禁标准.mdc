---
description: 五关强制质量检查标准 - 架构合规与代码质量保障
parent: 00_执行引擎_主文档.mdc
version: v14.0
lastUpdate: 2025-10-24
---

# 质量门禁标准 - 五关强制检查

## 📋 概览

**核心原则**: 0错误 0警告 0违规
**触发条件**: 功能完成 OR 代码≥300行
**预期时长**: 3-5分钟
**通过标准**: 五关全部通过

---

## 🎯 五关总览

```yaml
第一关: 架构完整性检查
  - 检查时间: ~3秒
  - 通过标准: 0违规

第二关: 代码重复度检查
  - 检查时间: ~5秒
  - 通过标准: 0重复

第三关: 编译静态检查
  - 检查时间: ~15秒
  - 通过标准: 0错误0警告

第四关: packages专项检查
  - 检查时间: ~10秒
  - 通过标准: 编译通过+lint通过
  - 说明: 仅当修改packages时执行

第五关: 技术债务监控
  - 检查时间: ~2秒
  - 通过标准: 评分≥85分
```

---

## 🔒 第一关: 架构完整性检查

### 检查目标
**确保代码符合项目架构规范,0违规**

### 检查项清单

#### 检查1: packages相对路径违规

```bash
- [ ] 检查packages中的相对路径引用
      命令: grep -r "'../'" src/SmartAbp.Vue/packages/ | wc -l
      预期: 0 ✅

      违规示例:
        ❌ import { xxx } from '../../../utils'
        ✅ import { xxx } from '@smartabp/lowcode-shared'
```

#### 检查2: packages @别名违规

```bash
- [ ] 检查packages中的@别名引用
      命令: grep -r "@/" src/SmartAbp.Vue/packages/ | grep -v node_modules | wc -l
      预期: 0 ✅

      违规示例:
        ❌ import { xxx } from '@/api/generated'
        ✅ import type { xxx } from '@smartabp/lowcode-shared/types/backend-contracts'
```

#### 检查3: 类型安全检查

```bash
- [ ] 检查as any和@ts-ignore使用
      命令: grep -rn "as any\|@ts-ignore" src/ --include="*.ts" --include="*.vue" | wc -l
      预期: 0 ✅ (特殊情况需注释说明)

      违规示例:
        ❌ const data = response as any
        ❌ // @ts-ignore
        ✅ const data = response as UserDto (使用具体类型)
```

#### 检查4: 废弃包引用

```bash
- [ ] 检查废弃包引用
      命令: grep -r "@smartabp/metadata-core" src/
      预期: 0 ✅ (metadata-core已废弃,使用lowcode-shared代替)
```

### 验证清单

```markdown
- [ ] 相对路径违规: 0 ✅
- [ ] @别名违规: 0 ✅
- [ ] as any使用: 0 ✅
- [ ] @ts-ignore使用: 0 ✅
- [ ] 废弃包引用: 0 ✅
```

### 失败处理

```yaml
症状: 检查结果 > 0
原因: 代码违反架构规范

解决方案:
  1. 使用grep定位具体违规位置
     grep -rn "'../'" src/SmartAbp.Vue/packages/

  2. 修改为正确的引用方式
     - 相对路径 → 包名引用
     - @别名 → backend-contracts类型
     - as any → 具体类型定义

  3. 重新检查直到全部为0
```

---

## 🔍 第二关: 代码重复度检查

### 检查目标
**确保无重复组件、函数、类名**

### 检查项清单

#### 检查1: 重复文件名(Vue组件)

```bash
- [ ] 检查Vue组件重复
      命令: find src/SmartAbp.Vue/src src/SmartAbp.Vue/packages -name "*.vue" | sed 's/.*\///' | sort | uniq -d
      预期: 无输出 ✅

      违规示例:
        ❌ src/views/UserList.vue
        ❌ src/components/UserList.vue (重复)
```

#### 检查2: 重复函数签名

```bash
- [ ] 检查TypeScript函数重复
      命令: grep -rh "export function\|export const.*=" src/ --include="*.ts" | sort | uniq -d | wc -l
      预期: 0 ✅

      违规示例:
        ❌ export function getUserList() {} (文件A)
        ❌ export function getUserList() {} (文件B,重复)
```

#### 检查3: 重复类/接口名(后端)

```bash
- [ ] 检查C#类/接口重复
      命令: grep -rh "class.*Service\|interface.*Dto" src/ --include="*.cs" | sort | uniq -d | wc -l
      预期: 0 ✅

      违规示例:
        ❌ public class UserAppService (模块A)
        ❌ public class UserAppService (模块B,重复)
```

### 验证清单

```markdown
- [ ] 组件名重复: 0 ✅
- [ ] 函数名重复: 0 ✅
- [ ] 类/接口名重复: 0 ✅
```

### 失败处理

```yaml
症状: 发现重复项
原因: 命名冲突

解决方案:
  1. 列出所有重复项
     find -name "*.vue" | sed 's/.*\///' | sort | uniq -d

  2. 分析保留哪个版本
     - 检查功能是否真的重复
     - 如果重复,保留更完整的版本
     - 如果不重复,重命名区分

  3. 删除或重命名重复项

  4. 重新检查直到无重复
```

---

## ⚙️ 第三关: 编译静态检查

### 检查目标
**确保代码编译通过,符合代码规范**

### 检查项清单

#### 检查1: TypeScript类型检查

```bash
- [ ] 执行TypeScript编译检查
      命令: npm run type-check
      目录: src/SmartAbp.Vue
      预期: 0 errors ✅

      成功输出示例:
        ✓ Type checking completed successfully

      失败输出示例:
        ✗ src/views/User.vue:25:10
        Type 'string' is not assignable to type 'number'
```

#### 检查2: ESLint代码规范检查

```bash
- [ ] 执行ESLint检查
      命令: npm run lint
      目录: src/SmartAbp.Vue
      预期: 0 errors, 0 warnings ✅

      成功输出示例:
        ✓ 0 errors, 0 warnings

      失败输出示例:
        ✗ 2 errors, 3 warnings
        src/views/User.vue:10:1  'unused-var' is defined but never used
```

#### 检查3: 后端编译检查(如适用)

```bash
- [ ] 执行.NET编译检查
      命令: dotnet build
      目录: src/
      预期: Build succeeded. 0 Error(s) ✅

      成功输出示例:
        Build succeeded.
        0 Warning(s)
        0 Error(s)

      失败输出示例:
        Build FAILED.
        src/XXX.cs(10,5): error CS0103: The name 'xxx' does not exist
```

### 自动修复(可选)

```bash
- [ ] ESLint自动修复(可选,谨慎使用)
      命令: npm run lint --fix
      说明: 仅修复安全的格式问题
```

### 验证清单

```markdown
- [ ] TypeScript编译: 0错误 ✅
- [ ] ESLint检查: 0错误0警告 ✅
- [ ] .NET编译(如适用): 成功 ✅
```

### 失败处理

```yaml
TypeScript错误:
  症状: Type 'X' is not assignable to type 'Y'
  解决:
    1. 查看错误信息定位文件和行号
    2. 使用search_symbol查找类型定义
    3. 修正类型或添加类型定义
    4. 重新检查

ESLint错误:
  症状: Unexpected console statement
  解决:
    1. 查看错误规则
    2. 修正代码或添加eslint-disable注释
    3. 如果是格式问题,使用 --fix 自动修复
    4. 重新检查

.NET编译错误:
  症状: CS0103: The name 'xxx' does not exist
  解决:
    1. 查看错误信息定位问题
    2. 添加using语句或修正命名空间
    3. 重新编译检查
```

---

## 📦 第四关: packages专项检查

### 触发条件
**仅当修改了packages目录时执行**

### 智能跳过机制

```bash
if (packages无修改) {
  跳过第四关,节省时间
  继续第五关
}
```

### 检查项清单

#### 检查1: packages编译

```bash
- [ ] 检查packages编译
      命令: cd src/SmartAbp.Vue && npm run build:packages
      预期: 编译成功 ✅

      成功输出:
        ✓ lowcode-shared built successfully
        ✓ lowcode-core built successfully
        ✓ lowcode-designer built successfully
```

#### 检查2: packages lint

```bash
- [ ] 检查packages代码规范
      命令: npm run lint:packages
      预期: 0错误0警告 ✅
```

#### 检查3: packages依赖关系

```bash
- [ ] 检查packages依赖层级
      验证依赖层级规则:
        层级0: lowcode-shared (零包依赖) ✅
        层级1: lowcode-core, lowcode-api (只依赖shared) ✅
        层级2: lowcode-designer (依赖shared+core) ✅

      禁止:
        ❌ 层级0依赖任何包
        ❌ 逆向依赖
        ❌ 循环依赖
```

### 验证清单

```markdown
- [ ] packages编译: 成功 ✅
- [ ] packages lint: 0错误0警告 ✅
- [ ] 依赖层级: 合规 ✅
- [ ] 构建产物: 生成成功 ✅
```

---

## 📊 第五关: 技术债务监控

### 检查目标
**监控技术债务,保持代码健康度**

### 检查项清单

#### 检查1: 技术债务评分

```bash
- [ ] 计算技术债务评分
      公式:
        基础分: 100分
        - 大文件扣分: 每个>500行文件 -2分
        - TODO扣分: 每10个TODO -1分
        - FIXME扣分: 每5个FIXME -2分
        - 重复代码扣分: 重复率每1% -1分

      预期: ≥85分 ✅
```

#### 检查2: 大文件监控

```bash
- [ ] 检查大文件数量
      命令: find src/ -name "*.ts" -o -name "*.vue" | xargs wc -l | awk '$1>500' | wc -l
      预期: <10个 ⚠️ (警告)

      超过500行的文件应考虑拆分
```

#### 检查3: TODO/FIXME统计

```bash
- [ ] 统计待办事项
      命令: grep -r "TODO\|FIXME" src/ --include="*.ts" --include="*.vue" | wc -l
      预期: <50个 ⚠️ (警告)

      过多TODO表明代码未完成或需要改进
```

#### 检查4: 无重复组件名(最终检查)

```bash
- [ ] 最终重复检查
      确保第二关的重复检查结果持续为0
```

### 评分标准

```yaml
优秀(95-100分):
  - 代码质量极高
  - 几乎无技术债务
  - 可直接发布生产

良好(85-94分): ✅
  - 代码质量高
  - 技术债务可控
  - 符合发布标准

需改进(70-84分):
  - 代码质量一般
  - 技术债务较多
  - 建议优化后发布

不合格(<70分):
  - 代码质量差
  - 技术债务严重
  - 禁止发布,必须重构
```

### 验证清单

```markdown
- [ ] 技术债务评分: ≥85分 ✅
- [ ] 大文件数量: <10个 ⚠️
- [ ] TODO/FIXME: <50个 ⚠️
- [ ] 无重复组件: ✅
```

---

## 🚀 并行执行优化

### 优化策略
**合理利用并行,加速检查过程**

### 执行顺序

```bash
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 并行组1: 编译检查(最耗时,优先启动)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

npm run type-check &
PID_TS=$!

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 并行组2: 轻量级检查(可同时执行)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 第一关: 架构检查
check_architecture() {
  grep -r "'../'" packages/ | wc -l
  grep -r "@/" packages/ | wc -l
} &
PID_ARCH=$!

# 第二关: 重复检查
check_duplicates() {
  find -name "*.vue" | sed 's/.*\///' | sort | uniq -d
} &
PID_DUP=$!

# 等待并行组1和2完成
wait $PID_TS $PID_ARCH $PID_DUP

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 串行执行: 第三关ESLint(依赖TS检查结果)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

npm run lint

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 条件执行: 第四关packages(仅在修改时)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

if [packages有修改]; then
  npm run build:packages
  npm run lint:packages
fi

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 串行执行: 第五关技术债务(最快)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

calculate_tech_debt()
```

**预期总时长**: 15-25秒(并行) vs 35-45秒(串行)

---

## 📋 完整检查清单(汇总)

```markdown
☑️ 第一关: 架构完整性 ✅
   - [x] 相对路径: 0违规
   - [x] @别名: 0违规
   - [x] as any: 0使用
   - [x] 废弃包: 0引用

☑️ 第二关: 代码重复度 ✅
   - [x] 组件名: 0重复
   - [x] 函数名: 0重复
   - [x] 类名: 0重复

☑️ 第三关: 编译静态检查 ✅
   - [x] TypeScript: 0错误
   - [x] ESLint: 0错误0警告
   - [x] .NET编译: 成功(如适用)

☑️ 第四关: packages专项 ✅
   - [x] 编译: 成功
   - [x] lint: 0错误0警告
   - [x] 依赖: 合规
   - 说明: 仅在修改packages时执行

☑️ 第五关: 技术债务 ✅
   - [x] 评分: ≥85分
   - [x] 大文件: <10个⚠️
   - [x] TODO: <50个⚠️

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 五关全部通过!
📊 代码质量: 优秀
🚀 可以进入Git同步阶段
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 🎯 架构标准参考

### 后端ABP vNext架构(98/100分)

```yaml
DDD分层架构:
  Layer 4 - HttpApi (控制器层):
    - RESTful规范: 100%符合
    - 路由标准: /api/[module]/[entity]

  Layer 3 - Application (应用服务层):
    - CRUD方法: 完整实现
    - AutoMapper: 100%正确
    - 查询优化: IQueryable分页

  Layer 2 - Domain (领域层):
    - 聚合根: 完整定义
    - 导航属性: 100%配置
    - 业务逻辑: 集中在Domain

  Layer 1 - Domain.Shared (共享基础):
    - 枚举/常量: 统一定义
    - 零业务依赖: 100%合规

  Layer 0 - EntityFrameworkCore (基础设施):
    - Fluent API: 关系映射完整
    - Migration: 数据库同步
```

### 前端packages架构(95/100分)

```yaml
契约类型系统(backend-contracts.ts):
  - 独立契约层: 零外部依赖 ✅
  - 45个契约类型: 100%精确映射后端DTO ✅
  - 向后兼容: type aliases ✅

packages依赖层级:
  层级0: lowcode-shared
    - backend-contracts.ts(45个契约)
    - 零包依赖 ✅

  层级1: lowcode-core, lowcode-api
    - 只依赖lowcode-shared ✅

  层级2: lowcode-designer
    - 依赖shared+core ✅

禁止规则:
  ❌ 层级0依赖任何包
  ❌ 逆向依赖(层级1→层级2)
  ❌ 循环依赖(A→B→A)
  ❌ packages引用src/api/generated
```

---

## 📚 相关文档

- [返回主文档](00_执行引擎_主文档.mdc)
- [Level 3-模式1: 四阶段流程](03_Level3_模式1_四阶段.mdc)
- [Level 3-模式2: 八阶段流程](04_Level3_模式2_完整八阶段.mdc)

---

## 📊 元数据

```yaml
版本: v14.0
创建日期: 2025-10-24
更新日期: 2025-10-24
作者: SmartAbp团队
字数: ~300行
检查时长: 3-5分钟
通过标准: 0错误0警告0违规
重要性: 极高(质量保障核心)
```

---

**最后更新**: 2025-10-24
**核心原则**: 0错误0警告0违规,绝不妥协!
