---
description: "SmartAbp开发流程规范 - 质量保证和测试策略（专家模式深度分析）"
globs: ["**/*.ts", "**/*.vue", "**/*.cs"]
alwaysApply: true
priority: 900
expertMode: true
---

# 🔄 SmartAbp 开发流程规范

## 阶段一：代码编写前的强制准备工作

### 1. 规则加载检查
- [ ] 阅读核心开发原则
- [ ] 检查相关模板文件
- [ ] 查阅相关ADR文档

### 2. Serena代码分析
- [ ] 使用代码搜索工具查找相关实现
- [ ] 分析模块结构和依赖关系
- [ ] 避免重复代码（致命错误）

### 3. 架构理解确认
- [ ] 理解当前代码结构
- [ ] 确认技术栈和依赖关系
- [ ] 检查低代码引擎Monorepo结构

## 阶段二：工作计划审批制度

### 计划制定要求
- 使用sequential-thinking制定详细计划
- 分析工作步骤和风险评估
- 必须等待用户审批确认

### 计划必须包含
- [ ] 详细的实现步骤
- [ ] 模板使用说明
- [ ] ADR遵循情况
- [ ] 验证检查点
- [ ] 风险评估
- [ ] 时间估算

## 阶段三：测试质量保证

### 灵活测试策略
- **复杂功能**：推荐使用TDD测试驱动开发，确保设计质量
- **基础功能**：编写适当的单元测试和集成测试
- **关键业务流程**：必须有完整的测试覆盖

### TDD循环执行（复杂功能推荐）
1. **红**：编写测试 → 运行测试（必须失败）
2. **绿**：编写最小实现 → 运行测试（必须通过）
3. **重构**：优化代码 → 运行测试（必须保持通过）

### 测试质量要求（参考已验证的测试标准）
- [ ] 单元测试：重要公共方法有对应测试
- [ ] 集成测试：关键业务流程有端到端测试
- [ ] 边界测试：覆盖主要异常情况和边界条件
- [ ] 性能测试：关键功能有性能基准测试

### 🏆 TDD Phase 2&3 成功验证（48/48测试通过）
- ✅ **Phase 2**: 增强主题系统（19/19测试通过）
  - EnhancedThemeEditor.vue (600+行)
  - useEnhancedThemeStore.ts (280行)
  - 三层令牌架构+WCAG验证+快照管理
- ✅ **Phase 3**: 增强状态机引擎（20/20测试通过）  
  - EnhancedStateMachine.vue (700+行)
  - useEnhancedStateMachineStore.ts (520行)
  - 业务规则引擎+代码生成+工作流模板
- ✅ **企业工作台**: LowCode Studio（9/9测试通过）
  - LowCodeStudioView.vue (企业级三步流程)
  - 壳层+导航+工作区+日志栏完整实现

## 阶段四：质量验证检查

### 强制验证序列
```bash
npm run type-check   # 零类型错误
npm run lint         # 零lint错误
npm run test:coverage # ≥80%测试覆盖率
npm run build        # 零构建错误
npm run dev          # 零运行时错误
```

### 代码质量检查
- [ ] ESLint零错误零警告
- [ ] TypeScript零类型错误
- [ ] 测试覆盖率≥80%
- [ ] 性能基准达标
- [ ] 安全检查通过

## 🚨 阶段五：强制质量保证铁律（每个开发任务完成后必须执行）

### 1. 架构整洁强制检查
每次完成一个阶段的开发任务后，**必须立即**进行架构整洁检查：
- [ ] **架构整洁铁律验证**: 检查是否违反黑盒原则、路径隔离、别名通信
- [ ] **低代码引擎架构铁律验证**: 检查是否违反packages目录架构约束
- [ ] **违规检查命令**:
  ```bash
  # 检查相对路径违规
  grep -r "'../'" src/SmartAbp.Vue/packages/ --include="*.ts" --include="*.vue"
  # 检查@/主应用引用违规  
  grep -r "@/" src/SmartAbp.Vue/packages/ --include="*.ts" --include="*.vue"
  # 检查类型绕过违规
  grep -r "as any\|@ts-ignore" src/ --include="*.ts" --include="*.vue"
  ```
- [ ] **零容忍标准**: 任何架构违规必须立即修复，禁止例外

### 2. 重复代码强制检查
每次完成一个阶段的开发任务后，**必须立即**进行重复代码检查：
- [ ] **同名检查**: 严禁实现同样名字的方法、类、组件、路由、状态
- [ ] **同功能检查**: 严禁实现同样功能的代码块
- [ ] **检查范围**: 方法名、类名、组件名、路由路径、状态变量、接口定义
- [ ] **DRY原则执行**: Don't Repeat Yourself - 发现重复立即重构
- [ ] **重复代码检查工具**:
  ```bash
  # 检查重复方法名
  grep -r "function\s+\w+\|const\s+\w+\s*=" src/ --include="*.ts" --include="*.vue" | sort | uniq -d
  # 检查重复类名
  grep -r "class\s+\w+\|interface\s+\w+" src/ --include="*.ts" | sort | uniq -d
  ```

### 3. 代码编译和质量强制检查
每次完成一个阶段的开发任务后，**必须立即**进行编译和质量检查：
- [ ] **前端项目检查**:
  ```bash
  npm run type-check  # TypeScript类型检查 - 必须通过
  npm run lint --fix  # ESLint代码规范检查 - 必须通过
  npm run build       # 构建检查 - 必须成功
  ```
- [ ] **后端项目检查**:
  ```bash
  dotnet build src/SmartAbp.CodeGenerator/SmartAbp.CodeGenerator.csproj  # 编译检查 - 必须通过
  dotnet test --no-build  # 单元测试检查 - 必须通过
  ```
- [ ] **🚨 BUG修复铁律**: 
  - ❌ **严禁删除代码**来"修复"编译错误
  - ❌ **严禁注释代码**来通过编译检查
  - ❌ **严禁使用`as any`**绕过类型检查
  - ❌ **严禁使用`@ts-ignore`**忽略类型错误
  - ✅ **必须在企业最佳编程规则前提下**完成高质量修复
  - ✅ **必须通过完善类型定义**解决类型错误
  - ✅ **必须通过重构和优化**解决代码问题

### 4. Git版本管理强制同步
代码质量检查通过后，**必须立即**执行版本管理：
- [ ] **使用项目标准Git脚本**:
  ```bash
  bash scripts/git-safe-sync.sh --non-interactive --auto-commit
  ```
- [ ] **禁止手动Git命令**: 严禁使用手动git add/commit/push
- [ ] **同步验证**: 确保本地与远程仓库完全同步
- [ ] **冲突处理**: 如有冲突，使用项目标准解决流程

## 🏆 强制执行标准
- **零例外政策**: 以上4个检查步骤，每个开发任务完成后**必须全部执行**
- **质量门禁**: 任何一个检查不通过，**禁止进入下一个开发任务**  

- **AI行为约束**: 任何AI编程模型都必须严格遵守此流程