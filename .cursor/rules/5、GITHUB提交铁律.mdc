---
description: "Git版本控制统一铁律 - AI强制执行规则"
globs: ["**/*"]
alwaysApply: true
priority: 1100
---

# 🚨 Git版本控制统一铁律

## 核心原则
**每完成一个小节的功能开发，AI必须立即执行以下强制流程，无任何例外：**


### 1. 架构整洁强制检查
每次完成一个阶段的开发任务后，**必须立即**进行架构整洁检查：
- [ ] **架构整洁铁律验证**: 检查是否违反黑盒原则、路径隔离、别名通信
- [ ] **低代码引擎架构铁律验证**: 检查是否违反packages目录架构约束
- [ ] **违规检查命令**:
  ```bash
  # 检查相对路径违规
  grep -r "'../'" src/SmartAbp.Vue/packages/ --include="*.ts" --include="*.vue"
  # 检查@/主应用引用违规
  grep -r "@/" src/SmartAbp.Vue/packages/ --include="*.ts" --include="*.vue"
  # 检查类型绕过违规
  grep -r "as any\|@ts-ignore" src/ --include="*.ts" --include="*.vue"
  ```
- [ ] **零容忍标准**: 任何架构违规必须立即修复，禁止例外

### 2. 重复代码强制检查
每次编程30分钟之后，或每次完成一个阶段的开发任务后，**必须立即**进行重复代码检查：
- [ ] **同名检查**: 严禁实现同样名字的方法、类、组件、路由、状态
- [ ] **同功能检查**: 严禁实现同样功能的代码块
- [ ] **检查范围**: 方法名、类名、组件名、路由路径、状态变量、接口定义
- [ ] **DRY原则执行**: Don't Repeat Yourself - 发现重复立即重构
- [ ] **重复代码检查工具**:
  ```bash
  # 检查重复方法名
  grep -r "function\s+\w+\|const\s+\w+\s*=" src/ --include="*.ts" --include="*.vue" | sort | uniq -d
  # 检查重复类名
  grep -r "class\s+\w+\|interface\s+\w+" src/ --include="*.ts" | sort | uniq -d
  ```

### 3. 代码编译和质量强制检查
每次完成一个阶段的开发任务后，**必须立即**进行编译和质量检查：
- [ ] **前端项目检查**:
  ```bash
  npm run type-check  # TypeScript类型检查 - 必须通过
  npm run lint --fix  # ESLint代码规范检查 - 必须通过
  npm run build       # 构建检查 - 必须成功
  ```
- [ ] **后端项目检查**:
  ```bash
  dotnet build src/SmartAbp.CodeGenerator/SmartAbp.CodeGenerator.csproj  # 编译检查 - 必须通过
  dotnet test --no-build  # 单元测试检查 - 必须通过
  ```
- [ ] **🚨 BUG修复铁律**:
  - ❌ **严禁删除代码**来"修复"编译错误
  - ❌ **严禁注释代码**来通过编译检查
  - ❌ **严禁使用`as any`**绕过类型检查
  - ❌ **严禁使用`@ts-ignore`**忽略类型错误
  - ✅ **必须在企业最佳编程规则前提下**完成高质量修复
  - ✅ **必须通过完善类型定义**解决类型错误
  - ✅ **必须通过重构和优化**解决代码问题

### 4. Git版本管理强制同步
代码质量检查通过后，**必须立即**执行版本管理：
- [ ] **使用项目标准Git脚本**:
  ```bash
  bash scripts/git-safe-sync.sh --non-interactive --auto-commit
  ```
- [ ] **禁止手动Git命令**: 严禁使用手动git add/commit/push
- [ ] **同步验证**: 确保本地与远程仓库完全同步
- [ ] **冲突处理**: 如有冲突，使用项目标准解决流程

## 🏆 强制执行标准
- **零例外政策**: 以上4个检查步骤，每个开发任务完成后**必须全部执行**
- **质量门禁**: 任何一个检查不通过，**禁止进入下一个开发任务**

- **AI行为约束**: 任何AI编程模型都必须严格遵守此流程

### 第一阶段：质量门禁检查（零容忍）
```bash
# AI必须执行这些命令并确保100%通过
npm run type-check     # TypeScript类型检查 - 必须0错误
npm run lint --fix     # ESLint代码规范 - 必须0错误
npm run build          # 构建验证 - 必须成功
```

### 第二阶段：强制Git同步
```bash
# 优先使用项目Git脚本
bash scripts/git-safe-sync.sh --non-interactive --auto-commit

# 脚本不可用时的备用方案
git add . && git commit -m "功能完成: AI开发小节" && git pull && git push
```

### 第三阶段：同步验证
AI必须验证：
- ✅ 远程推送成功
- ✅ `git status` 显示工作区干净
- ✅ 本地与远程版本一致

## AI行为强制要求

### 触发条件
- 📝 完成任何Vue组件编写/修改
- 🔧 修复任何TypeScript/JavaScript错误
- 🏗️ 完成任何架构重构
- 📚 更新任何文档文件
- 🧪 编写/修改任何测试文件

### 执行要求
- ✅ 必须自动执行质量检查和Git同步
- ❌ 禁止要求用户手动执行Git操作
- ❌ 禁止跳过质量检查或同步流程

### 异常处理
- **网络问题**：重试3次，失败后告知用户
- **合并冲突**：优先保留远程更改
- **操作失败**：记录原因，要求解决后重新执行

**铁律：每个开发小节都必须严格执行！**
