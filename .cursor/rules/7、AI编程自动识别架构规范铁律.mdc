---
description: "防止AI编程模型破坏前端框架工程化优化成果的强制识别机制"
globs: ["**/*"]
alwaysApply: true
priority: 1400
smartDetection: true
---

# 🧠 AI编程自动识别架构规范铁律

## 🎯 核心问题
**问题描述**: 前端框架工程化优化完成后，AI编程模型不能自动识别优化成果，很快又搞乱了架构。

**解决策略**: 建立智能架构识别和保护机制，确保AI始终遵循已建立的工程化标准。

## 🔍 自动架构识别触发机制

### 强制识别触发条件
当AI遇到以下操作时，**必须先执行架构识别流程**：

#### 1. 文件操作触发
```typescript
// 触发条件
- 创建新文件 (*.vue, *.ts, *.js)
- 修改packages目录下任何文件
- 添加新组件或工具函数
- 修改类型定义文件
```

#### 2. 代码编写触发
```typescript
// 触发条件
- 编写组件 → 必须检查是否已有相似组件
- 编写工具函数 → 必须检查lowcode-shared中是否已存在
- 编写错误处理 → 必须使用统一错误管理系统
- 编写类型定义 → 必须检查全局类型声明
```

#### 3. 架构变更触发
```typescript
// 触发条件
- 任何package.json修改
- 任何依赖关系变更
- 任何目录结构调整
- 任何导入路径修改
```

## 🏗️ 架构识别执行流程

### 第一步：项目架构扫描
```bash
🔍 **强制架构扫描** (每次编程前必须执行)

1. **packages架构识别**
   ✅ 检查packages目录结构是否符合标准
   ✅ 验证lowcode-shared基础库是否存在
   ✅ 确认各package职责分离是否清晰

2. **现有组件库扫描**
   ✅ 列出所有现有Vue组件
   ✅ 识别可复用的组件模式
   ✅ 检查组件命名规范

3. **工具函数库识别**
   ✅ 扫描lowcode-shared/utils目录
   ✅ 识别已有的ValidationUtils、PerformanceUtils等
   ✅ 确认模块化拆分状态
```

### 第二步：代码重复检测
```bash
🚫 **代码重复强制检测** (编程时实时检查)

1. **组件重复检测**
   - 新建组件前必须搜索相似功能组件
   - 发现重复立即停止，改为复用现有组件

2. **函数重复检测**
   - 编写工具函数前必须检查lowcode-shared
   - 发现重复立即停止，改为导入现有函数

3. **类型重复检测**
   - 定义类型前必须检查全局类型声明
   - 发现重复立即停止，改为引用现有类型
```

### 第三步：架构规范验证
```bash
🛡️ **架构规范强制验证** (每次提交前必须通过)

1. **目录结构验证**
   ✅ packages/lowcode-shared/src/types/ (全局类型)
   ✅ packages/lowcode-shared/src/errors/ (统一错误)
   ✅ packages/lowcode-shared/src/utils/ (通用工具)
   ✅ packages/lowcode-shared/src/components/ (共享组件)

2. **依赖关系验证**
   ✅ 禁止高层package依赖低层package
   ✅ 禁止packages间循环依赖
   ✅ 强制使用@smartabp/*别名

3. **类型安全验证**
   ✅ 禁止使用as any绕过类型检查
   ✅ 强制使用严格类型定义
   ✅ 确保全局类型声明统一
```

## 🤖 AI编程强制识别指令

### 编程前强制检查清单
```typescript
🚨 **每次编程前AI必须执行以下检查**：

1. **🔍 Step 1: 架构扫描**
   - list_dir("src/SmartAbp.Vue/packages")
   - grep("Loading|Error|Utils", path="packages", output_mode="files_with_matches")
   - mcp_serena_get_symbols_overview("src/SmartAbp.Vue/packages/lowcode-shared")

2. **🔍 Step 2: 现有功能检索**
   - 搜索相似组件: codebase_search("相关功能关键词")
   - 检查已有工具函数: mcp_serena_find_symbol("Utils")
   - 验证类型定义: grep("interface|type", path="packages/lowcode-shared/src/types")

3. **🔍 Step 3: 架构规范确认**
   - 读取架构文档: read_file("docs/架构优化/前端框架工程化优化方案.md")
   - 验证packages层级: 确认当前操作符合Phase 0重构方案
   - 检查依赖关系: 避免违反依赖层级定义
```

### 编程中强制遵循规范
```typescript
🛡️ **编程过程中AI必须遵循**：

1. **组件编写规范**
   - 禁止重复造轮子，优先复用现有组件
   - 必须继承BaseComponentProps接口
   - 必须使用统一的LoadingState组件

2. **工具函数规范**
   - 禁止在业务文件中定义通用工具函数
   - 必须将工具函数添加到lowcode-shared/utils
   - 必须使用统一的ValidationUtils、PerformanceUtils

3. **错误处理规范**
   - 禁止定义新的错误类
   - 必须使用lowcode-shared/errors中的BaseError
   - 必须通过ErrorManager统一处理错误

4. **类型定义规范**
   - 禁止在业务文件中定义全局类型
   - 必须在lowcode-shared/types中统一定义
   - 必须使用严格类型，禁止as any
```

## 🚨 违规自动纠正机制

### 智能违规检测
```typescript
📋 **自动违规检测规则**：

1. **重复代码检测**
   if (新建组件 && 存在相似组件) {
     停止创建 → 提示复用现有组件 → 展示复用示例
   }

2. **架构违规检测**
   if (违反packages层级) {
     停止操作 → 解释层级规范 → 提供正确实现方式
   }

3. **类型安全检测**
   if (使用as any) {
     停止编写 → 要求提供严格类型 → 展示类型定义示例
   }
```

### 自动纠正流程
```typescript
🔧 **违规自动纠正步骤**：

1. **立即停止当前操作**
   - 不继续编写违规代码
   - 不提交不符合规范的内容

2. **识别正确实现方式**
   - 搜索现有相似实现
   - 引用架构文档中的标准做法
   - 提供具体的代码示例

3. **执行标准化实现**
   - 使用现有组件/工具/类型
   - 遵循established架构模式
   - 确保代码质量达到95分标准
```

## 🎯 架构保护策略

### 持续架构监控
```typescript
⚡ **实时架构保护机制**：

1. **文件创建拦截**
   - 创建.vue文件 → 自动检查是否有相似组件
   - 创建.ts工具文件 → 自动检查lowcode-shared/utils
   - 创建类型文件 → 自动检查全局类型声明

2. **代码编写引导**
   - 智能提示现有可复用组件
   - 自动导入推荐的工具函数
   - 强制使用标准化的错误处理

3. **架构演进保护**
   - 任何架构变更都必须经过架构文档更新
   - 重大变更必须更新重构方案文档
   - 确保向前兼容性和渐进式改进
```

### 团队协作保护
```typescript
👥 **多人协作架构一致性**：

1. **知识共享机制**
   - 所有架构决策记录在ADR中
   - 重构方案文档实时更新
   - 代码Review强制检查架构合规性

2. **自动化质量门禁**
   - Git hooks强制执行架构检查
   - CI/CD流水线集成架构验证
   - 违规代码无法合并到主分支

3. **持续改进机制**
   - 定期架构健康检查
   - 性能基准回归测试
   - 技术债务监控和清理
```

## 💡 AI编程最佳实践

### 编程前思考清单
```typescript
🤔 **每次编程前AI必须思考**：

1. **是否有现成的解决方案？**
   - 搜索现有组件库
   - 检查工具函数库
   - 查看类型定义库

2. **我的实现是否符合架构规范？**
   - 检查packages层级
   - 验证依赖关系
   - 确认命名规范

3. **是否会产生技术债务？**
   - 避免代码重复
   - 确保类型安全
   - 遵循SOLID原则
```

### 编程中验证机制
```typescript
✅ **编程过程中持续验证**：

1. **每写10行代码** → 检查是否符合架构规范
2. **每完成一个功能** → 执行代码重复检测
3. **每提交代码前** → 通过完整质量门禁
```

## 🚀 成功实施保障

### 技术保障
- **自动化检查工具**: 集成到IDE和CI/CD中
- **智能提示系统**: 实时引导正确的架构实践
- **强制质量门禁**: 确保低质量代码无法进入主分支

### 流程保障
- **架构文档维护**: 确保文档与代码同步更新
- **定期架构Review**: 持续监控和改进架构质量
- **团队培训机制**: 确保所有开发者理解架构规范

### 效果保障
- **架构一致性**: 100%符合工程化标准
- **代码复用率**: 维持85%以上复用率
- **开发效率**: 持续提升40%的开发效率

---

## 🎖️ 执行承诺

**🔥 专家模式保证**：
- 🧠 **智能识别**: 100%准确识别已建立的架构模式
- 🛡️ **架构保护**: 0容忍破坏现有工程化成果
- 🚀 **持续改进**: 在现有基础上持续优化，而非重复造轮子
- 📈 **质量保证**: 确保每次编程都提升整体架构质量

**违反此铁律的后果**: 立即停止当前操作，执行架构识别流程，重新按照规范实现！
