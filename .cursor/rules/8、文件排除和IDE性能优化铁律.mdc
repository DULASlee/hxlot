# 8、文件排除和IDE性能优化铁律

## 🚀 核心优化原则

### 1. 文件监控排除原则 (零容忍)
- **严禁** IDE监控编译产物、依赖目录、生成代码
- **必须** 将以下目录添加到 `files.watcherExclude`:
  - `**/node_modules/**` - 所有依赖目录
  - `**/.git/**` - Git元数据
  - `**/dist/**`, `**/build/**` - 编译输出
  - `**/packages/**/dist/**` - packages编译产物
  - `**/generated/**`, `**/*.generated.*` - 生成代码
  - `**/__tests__/__snapshots__/**` - 测试快照

### 2. 搜索优化原则 (强制要求)
- **必须** 排除非源码目录，提升搜索效率
- **必须** 将以下目录添加到 `search.exclude`:
  - `**/node_modules` - 依赖目录
  - `**/packages/**/dist` - packages编译产物
  - `**/generated` - 生成代码目录

### 3. TypeScript监控优化 (性能关键)
- **必须** 配置 `typescript.tsserver.watchOptions`
- **必须** 排除以下目录的TypeScript监控:
  - `**/node_modules`
  - `**/dist`
  - `**/generated`
  - `**/build`
  - `**/coverage`

## 📋 标准配置模板

### VS Code标准配置 (必须使用)
```json
{
  "files.watcherExclude": {
    "**/node_modules/**": true,
    "**/.git/**": true,
    "**/dist/**": true,
    "**/build/**": true,
    "**/coverage/**": true,
    "**/packages/**/dist/**": true,
    "**/packages/**/node_modules/**": true,
    "**/generated/**": true,
    "**/*.generated.*": true,
    "**/__tests__/__snapshots__/**": true,
    "**/.cursor-cache/**": true,
    "**/.vscode-server/**": true
  },
  "search.exclude": {
    "**/node_modules": true,
    "**/bower_components": true,
    "**/*.code-search": true,
    "**/packages/**/dist": true,
    "**/generated": true
  },
  "typescript.tsserver.watchOptions": {
    "exclude": [
      "**/node_modules",
      "**/dist",
      "**/generated",
      "**/build",
      "**/coverage"
    ]
  }
}
```

## 🎯 AI执行要求

### 1. 代码生成约束
- 所有新创建的目录/文件**必须**考虑IDE性能影响
- 生成代码**必须**输出到 `.generated/` 目录
- 编译产物**必须**输出到 `dist/` 或 `build/` 目录

### 2. 配置维护责任
- 新创建的目录如果需要排除，**必须**及时更新 `.vscode/settings.json`
- **严禁** 在源码目录中存放编译产物或生成代码
- **必须** 定期检查并优化监控配置

### 3. 团队协作要求
- 所有团队成员**必须**使用统一的IDE配置
- `.vscode/settings.json` **必须**提交到版本控制
- **严禁** 个人修改覆盖团队标准配置

## ⚡ 专家模式执行链

### 开发前检查 (强制)
```bash
# 检查当前IDE配置状态
grep -n "watcherExclude\|search.exclude" .vscode/settings.json
# 检查文件监控负载
find . -name "*.ts" -o -name "*.vue" -o -name "*.cs" | wc -l
```

### 性能优化验证 (完成开发后)
```bash
# 验证配置生效
code --list-extensions | grep -E "(typescript|vue)"
# 检查内存使用
ps aux | grep -E "(Code|Cursor)" | grep -v grep
```

## 🚨 违规处理

### 零容忍违规行为
- ❌ 在源码目录存放 `node_modules`
- ❌ 编译产物混入源码目录
- ❌ 生成代码污染源码结构
- ❌ 个人IDE配置覆盖团队标准

### 强制纠正措施
- 立即停止违规操作
- 按照标准配置模板修复
- 更新相关文档说明
- 团队通报学习

---

**执行标准**: 此铁律为专家模式强制要求，所有AI编程必须遵守
**生效时间**: 立即生效
**适用范围**: 全项目所有开发活动
