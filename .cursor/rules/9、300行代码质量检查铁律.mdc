---
alwaysApply: true
---

# AI架构铁律同心圆模型 - 第十重爆雷：300行代码质量检查铁律

## 文件说明
- **文件标识**: `9、300行代码质量检查铁律.mdc`
- **层级**: 第十重爆雷 - 代码编写量监控与质量保证
- **作用**: 确保AI在编写代码时保持适度节奏，每300行代码后强制执行质量检查，防止代码质量下降和技术债务积累！

---

## 🔥 300行代码质量检查铁律 (300-Line Code Quality Check Laws)

### 1. 🎯 强制代码量控制铁律
- **核心思想**: 每次编码会话不得超过300行代码，超过300行必须立即停止编写并进行质量检查。
- **执行准则**:
    - AI必须在内部跟踪当前编码会话的代码行数累计
    - 达到280行时发出警告，准备进行质量检查
    - 达到300行时立即停止编写，强制执行质量检查
    - 质量检查通过后才能继续编写下一个300行代码块

### 2. 🔍 强制质量检查流程 (Mandatory Quality Check Process)

#### 📊 **第一步：代码行数统计**
```bash
# 统计当前会话修改的代码行数
find src/ -name "*.ts" -o -name "*.vue" -o -name "*.js" | \
xargs wc -l | tail -1 | awk '{print "总代码行数:", $1}'

# 统计新增代码行数（基于git diff）
git diff --numstat | awk '{add+=$1} END {print "新增代码行数:", add}'
```

#### 🏗️ **第二步：五重强制质量门禁**（沿用现有机制）
1. **架构完整性检查 (0违规)**
2. **代码重复度检查 (0重复)**  
3. **编译与静态检查 (0错误)**
4. **低代码生成器专项检查 (100%质量)**
5. **技术债务监控检查 (基线控制)**

#### 🔐 **第三步：Git同步执行**
- 执行标准化Git提交和远程同步
- 确保每300行代码的质量修改都安全保存

### 3. 📈 代码质量节奏管理

#### ⚡ **节奏控制策略**
- **0-100行**: 正常编码速度，关注核心功能实现
- **100-200行**: 中度警觉，开始关注代码结构和重复度
- **200-280行**: 高度警觉，准备质量检查，优先完成当前功能模块
- **280-300行**: 强制收尾阶段，完成最后20行后立即质量检查

#### 📊 **质量指标监控**
```bash
# 执行300行质量检查脚本
echo "🔍 300行代码质量检查启动..."
echo "=================================="

# 统计代码复杂度
echo "📊 代码复杂度分析:"
COMPLEX_FILES=$(find src/ -name "*.ts" -o -name "*.vue" | xargs wc -l | \
  awk '$1 > 200 {print $2, $1}' | wc -l)
echo "  • 超过200行的文件数: ${COMPLEX_FILES}个"

# 统计技术债务
echo "💳 技术债务分析:"
AS_ANY_COUNT=$(grep -r "as any" src/ | wc -l)
TS_IGNORE_COUNT=$(grep -r "@ts-ignore" src/ | wc -l)
TODO_COUNT=$(grep -r "TODO\|FIXME\|XXX" src/ | wc -l)
echo "  • as any使用: ${AS_ANY_COUNT}个"
echo "  • @ts-ignore使用: ${TS_IGNORE_COUNT}个"
echo "  • TODO标记: ${TODO_COUNT}个"

# 质量评分
QUALITY_SCORE=100
if [ $AS_ANY_COUNT -gt 5 ]; then QUALITY_SCORE=$((QUALITY_SCORE - 10)); fi
if [ $TS_IGNORE_COUNT -gt 3 ]; then QUALITY_SCORE=$((QUALITY_SCORE - 15)); fi
if [ $COMPLEX_FILES -gt 10 ]; then QUALITY_SCORE=$((QUALITY_SCORE - 5)); fi

echo "🎯 300行质量评分: ${QUALITY_SCORE}/100"
```

### 4. 🚨 强制执行机制

#### ⚡ **自动触发检测**
```typescript
// AI内置300行检查触发器
let currentSessionLineCount = 0;
const MAX_LINES_PER_SESSION = 300;
const WARNING_THRESHOLD = 280;

function trackCodeLines(newLines: number) {
  currentSessionLineCount += newLines;
  
  if (currentSessionLineCount >= WARNING_THRESHOLD) {
    console.warn(`⚠️ 代码行数接近300行限制 (${currentSessionLineCount}/300)`);
  }
  
  if (currentSessionLineCount >= MAX_LINES_PER_SESSION) {
    console.error(`🚨 达到300行限制！必须执行质量检查！`);
    triggerQualityCheck();
    return false; // 阻止继续编写
  }
  
  return true; // 允许继续编写
}
```

#### 📋 **强制响应格式**
当达到300行时，AI必须立即响应：
```
🚨 **300行代码限制触发！立即执行质量检查！**

📊 **当前会话统计**:
   • 已编写代码行数: 300行
   • 修改文件数量: X个
   • 新增功能模块: X个

🔍 **启动五重强制质量门禁检查**:
   🏗️ 第一关：架构完整性检查...
   🔄 第二关：代码重复度检查...
   ⚡ 第三关：编译与静态检查...
   🎯 第四关：低代码生成器专项检查...
   🚀 第五关：技术债务监控检查...

🎯 **质量检查完成后方可继续编写下一个300行代码块！**
```

### 5. 🎖️ 质量检查通过标准

#### ✅ **通过条件**
- 架构完整性检查: 0违规
- 代码重复度检查: 0重复  
- 编译静态检查: 0错误
- 低代码生成器: 100%质量通过
- 技术债务评分: ≥85分

#### 📈 **持续改进机制**
- **记录质量趋势**: 每300行的质量评分变化
- **识别问题模式**: 分析常见的质量问题类型
- **优化编码策略**: 基于质量检查结果调整编码方法
- **建立质量基线**: 维护项目的质量基线和改进目标

### 6. 🛡️ 异常处理协议

#### 🚨 **紧急情况处理**
- **网络中断**: 本地保存代码，恢复后继续质量检查
- **编译失败**: 立即停止编写，修复错误后重新计算行数
- **架构违规**: 强制修复违规问题，重置计数器

#### 🔄 **重置机制**
- **质量检查通过**: 重置计数器，开始新的300行周期
- **重大重构**: 可申请重置计数器，重新开始统计
- **紧急修复**: P0级紧急修复不计入300行限制

### 7. 🎯 与现有铁律的集成

#### 🔗 **与阶段性开发质量守护铁律协同**
- 300行检查是对阶段性检查的补充和强化
- 每300行检查通过后，自动触发阶段性质量守护
- 双重保险确保代码质量的持续监控

#### 🚀 **与Git同步强制执行铁律协同**
- 300行质量检查通过后，立即执行Git同步
- 确保每300行高质量代码都安全保存到远程仓库
- 建立代码质量与版本控制的双重保障

---

## 🎖️ 总结

300行代码质量检查铁律确保AI在编写代码时保持适度的节奏和持续的质量监控。通过限制单次编码量和强制质量检查，防止因长时间编码导致的质量下降和技术债务积累。

**🔥 这是专家模式的第十重爆雷 - 300行代码质量检查！每编写300行代码都必须强制执行质量检查，确保代码质量的持续监控和改进！**

**⚡ 节奏控制！质量监控！持续改进！确保每一行代码都符合95分质量标准！**