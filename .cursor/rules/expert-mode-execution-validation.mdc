---
description: "专家模式执行验证铁律 - 防止AI执行与声称分离"
globs: ["**/*"]
alwaysApply: true
priority: 1200
expertMode: true
---

# 🚨 专家模式执行验证铁律

## 问题诊断
**关键缺陷**: AI声称"加载规范"但实际未执行具体规则验证，导致违反项目铁律。

## 强制执行验证环节

### 专家模式激活时的强制检查清单

当AI进入专家模式时，必须完成以下**实际验证**，而非仅仅声称：

#### 第一重爆雷：项目开发规范实际加载验证
- ✅ 必须使用 `read_file` 读取 `.cursor/rules/git-version-control-law.mdc`
- ✅ 必须识别Git操作的具体要求：`bash scripts/git-safe-sync.sh --non-interactive --auto-commit`
- ✅ 必须读取 `.cursor/rules/core-principles.mdc` 了解项目铁律
- ❌ 禁止仅声称"已加载规范"而不实际读取

#### 第二重爆雷：项目基础设施实际发现
- ✅ 必须使用 `list_dir` 检查 `scripts/` 目录
- ✅ 必须识别项目现有工具：git-safe-sync.sh/bat/ps1
- ✅ 必须使用 `read_file` 了解脚本功能和参数
- ❌ 禁止跳过项目工具发现直接使用手动方式

#### 第三重爆雷：规则符合性实际验证
- ✅ 执行任何Git操作前，必须验证是否符合 `git-version-control-law.mdc`
- ✅ 必须选择规则规定的方式：项目脚本优先，手动方式为备选
- ✅ 必须在操作后验证是否符合质量门禁要求
- ❌ 禁止违反明确的项目规则后再事后解释

## AI行为强制要求

### 专家模式响应格式修正
当进入专家模式时，必须使用此格式：

```
🔥 **专家模式已激活！执行验证中...**

📋 **第一重验证：实际读取项目规范**
   🔍 正在读取 .cursor/rules/git-version-control-law.mdc...
   [此处必须显示实际读取内容的关键摘要]
   ✅ Git操作要求：使用 scripts/git-safe-sync.sh --non-interactive --auto-commit
   ✅ 质量门禁要求：type-check + lint + build 必须通过

🔍 **第二重验证：实际发现项目工具**
   🔍 正在检查 scripts/ 目录...
   [此处必须显示实际发现的工具列表]
   ✅ 发现企业级Git脚本：git-safe-sync.sh (305行)
   ✅ 多平台支持：.sh/.bat/.ps1 三个版本

🚀 **第三重验证：规则符合性承诺**
   ✅ 承诺：所有Git操作将使用项目标准脚本
   ✅ 承诺：所有代码修改将通过质量门禁
   ✅ 承诺：违反规则时立即承认错误并纠正

[后续爆雷环节...]
```

### 违规处理机制
当AI违反已验证的规则时：
1. **立即停止当前操作**
2. **承认违规行为** - 不得找借口或技术辩护
3. **执行纠正措施** - 使用正确的项目标准方式重新执行
4. **更新执行记录** - 记录违规原因，避免重复错误

## 下一代AI防护机制

### 对话开始时的强制检查
每次新对话开始时，AI必须：
1. 检查是否触发专家模式关键词
2. 如果触发，立即执行实际验证而非形式化声称
3. 建立"已验证规则清单"作为后续操作的约束

### 操作前规则查询机制
执行任何重要操作前，AI必须：
1. 查询相关规则文件确认操作方式
2. 展示找到的具体规则内容
3. 确认操作符合规则要求后再执行

### 质量保证验证
所有专家模式操作完成后，AI必须：
1. 对照原始规则检查操作是否合规
2. 如发现违规，主动报告并提出改正方案
3. 记录经验教训，更新执行流程

## 技术实现要点

### 规则文件强制读取
- 使用 `read_file` 工具实际读取规则内容
- 解析关键规则条目并在响应中显示
- 建立规则-操作的映射关系

### 项目资产强制发现
- 使用 `list_dir` 和 `glob_file_search` 发现项目工具
- 使用 `read_file` 了解工具功能和使用方法
- 优先选择项目工具而非通用方法

### 操作后验证机制
- 每次操作后检查是否符合预期规则
- 如发现偏差，立即纠正并解释原因
- 建立操作-验证的闭环流程

这个机制的目标是确保AI的"专家模式"不再是空洞的声称，而是真正基于项目规范的专业执行。
