# Git版本管理铁律 - AI强制执行规则

## 🚨 CRITICAL: Git版本控制铁律

### 核心原则
**每完成一个小节的功能开发，AI必须立即执行以下强制流程，无任何例外：**

### 第一阶段：质量门禁检查（零容忍）
```bash
# AI必须执行这些命令并确保100%通过
npm run type-check     # TypeScript类型检查 - 必须0错误
npm run lint --fix     # ESLint代码规范 - 必须0错误
npm run build          # 构建验证 - 必须成功
```

**⚠️ 如果任何检查失败，AI必须立即修复，绝不允许跳过！**

### 第二阶段：强制Git同步（自动化）
AI必须选择以下方式之一执行Git同步：

#### 方式一：Git安全同步脚本（推荐）
```bash
bash scripts/git-safe-sync.sh --non-interactive --auto-commit
```

#### 方式二：手动标准流程
```bash
git add .
git commit -m "功能完成: $(date '+%Y%m%d_%H%M%S') - AI开发小节完成"
git pull origin main --strategy-option=theirs --no-edit
git push origin main
```

### 第三阶段：同步验证
AI必须验证：
- ✅ 远程推送成功
- ✅ `git status` 显示工作区干净
- ✅ 本地与远程版本一致

### AI行为强制要求

#### 什么时候必须执行Git同步？
- 📝 完成任何Vue组件编写/修改
- 🔧 修复任何TypeScript/JavaScript错误
- 🏗️ 完成任何架构重构
- 📚 更新任何文档文件
- 🧪 编写/修改任何测试文件
- ⚙️ 修改任何配置文件
- 🎨 完成任何UI/样式改进

#### AI响应模式
当完成上述任何工作后，AI必须：
1. **立即声明**："🚀 小节任务完成，启动版本控制铁律检查..."
2. **执行质量检查**：运行type-check、lint、build
3. **执行Git同步**：运行Git脚本或手动同步
4. **确认完成**："✅ 版本控制铁律执行完毕，代码已同步到远程仓库"

#### 严格禁止的行为
- ❌ **绝不允许**说"请手动执行Git操作"
- ❌ **绝不允许**跳过质量检查直接提交
- ❌ **绝不允许**在本地保留未同步的代码修改
- ❌ **绝不允许**因为"小改动"而跳过同步流程
- ❌ **绝不允许**在合并冲突时强制覆盖远程更改

### 异常处理规则

#### 网络问题
如果Git操作失败：
1. 重试最多3次
2. 如果依然失败，告知用户网络问题并提供手动同步指令
3. 记录失败原因，要求用户解决后重新执行

#### 合并冲突
遇到合并冲突时：
1. 优先保留远程更改（--strategy-option=theirs）
2. 如果自动合并失败，提供具体的冲突解决方案
3. 绝不允许强制推送覆盖远程更改

### 质量保证
AI必须在每次执行版本控制铁律后确认：
- ✅ 所有质量检查通过
- ✅ Git操作成功完成
- ✅ 远程仓库已更新
- ✅ 本地工作区干净

**这是不可妥协的铁律！每个开发小节都必须严格执行！**
