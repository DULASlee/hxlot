---
description: "Git版本控制统一铁律 - AI强制执行规则"
globs: ["**/*"]
alwaysApply: true
priority: 1100
---

# 🚨 Git版本控制统一铁律

## 核心原则
**每完成一个小节的功能开发，AI必须立即执行以下强制流程，无任何例外：**

### 第一阶段：质量门禁检查（零容忍）
```bash
# AI必须执行这些命令并确保100%通过
npm run type-check     # TypeScript类型检查 - 必须0错误
npm run lint --fix     # ESLint代码规范 - 必须0错误
npm run build          # 构建验证 - 必须成功
```

### 第二阶段：强制Git同步
```bash
# 优先使用项目Git脚本
bash scripts/git-safe-sync.sh --non-interactive --auto-commit

# 脚本不可用时的备用方案
git add . && git commit -m "功能完成: AI开发小节" && git pull && git push
```

### 第三阶段：同步验证
AI必须验证：
- ✅ 远程推送成功
- ✅ `git status` 显示工作区干净
- ✅ 本地与远程版本一致

## AI行为强制要求

### 触发条件
- 📝 完成任何Vue组件编写/修改
- 🔧 修复任何TypeScript/JavaScript错误
- 🏗️ 完成任何架构重构
- 📚 更新任何文档文件
- 🧪 编写/修改任何测试文件

### 执行要求
- ✅ 必须自动执行质量检查和Git同步
- ❌ 禁止要求用户手动执行Git操作
- ❌ 禁止跳过质量检查或同步流程

### 异常处理
- **网络问题**：重试3次，失败后告知用户
- **合并冲突**：优先保留远程更改
- **操作失败**：记录原因，要求解决后重新执行

**铁律：每个开发小节都必须严格执行！**
