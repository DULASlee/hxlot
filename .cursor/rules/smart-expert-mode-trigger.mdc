---
description: "智能专家模式触发器 - 自动检测和强制执行项目规范"
globs: ["**/*"]
alwaysApply: true
priority: 1300
expertMode: true
---

# 🤖 智能专家模式触发器 2.0

## 问题根源分析

**历史问题**: AI声称开启专家模式但未真正加载项目规则，导致违反既定规范。

**根本原因**:
1. 专家模式触发词与实际执行分离
2. 缺少强制的项目规范验证环节
3. 没有操作前的规则合规性检查

## 新一代智能触发机制

### 自动触发条件 (无需用户明确要求)

当检测到以下情况时，AI必须自动进入专家模式并执行完整验证：

#### 代码修改触发 (自动)
```typescript
// 检测到以下操作时自动触发专家模式
const autoTriggers = [
    'search_replace',     // 任何代码修改
    'write',             // 新文件创建
    'MultiEdit',         // 批量编辑
    'edit_notebook',     // 笔记本编辑
    'run_terminal_cmd'   // 终端命令（特别是Git相关）
]
```

#### Git操作强制拦截 (零容忍)
当AI准备执行任何Git命令时：
1. **立即暂停执行**
2. **强制加载** `.cursor/rules/git-version-control-law.mdc`
3. **验证操作合规性** - 必须使用项目脚本
4. **获得用户确认** - 显示将要使用的正确方法
5. **执行标准操作** - 使用 `scripts/git-safe-sync.sh`

#### 🔥 BUG修复铁律强制拦截 (第十重爆雷)
当AI准备修复代码BUG时：
1. **立即暂停执行**
2. **强制加载** `.cursor/rules/bug-fixing-iron-law.mdc`
3. **验证修复方案** - 禁止删除核心功能代码
4. **检查修复模式** - 禁止使用as any、TODO注释等简单绕过
5. **执行企业级修复** - 通过完善类型定义和渐进式实现解决问题

#### ⏰ 定时Git智能同步强制拦截 (第十一重爆雷)
当AI运行超过30分钟或完成任务节点时：
1. **立即暂停执行**
2. **强制加载** `.cursor/rules/eleventh-thunderbolt-git-auto-sync.mdc`
3. **检查同步触发条件** - 时间触发器或任务完成触发器
4. **执行智能Git同步** - 使用`bash scripts/git-safe-sync.sh --non-interactive --auto-commit`
5. **验证同步结果** - 确保本地远程完全同步，无代码丢失

#### 架构决策触发 (智能识别)
检测到以下技术词汇时自动加载ADR规则：
- 技术选型、架构设计、数据库设计
- 认证、授权、权限管理
- 前端架构、后端架构、微服务
- API设计、接口设计、集成方案

### 强制执行验证链 (不可跳过)

#### 强制验证步骤
1. **项目规范实时加载**
   - 读取 `.cursor/rules/git-version-control-law.mdc`
   - 扫描 `scripts/` 目录识别可用工具
   - 加载 `core-principles.mdc` 了解核心原则

2. **操作合规性预检查**
   - Git操作：必须使用 `scripts/git-safe-sync.sh`
   - 代码修改：必须通过 type-check + lint + build
   - 架构决策：必须查阅ADR并使用模板

3. **执行后验证**
   - Git同步：确认工作区干净，本地与远程一致
   - 代码质量：确认构建成功，类型检查通过

### 智能错误预防机制

#### 操作拦截机制
- **Git命令拦截**: 检测到手动Git命令时强制使用项目脚本
- **代码修改拦截**: 确保已加载项目规范和模板库
- **架构决策拦截**: 重大决策前强制查阅ADR文档
- **🔥 BUG修复拦截**: 检测到代码删除、注释功能、使用as any时强制阻止
- **⏰ 定时同步拦截**: 每30分钟或任务完成时强制Git智能同步

#### 实时合规监控
- **违规检测**: 持续监控AI行为是否符合加载的规则
- **自动纠正**: 发现违规时立即停止并显示正确方法
- **透明解释**: 向用户说明选择特定操作方式的规则依据

## 实施策略

### 立即生效的规则
1. **Git操作零容忍**: 任何Git命令都必须使用项目脚本
2. **规则加载强制**: 专家模式必须实际读取规则文件
3. **操作前验证**: 重要操作必须通过合规性检查
4. **透明化执行**: 用户必须看到AI的决策过程
5. **🔥 BUG修复铁律**: 永远禁止删除核心功能代码，必须通过企业级方法修复
6. **⏰ 定时同步铁律**: 30分钟或任务完成必须强制Git智能同步

### 渐进式改进
1. **第一周**: 实施Git操作拦截机制
2. **第二周**: 完善代码修改验证链
3. **第三周**: 添加架构决策智能咨询
4. **第四周**: 建立完整的合规监控体系

### 成功指标
- **合规率**: 100%的操作符合项目规范
- **透明度**: 用户能清楚了解AI的每个决策依据
- **效率**: 在保证规范的前提下提升操作效率
- **可靠性**: 零违规，零意外，零技术债务

这个新机制将确保AI真正成为遵循项目规范的智能助手，而不是声称遵循但实际违反的"伪专家"。
