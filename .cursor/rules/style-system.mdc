---
description: "SmartAbp样式系统规则 - 设计系统一致性"
globs: ["**/*.{css,scss,vue}", "src/styles/**/*"]
alwaysApply: true
priority: 700
---

# 🎨 SmartAbp 样式系统规则（基于增强主题系统TDD验证）

## 🏆 增强主题系统实现（19/19 TDD测试验证通过）
基于EnhancedThemeEditor.vue和useEnhancedThemeStore，实现完整的企业级主题定制系统：
- ✅ **三层设计令牌架构**: 基础令牌 → 语义变量 → 组件使用
- ✅ **WCAG AA标准**: 4.5:1对比度自动检查和警告
- ✅ **实时预览**: 防抖更新+性能优化
- ✅ **快照管理**: 主题版本控制和回退
- ✅ **导出导入**: JSON格式主题配置
- ✅ **本地持久化**: localStorage自动保存

## 设计系统强制使用

### CSS变量层级规范（三层令牌架构）
```css
/* 第一层：基础设计令牌 */
:root {
  --color-primary-500: #0ea5e9;
  --spacing-1: 0.25rem;
  --font-size-base: 1rem;
}

/* 第二层：主题语义变量 */
.theme-tech-blue {
  --theme-brand-primary: var(--color-primary-500);
  --theme-bg-component: var(--color-gray-100);
  --el-color-primary: var(--color-primary-500);
}

/* 第三层：组件使用 */
.button-primary {
  background: var(--theme-brand-primary);
  padding: var(--spacing-2) var(--spacing-4);
}
```

### 严禁行为
- ❌ 跳过设计系统直接定义自定义变量
- ❌ 使用已废弃的变量格式
- ❌ 硬编码样式值

## 样式文件结构

### 强制目录结构
```
src/SmartAbp.Vue/src/styles/
├── design-system/    # 统一设计系统
├── base/            # 基础样式
├── layouts/         # 布局样式
└── main.css         # 主样式入口
```

## Vue组件样式规范

### BEM命名规范
```vue
<template>
  <div class="user-management">
    <header class="user-management__header">
      <h1 class="user-management__title">用户管理</h1>
    </header>
  </div>
</template>

<style scoped>
.user-management {
  padding: var(--spacing-6);
  background: var(--theme-bg-component);
  border-radius: var(--radius-lg);
}
</style>
```

### 禁止行为
- ❌ 混乱的命名（驼峰、通用类名）
- ❌ 硬编码间距和颜色值
- ❌ 绕过增强主题系统直接定义样式
- ❌ 违反WCAG AA对比度标准（<4.5:1）

## 🎯 增强主题系统使用规范

### 主题预设应用
```typescript
// ✅ 推荐：使用增强主题Store
import { useEnhancedThemeStore } from '@/stores/lowcode/enhancedTheme'

const themeStore = useEnhancedThemeStore()
themeStore.switchTheme('theme-tech-blue')
```

### 动态主题变量更新
```typescript
// ✅ 推荐：防抖更新机制
themeStore.debouncedUpdate('--theme-brand-primary', '#ff6b6b')

// ❌ 禁止：直接DOM操作
document.documentElement.style.setProperty('--theme-brand-primary', '#ff6b6b')
```

### WCAG对比度验证
```typescript
// ✅ 推荐：自动对比度检查
const isCompliant = themeStore.validateContrastRatios()
if (!isCompliant) {
  // 显示对比度警告
  console.warn('当前主题不符合WCAG AA标准')
}
```