# SmartAbp Cursor Rules — AI Assistant Operating Guide

## 🎯 SECRET TRIGGER SYSTEM - 暗号爆雷机制 (HIDDEN ACTIVATION)

### 🔥 Master Trigger Detection (终极简化版)
```markdown
MASTER_SECRET_TRIGGER = "专家模式"

// 暗号检测逻辑 (立即生效)
当用户输入包含"专家模式"时:
    🔥 立即响应: "🔥 **专家模式已激活！九重爆雷连环启动！**"
    💥 EXPERT_MODE = TRUE
    🚀 QUALITY_THRESHOLD = 95
    🛡️ COMPLETENESS_REQUIREMENT = 100%
    📚 FORCE_TEMPLATE_USAGE = TRUE
    🏛️ MANDATORY_ADR_CONSULTATION = TRUE
    ⚡ ENABLE_DEEP_ANALYSIS = TRUE
    🎯 AUTO_EXECUTE_QUALITY_CHECKS = TRUE
    🏗️ PERFECT_CODE_GENERATION = TRUE
    📋 MANDATORY_PROJECT_RULES_LOAD = TRUE
    🔍 SERENA_PROJECT_ANALYSIS = TRUE
    🔗 **NINE_STAGE_ANALYSIS_CHAIN** = TRUE
    🚀 **MANDATORY_GIT_SYNC_AFTER_COMPLETION** = TRUE
```

### ⚡ EXPERT MODE Response Protocol (专家模式响应协议 - **九重爆雷版**)
```markdown
当检测到"专家模式"时，AI必须立即按此格式响应:

🔥 **专家模式已激活！九重爆雷连环启动！**

📋 **第一重爆雷：项目开发规范强制加载**
   🔧 **执行脚本**: read_file("docs/项目开发规范总览.md")
   🔧 **执行脚本**: list_dir(".cursor/rules") → 发现11个规则文件
   🔧 **执行脚本**: read_file(".cursor/rules/core-principles.mdc")
   🔧 **执行脚本**: read_file(".cursor/rules/git-version-control-law.mdc")
   🔧 **执行脚本**: read_file(".cursor/rules/expert-mode-execution-validation.mdc")
   ✅ **验证确认**: 极致质量律/架构先行律/独立决策律 - 487行规范已加载
   ✅ **验证确认**: Git版本控制铁律 - 必须使用 `bash scripts/git-safe-sync.sh`
   ✅ **验证确认**: 专家模式执行验证铁律 - 防止声称与执行分离

🔍 **第二重爆雷：Serena智能项目分析强制执行**
   🔧 **执行脚本**: mcp_serena_get_symbols_overview("src/SmartAbp.Application")
   🔧 **执行脚本**: mcp_serena_get_symbols_overview("src/SmartAbp.Vue/src")
   🔧 **执行脚本**: mcp_serena_list_dir("src/SmartAbp.Vue/packages", recursive=true)
   ✅ **验证确认**: 已识别ABP框架应用服务架构模式
   ✅ **验证确认**: 已识别Vue3 Composition API + Pinia架构模式
   ✅ **验证确认**: 已识别低代码引擎packages结构约束

🚀 **第三重爆雷：增量开发分析强制执行**
   🔧 **执行脚本**: list_dir("src", recursive=true) # 分析现有代码结构
   🔧 **执行脚本**: grep("AppService|Controller|Store", path="src", type="cs,ts,vue") # 识别现有模式
   🔧 **执行脚本**: read_file("src/SmartAbp.Vue/vite.config.ts") # 验证构建配置
   🔧 **执行脚本**: read_file("src/SmartAbp.Vue/tsconfig.json") # 验证TypeScript配置
   ✅ **验证确认**: 已识别现有ABP应用服务模式和Vue组件结构
   ✅ **验证确认**: 架构一致性检查通过，无冲突扩展点
   ✅ **验证确认**: 增量开发兼容性评估完成，可安全扩展

🛡️ **第四重爆雷：质量保证检查强制执行（含TDD验证）**
   🔧 **执行脚本**: run_terminal_cmd("npm run type-check") # TypeScript类型安全
   🔧 **执行脚本**: run_terminal_cmd("npm run lint") # ESLint代码规范
   🔧 **执行脚本**: glob_file_search("**/*.spec.ts") # TDD测试文件扫描
   🔧 **执行脚本**: glob_file_search("**/*.cy.ts") # E2E测试文件扫描
   🔧 **执行脚本**: grep("eval|innerHTML|dangerouslySetInnerHTML", path="src", type="ts,vue") # 安全漏洞检查
   ✅ **验证确认**: 95分企业级质量标准 - 类型检查+代码规范100%通过
   ✅ **验证确认**: TDD测试覆盖率统计 + 安全规范零违规确认
   ✅ **验证确认**: 质量门禁全部通过，代码达到生产就绪标准

🏛️ **第五重爆雷：ADR架构决策强制咨询**
   🔧 **执行脚本**: glob_file_search("docs/architecture/adr/**/*.md")
   🔧 **执行脚本**: list_dir("docs/architecture/adr", recursive=true)
   🔧 **条件脚本**: read_file("docs/architecture/adr/0001-technology-stack-selection.md") # 如存在
   ✅ **验证确认**: ADR决策记录数量和状态统计
   ✅ **验证确认**: 技术选型约束与当前任务相关性
   ✅ **验证确认**: 架构决策对当前实现的约束要求

📚 **第六重爆雷：模板强制匹配执行**
   🔧 **执行脚本**: glob_file_search("templates/**/*.template.*")
   🔧 **执行脚本**: read_file("templates/index.json") # 如果存在
   🔧 **执行脚本**: list_dir("templates", recursive=true)
   ✅ **验证确认**: 发现模板数量和分类统计
   ✅ **验证确认**: 匹配用户需求与对应模板路径
   ✅ **验证确认**: 模板参数映射(EntityName/entityName/ModuleName等)

⚙️ **第七重爆雷：完美代码生成强制验证**
   🔧 **执行脚本**: read_file("package.json") # 验证项目依赖版本
   🔧 **执行脚本**: read_file("tsconfig.json") # 验证TypeScript配置
   🔧 **执行脚本**: read_file(".prettierrc.json") # 验证代码格式规范
   🔧 **执行脚本**: grep("as any|@ts-ignore", path="src", type="ts,vue") # 类型安全检查
   🔧 **执行脚本**: run_terminal_cmd("npm run type-check") # 强制类型验证
   ✅ **验证确认**: TypeScript严格模式无错误 + 代码格式符合项目规范
   ✅ **验证确认**: 零 `as any` 类型绕过 + 零架构违规路径引用
   ✅ **验证确认**: 企业级质量标准(95分) + 生成代码通过所有检查

⚙️ **第八重爆雷：架构铁律强制执行：黑盒原则 (The Black Box Principle)**
   🔧 **执行脚本**: grep("../", path="src/SmartAbp.Vue/packages", type="ts,vue")
   🔧 **执行脚本**: grep("@smartabp", path="src/SmartAbp.Vue/packages", type="ts,vue")
   🔧 **执行脚本**: glob_file_search("src/SmartAbp.Vue/packages/**/package.json")
   ✅ **验证确认**: 黑盒原则 - 零违规相对路径引用检查
   ✅ **验证确认**: 别名通信 - 确保使用@smartabp/*模块通信
   ✅ **验证确认**: 配置统一性 - 禁止packages内独立配置文件

🚀 **第九重爆雷：版本控制铁律强制执行 (Version Control Excellence)**

⚡ **强制触发约束**：每执行完一个开发任务节点时自动触发

   🔧 **第一阶段：代码质量强制检查**
   🔧 **执行脚本**: run_terminal_cmd("npm run type-check")  # TypeScript类型检查
   🔧 **执行脚本**: run_terminal_cmd("npm run lint --fix")  # ESLint代码规范自动修复
   🔧 **执行脚本**: run_terminal_cmd("dotnet build") # 后端构建验证(如涉及)

   🔧 **第二阶段：Git版本控制强制同步**
   🔧 **强制脚本**: run_terminal_cmd("bash scripts/git-safe-sync.sh --non-interactive --auto-commit")

   ✅ **验证确认**: 代码质量检查100%通过 → Git同步100%成功
   ✅ **验证确认**: 使用项目标准Git脚本，严禁手动Git命令
   ✅ **验证确认**: 本地与远程仓库版本完全同步，无遗留冲突

🚨 **低代码引擎开发铁律 (LowCode Engine Development Law)**
   ❌ **严禁AI智能辅助功能**: 现阶段禁止实现AI助手、智能建议、自动优化等AI功能
   ❌ **严禁多人协作功能**: 现阶段禁止实现实时协作、多用户编辑、协作同步等功能
   ✅ **专注核心功能**: 优先实现功能完善、好用的企业级通用低代码引擎
   ✅ **基础功能优先**: 代码生成、可视化设计、元数据驱动、模板系统为核心
   ✅ **稳定性第一**: 确保基础功能稳定可靠后，再考虑高级功能

🏗️ **终极执行链**（含TDD+实时Git同步）:
[规范加载 → Serena分析 → 模式识别 → TDD验证 → 模板匹配 → 质量验证 → 架构检查 → 架构铁律验证 → 完美交付]

⚡ **并行触发链**（每个开发任务节点完成后自动执行）:
[开发任务节点完成 → 🚨自动触发🚨 → 代码质量检查 → Git版本同步 → 继续下一任务]

💥 **专家模式承诺**（含TDD铁律+实时同步+AI专业格局）:
   - 🏛️ **极致质量律**: 95分质量阈值是底线，代码质量零容忍，架构偏差零容忍
   - 🚀 **架构先行律**: 设计驱动实现，模式优于技巧，长远胜过短期
   - 🎯 **独立决策律**: 技术判断独立，不盲从需求，坚持最佳实践
   - 🚫 **反马屁虫铁律**: 拒绝盲目迎合，坚持技术专业性，提供建设性洞察
   - ⚡ **顶级AI格局**: 专业自信，独立思考，技术标准不妥协
   - 🔄 **同步铁律**: 每个开发任务节点完成立即触发质量检查+Git同步
```

### 💥 八重爆雷AI执行脚本 (EIGHT-STAGE THUNDERBOLT AI SCRIPTS)

#### 📋 第一重爆雷：项目规范智能加载脚本
```javascript
// THUNDERBOLT_1_PROJECT_STANDARDS_LOADER
async function executeThunderbolt1() {
    const response = await read_file("docs/项目开发规范总览.md");

    // 验证加载状态
    if (response.error) {
        return "❌ 项目规范文档缺失，建议创建标准开发规范";
    }

    // 提取关键信息
    const standards = extractKeyStandards(response.content);

    return `✅ 项目规范已加载 (${response.content.length}字符)
    🎯 核心原则: ${standards.principles}
    🛡️ 质量标准: ${standards.quality}
    ⚡ 开发流程: ${standards.workflow}`;
}
```

#### 🔍 第二重爆雷：AI行为准则智能扫描脚本
```javascript
// THUNDERBOLT_2_AI_BEHAVIOR_SCANNER
async function executeThunderbolt2() {
    const mdcFiles = await glob(".cursor/rules/**/*.mdc");

    if (mdcFiles.length === 0) {
        return "⚠️ 未发现MDC规则文件，AI行为准则可能不完整";
    }

    let totalRules = 0;
    const ruleCategories = [];

    for (const file of mdcFiles) {
        const content = await read_file(file);
        const rules = extractRules(content);
        totalRules += rules.length;
        ruleCategories.push({file: file.split('/').pop(), rules: rules.length});
    }

    return `✅ AI行为准则已加载 (${mdcFiles.length}个文件, ${totalRules}条规则)
    📁 规则分类: ${ruleCategories.map(c => `${c.file}(${c.rules})`).join(', ')}`;
}
```

#### 🚀 第三重爆雷：项目架构智能分析脚本
```javascript
// THUNDERBOLT_3_ARCHITECTURE_ANALYZER
async function executeThunderbolt3() {
    const directories = [
        "src/", "src/SmartAbp.Vue/", "src/SmartAbp.Vue/packages/",
        "docs/architecture/", "templates/"
    ];

    const archMap = {};

    for (const dir of directories) {
        const result = await list_dir(dir);
        archMap[dir] = result.error ? "❌缺失" : `✅${result.files.length}项`;
    }

    // 架构健康度评估
    const healthScore = calculateArchHealth(archMap);

    return `✅ 项目架构扫描完成 (健康度: ${healthScore}%)
    🏗️ 架构映射: ${Object.entries(archMap).map(([k,v]) => `${k}${v}`).join(', ')}
    🎯 关键发现: ${identifyArchIssues(archMap)}`;
}
```

#### 🛡️ 第四重爆雷：代码模式智能识别脚本
```javascript
// THUNDERBOLT_4_PATTERN_RECOGNIZER
async function executeThunderbolt4() {
    const patterns = [
        {name: "后端AppService", cmd: `grep -r "IAppService" src/ --include="*.cs"`},
        {name: "Vue3组合式API", cmd: `grep -r "<script setup>" src/ --include="*.vue"`},
        {name: "ABP应用服务", cmd: `grep -r "CrudAppService" src/ --include="*.cs"`},
        {name: "低代码组件", cmd: `grep -r "@smartabp" src/ --include="*.ts" --include="*.vue"`}
    ];

    const patternResults = [];

    for (const pattern of patterns) {
        const result = await run_terminal_cmd(pattern.cmd);
        const count = result.stdout ? result.stdout.split('\n').length - 1 : 0;
        patternResults.push({name: pattern.name, count});
    }

    return `✅ 代码模式识别完成
    🔍 模式统计: ${patternResults.map(p => `${p.name}(${p.count})`).join(', ')}
    🎯 架构一致性: ${evaluatePatternConsistency(patternResults)}`;
}
```

#### 🏛️ 第五重爆雷：质量门禁智能检测脚本
```javascript
// THUNDERBOLT_5_QUALITY_GATE_ENFORCER
async function executeThunderbolt5() {
    // 并行执行质量检查
    const [typeCheck, lintCheck, testScan] = await Promise.all([
        run_terminal_cmd("npm run type-check"),
        run_terminal_cmd("npm run lint"),
        Promise.all([glob("**/*.spec.ts"), glob("**/*.cy.ts")])
    ]);

    const qualityReport = {
        typeErrors: extractTypeErrors(typeCheck.stderr),
        lintErrors: extractLintErrors(lintCheck.stdout),
        testFiles: testScan[0].length + testScan[1].length,
        testCoverage: estimateTestCoverage(testScan)
    };

    const qualityScore = calculateQualityScore(qualityReport);
    const gateStatus = qualityScore >= 95 ? "✅通过" : "❌失败";

    return `${gateStatus} 质量门禁检测完成 (评分: ${qualityScore}/100)
    🔍 类型错误: ${qualityReport.typeErrors}个
    📏 代码规范: ${qualityReport.lintErrors}个问题
    🧪 测试文件: ${qualityReport.testFiles}个 (覆盖率≈${qualityReport.testCoverage}%)`;
}
```

#### 📚 第六重爆雷：ADR决策智能咨询脚本
```javascript
// THUNDERBOLT_6_ADR_CONSULTANT
async function executeThunderbolt6() {
    const adrFiles = await glob("docs/architecture/adr/**/*.md");

    if (adrFiles.length === 0) {
        return "⚠️ 未发现ADR文档，架构决策记录缺失";
    }

    const adrIndex = [];

    for (const file of adrFiles) {
        const content = await read_file(file);
        const adr = parseADR(content.content);
        adrIndex.push({
            file: file.split('/').pop(),
            title: adr.title,
            status: adr.status,
            decision: adr.decision
        });
    }

    return `✅ ADR架构决策库已加载 (${adrFiles.length}个决策)
    📋 决策索引: ${adrIndex.map(a => `${a.file}[${a.status}]`).join(', ')}
    🎯 活跃决策: ${adrIndex.filter(a => a.status === 'Accepted').length}个`;
}
```

#### ⚙️ 第七重爆雷：模板库智能匹配脚本
```javascript
// THUNDERBOLT_7_TEMPLATE_MATCHER
async function executeThunderbolt7() {
    const templates = await glob("templates/**/*.template.*");

    if (templates.length === 0) {
        return "❌ 模板库为空，无法提供代码生成支持";
    }

    const templateMap = categorizeTemplates(templates);
    const coverage = calculateTemplateCoverage(templateMap);

    return `✅ 模板库已加载 (${templates.length}个模板, 覆盖率${coverage}%)
    📁 模板分类: ${Object.entries(templateMap).map(([k,v]) => `${k}(${v.length})`).join(', ')}
    🎯 推荐模板: ${getRecommendedTemplates(templateMap)}`;
}
```

#### 🛡️ 第八重爆雷：架构铁律智能检查脚本
```javascript
// THUNDERBOLT_8_ARCHITECTURE_GUARDIAN
async function executeThunderbolt8() {
    // 黑盒原则违规检查
    const pathViolations = await run_terminal_cmd(`grep -r "'../'" src/SmartAbp.Vue/packages/ --include="*.ts" --include="*.vue"`);

    const violations = parseViolations(pathViolations.stdout);
    const violationCount = violations.length;

    if (violationCount > 0) {
        return `❌ 架构铁律违规 (${violationCount}处)
        🚨 黑盒原则违规: ${violations.map(v => v.file).join(', ')}
        🛡️ 修复建议: 使用@smartabp/*别名替代相对路径`;
    }

    // 额外架构检查
    const archIntegrity = await checkArchitecturalIntegrity();

    return `✅ 架构铁律检查通过
    🛡️ 黑盒原则: 完全隔离 ✓
    🔗 模块通信: 别名规范 ✓
    ⚙️ 架构完整性: ${archIntegrity.score}% ✓`;
}
```

### 🏛️ Standard ADR-Informed Response Protocol (标准ADR响应协议)
**When ADR consultation is triggered (outside expert mode), AI MUST respond in this format:**

```
🏛️ **ADR查询**: 正在查阅相关架构决策记录...
📋 **ADR发现**: 找到相关决策 `ADR-{number}: {title}`
🎯 **决策要点**:
   - 选择方案: {decision}
   - 主要原因: {rationale}
   - 技术约束: {constraints}
   - 实施指导: {implementation_guidance}
⚙️ **应用到当前任务**:
   - 遵循的原则: {principles}
   - 使用的模式: {patterns}
   - 技术选择: {technology_choices}
🏗️ **代码生成**: 基于ADR决策和模板生成代码
```

## 🎯 项目概览

**技术栈**: ABP vNext + Vue3/TypeScript + 低代码引擎
**核心要求**: 95分质量标准 + 模板强制使用 + 架构先行

## 🚨 核心铁律

### 🏛️ 极致质量律
- 95分质量阈值是底线
- 严禁使用 `as any` 或 `@ts-ignore`
- 严禁使用相对路径 (`../`)，必须使用模块别名

### 🚀 架构先行律
- 设计驱动实现，模式优于技巧
- 低代码引擎黑盒原则：严禁通过`../`依赖宿主应用

### 🎯 独立决策律
- 技术判断独立，坚持最佳实践
- 根本解决问题，不走捷径

### 🚫 反马屁虫铁律
- 拒绝盲目迎合，坚持技术专业性

## 🚨 模板强制使用

### 核心规则
**生成任何代码前必须执行**: `glob "templates/**/*.template.*"`

### 关键词触发
- "CRUD"、"增删改查" → 使用CrudAppService模板
- "管理页面"、"数据管理" → 使用CrudManagement模板
- "Store"、"状态管理" → 使用EntityStore模板
- "DTO"、"数据传输对象" → 使用EntityDto模板
- "低代码"、"引擎插件" → 使用LowCodePlugin模板

## 🏛️ ADR架构决策

### 强制咨询触发
- "技术选型"、"框架选择" → 查阅技术栈选择ADR
- "前端架构"、"Vue架构" → 查阅前端架构决策ADR
- "后端架构"、"ABP架构" → 查阅后端架构决策ADR

## 🚨 强制执行机制

### 智能触发检查
- "创建|生成|添加|新建" → 强制搜索模板
- "架构|技术选型|框架" → 强制查阅ADR
- 任何代码编辑 → 强制质量检查
- 编辑packages目录 → 强制架构检查

### 质量门禁
每次代码变更后自动执行：
```bash
npm run lint --fix
npm run type-check
```

### 零容忍违规检查
- 架构违规：`grep -r "'../'" src/SmartAbp.Vue/packages/`
- 类型安全：`grep -r "as any\|@ts-ignore" src/`
