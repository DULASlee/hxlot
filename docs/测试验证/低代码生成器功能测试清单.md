# SmartAbp 低代码生成器完整功能测试清单

**版本**: v2.0
**更新时间**: 2025-10-02
**测试目标**: 彻底排查所有豆腐渣工程，确保100%功能可用

---

## 📋 测试清单总览

| 分类 | 测试项 | 优先级 | 状态 |
|------|--------|--------|------|
| 基础连接 | 3项 | P0 | ⏳ 待测试 |
| 代码生成 | 8项 | P0 | ⏳ 待测试 |
| 前端集成 | 6项 | P0 | ⏳ 待测试 |
| 数据库功能 | 3项 | P1 | ⏳ 待测试 |
| UI配置 | 4项 | P1 | ⏳ 待测试 |
| 错误处理 | 5项 | P1 | ⏳ 待测试 |
| 性能验证 | 4项 | P2 | ⏳ 待测试 |

**总计**: 33项测试

---

## 🔥 P0级别：核心功能测试（必须100%通过）

### 一、基础API连接测试

#### 1.1 后端服务健康检查
- [ ] **测试步骤**:
  1. 启动后端: `cd src/SmartAbp.Web && dotnet run`
  2. 访问健康检查端点: `curl http://localhost:44379/health`
  3. 验证返回状态码200

- [ ] **预期结果**: 
  ```json
  {
    "status": "Healthy",
    "totalDuration": "00:00:00.0001234"
  }
  ```

- [ ] **失败处理**: 
  - 检查端口是否被占用
  - 检查appsettings.json配置
  - 查看后端日志

#### 1.2 代码生成器API端点可用性
- [ ] **测试步骤**:
  ```bash
  # 获取连接字符串列表
  curl http://localhost:44379/api/code-generator/connection-strings
  
  # 获取菜单树
  curl http://localhost:44379/api/code-generator/menus
  
  # 获取Schema版本
  curl http://localhost:44379/api/code-generator/schema-version-manifest
  ```

- [ ] **预期结果**: 所有端点返回200状态码

#### 1.3 前端API客户端连接
- [ ] **测试步骤**:
  1. 启动前端: `cd src/SmartAbp.Vue && npm run dev`
  2. 打开浏览器开发者工具
  3. 访问: `http://localhost:11369/CodeGen/entrance`
  4. 观察Network面板是否有API请求

- [ ] **预期结果**: 能看到 `/api/code-generator/*` 请求

---

### 二、核心代码生成功能测试

#### 2.1 最简单的CRUD模块生成
- [ ] **测试步骤**:
  1. 访问: `http://localhost:11369/CodeGen/ultra-simple`
  2. 填写表单:
     - 系统名称: `TestSystem`
     - 模块名称: `TestModule`
     - 实体名称: `TestEntity`
     - 显示名称: `测试实体`
  3. 点击"生成代码"按钮
  4. 等待生成完成

- [ ] **预期结果**:
  - ✅ 显示生成进度
  - ✅ 显示成功消息
  - ✅ 显示生成的文件列表
  - ✅ 文件数量 > 0

- [ ] **验证生成的文件**:
  ```
  后端文件（最少应包含）:
  □ TestEntity.cs - 实体类
  □ TestEntityDto.cs - DTO类
  □ ITestEntityAppService.cs - 服务接口
  □ TestEntityAppService.cs - 服务实现
  □ TestEntityController.cs - 控制器
  
  前端文件（如果生成）:
  □ test-entity.api.ts - API客户端
  □ test-entity-store.ts - Pinia Store
  □ TestEntityList.vue - 列表页面
  □ TestEntityForm.vue - 表单页面
  
  测试文件（如果生成）:
  □ TestEntityAppServiceTests.cs - 单元测试
  ```

#### 2.2 验证生成的代码质量
- [ ] **后端代码检查**:
  ```bash
  cd src/SmartAbp.Application
  # 检查生成的代码是否能编译
  dotnet build
  ```
  - [ ] 应该0错误0警告

- [ ] **前端代码检查**:
  ```bash
  cd src/SmartAbp.Vue
  # 类型检查
  npm run type-check
  ```
  - [ ] 应该0错误

#### 2.3 不同架构模式生成测试
- [ ] **Crud模式**:
  - [ ] 选择架构: `Crud`
  - [ ] 生成成功
  - [ ] 包含基本CRUD操作

- [ ] **DDD模式**:
  - [ ] 选择架构: `DDD`
  - [ ] 生成成功
  - [ ] 包含领域层、应用层分离

- [ ] **CQRS模式**:
  - [ ] 选择架构: `CQRS`
  - [ ] 生成成功
  - [ ] 包含Command和Query分离

#### 2.4 多实体模块生成
- [ ] **测试步骤**:
  1. 创建包含2个实体的模块
  2. 实体1: `Order` (订单)
  3. 实体2: `OrderItem` (订单项)
  4. 生成代码

- [ ] **预期结果**:
  - [ ] 为每个实体生成完整的文件
  - [ ] 文件数量 = 单实体数量 × 2

#### 2.5 实体属性完整性测试
- [ ] **测试不同数据类型**:
  ```json
  {
    "name": "TestEntity",
    "properties": [
      { "name": "StringField", "type": "string" },
      { "name": "IntField", "type": "int" },
      { "name": "DecimalField", "type": "decimal" },
      { "name": "DateTimeField", "type": "DateTime" },
      { "name": "BoolField", "type": "bool" },
      { "name": "GuidField", "type": "Guid" }
    ]
  }
  ```

- [ ] **预期结果**: 所有类型都能正确生成

#### 2.6 必填字段和验证规则
- [ ] **测试步骤**:
  ```json
  {
    "name": "TestEntity",
    "properties": [
      {
        "name": "RequiredField",
        "type": "string",
        "required": true,
        "maxLength": 100
      },
      {
        "name": "EmailField",
        "type": "string",
        "validationRules": ["Email"]
      }
    ]
  }
  ```

- [ ] **验证**:
  - [ ] 生成的DTO包含`[Required]`特性
  - [ ] 生成的DTO包含`[MaxLength(100)]`特性
  - [ ] 生成的DTO包含`[EmailAddress]`特性

#### 2.7 实体关系生成
- [ ] **一对多关系**:
  ```json
  {
    "entities": [
      {
        "name": "Order",
        "relationships": [{
          "type": "OneToMany",
          "target": "OrderItem",
          "navigationProperty": "Items"
        }]
      }
    ]
  }
  ```
  - [ ] 生成导航属性
  - [ ] 生成外键

- [ ] **多对多关系**:
  - [ ] 生成中间表
  - [ ] 生成双向导航属性

#### 2.8 代码生成增量模式
- [ ] **首次生成**:
  - [ ] 生成TestModule模块
  - [ ] 记录生成的文件列表

- [ ] **增量生成**:
  - [ ] 修改TestModule配置（添加新字段）
  - [ ] 再次生成
  - [ ] 验证：只更新修改的文件，不覆盖自定义代码

---

### 三、前端集成功能测试

#### 3.1 前端入口页面可访问性
- [ ] **测试URL**:
  ```
  http://localhost:11369/CodeGen/entrance - 入口选择页
  http://localhost:11369/CodeGen/ultra-simple - 极简模式
  http://localhost:11369/lowcode - 专业模式
  ```

- [ ] **预期结果**: 所有页面正常加载，无404错误

#### 3.2 模板选择器功能
- [ ] **测试步骤**:
  1. 访问代码生成页面
  2. 打开模板选择器
  3. 验证模板列表

- [ ] **预期模板**:
  - [ ] 通用CRUD模块
  - [ ] 组织管理模块
  - [ ] 用户管理模块
  - [ ] 角色管理模块
  - [ ] 权限管理模块
  - [ ] 菜单管理模块

#### 3.3 表单验证
- [ ] **必填字段验证**:
  - [ ] 空系统名称 → 显示错误提示
  - [ ] 空模块名称 → 显示错误提示
  - [ ] 空实体名称 → 显示错误提示

- [ ] **格式验证**:
  - [ ] 系统名称只能包含字母数字
  - [ ] 模块名称符合命名规范

#### 3.4 实时进度展示
- [ ] **测试步骤**:
  1. 点击生成代码
  2. 观察进度展示

- [ ] **预期显示**:
  - [ ] Loading状态
  - [ ] 进度百分比（如果支持）
  - [ ] 当前生成阶段（如果支持）

#### 3.5 生成结果预览
- [ ] **测试步骤**:
  1. 代码生成完成后
  2. 查看预览区域

- [ ] **预期显示**:
  - [ ] 生成的文件列表
  - [ ] 文件数量统计
  - [ ] 代码行数统计（如果支持）
  - [ ] 生成耗时

#### 3.6 代码复制和下载
- [ ] **复制功能**:
  - [ ] 点击"复制"按钮
  - [ ] 验证内容已复制到剪贴板

- [ ] **下载功能**（如果支持）:
  - [ ] 点击"下载"按钮
  - [ ] 下载ZIP文件
  - [ ] 解压验证文件完整性

---

## ⚡ P1级别：重要功能测试

### 四、数据库反查功能测试

#### 4.1 数据库连接测试
- [ ] **测试步骤**:
  1. 配置数据库连接字符串
  2. 调用反查API
  3. 验证返回结果

- [ ] **预期结果**: 返回数据库Schema信息

#### 4.2 表结构反查
- [ ] **测试步骤**:
  1. 反查指定Schema（如dbo）
  2. 获取所有表列表

- [ ] **预期结果**:
  - [ ] 返回表名列表
  - [ ] 包含列信息（名称、类型、是否可空）
  - [ ] 包含主键信息
  - [ ] 包含外键关系

#### 4.3 从数据库生成实体
- [ ] **测试步骤**:
  1. 选择数据库表
  2. 生成对应的实体类

- [ ] **预期结果**:
  - [ ] 自动映射数据库类型到C#类型
  - [ ] 生成正确的属性
  - [ ] 保留命名约定

---

### 五、UI配置功能测试

#### 5.1 保存UI配置
- [ ] **测试步骤**:
  ```bash
  curl -X POST http://localhost:44379/api/code-generator/ui-config?module=Test&entity=User \
    -H "Content-Type: application/json" \
    -d '{
      "displayName": "用户",
      "fields": [{
        "name": "Name",
        "displayName": "姓名",
        "editable": true,
        "visible": true,
        "required": true,
        "controlType": "input"
      }]
    }'
  ```

- [ ] **预期结果**: 返回200状态码

#### 5.2 获取UI配置
- [ ] **测试步骤**:
  ```bash
  curl http://localhost:44379/api/code-generator/ui-config?module=Test&entity=User
  ```

- [ ] **预期结果**: 返回之前保存的配置

#### 5.3 UI配置应用到生成
- [ ] **测试步骤**:
  1. 配置实体UI（设置某些字段不可编辑）
  2. 生成代码
  3. 检查生成的表单

- [ ] **预期结果**: 表单反映UI配置

#### 5.4 字段控件类型映射
- [ ] **测试不同控件类型**:
  - [ ] `input` → `<el-input>`
  - [ ] `select` → `<el-select>`
  - [ ] `date` → `<el-date-picker>`
  - [ ] `number` → `<el-input-number>`
  - [ ] `textarea` → `<el-input type="textarea">`

---

### 六、错误处理与边界条件测试

#### 6.1 无效输入处理
- [ ] **空模块配置**:
  ```json
  {}
  ```
  - [ ] 应返回400错误和详细消息

- [ ] **缺失必填字段**:
  ```json
  {
    "systemName": "Test"
    // 缺少name和displayName
  }
  ```
  - [ ] 应返回验证错误

#### 6.2 重复模块处理
- [ ] **测试步骤**:
  1. 生成TestModule
  2. 再次生成同名模块

- [ ] **预期行为**:
  - [ ] 提示模块已存在
  - [ ] 提供覆盖选项
  - [ ] 或生成增量更新

#### 6.3 网络错误处理
- [ ] **后端服务停止**:
  1. 停止后端服务
  2. 尝试生成代码

- [ ] **预期结果**:
  - [ ] 显示友好的错误消息
  - [ ] 提供重试选项

#### 6.4 超时处理
- [ ] **测试步骤**:
  1. 生成超大模块（10+实体）
  2. 模拟网络延迟

- [ ] **预期结果**:
  - [ ] 不应该超时
  - [ ] 或显示超时错误并允许重试

#### 6.5 权限验证
- [ ] **未登录用户**:
  - [ ] 应拒绝访问
  - [ ] 返回401错误

- [ ] **无权限用户**:
  - [ ] 应拒绝访问
  - [ ] 返回403错误

---

## 📊 P2级别：性能和优化测试

### 七、性能基准测试

#### 7.1 单实体生成性能
- [ ] **测试步骤**:
  1. 生成包含1个实体的模块
  2. 记录耗时

- [ ] **性能标准**:
  - [ ] < 10秒: 优秀 ⭐⭐⭐⭐⭐
  - [ ] < 30秒: 良好 ⭐⭐⭐⭐
  - [ ] < 60秒: 可接受 ⭐⭐⭐
  - [ ] > 60秒: 需优化 ❌

#### 7.2 多实体生成性能
- [ ] **测试场景**:
  - [ ] 3个实体: < 30秒
  - [ ] 5个实体: < 60秒
  - [ ] 10个实体: < 120秒

#### 7.3 并发生成测试
- [ ] **测试步骤**:
  1. 同时触发3个代码生成请求
  2. 验证都能正常完成

- [ ] **预期结果**:
  - [ ] 不崩溃
  - [ ] 不互相影响

#### 7.4 内存使用测试
- [ ] **测试步骤**:
  1. 监控后端内存使用
  2. 连续生成10次
  3. 观察内存增长

- [ ] **预期结果**:
  - [ ] 无内存泄漏
  - [ ] GC正常工作

---

## 📝 测试执行记录

### 测试环境
- **操作系统**: macOS 14.x / Windows 11 / Linux
- **后端版本**: .NET 9.0
- **前端版本**: Vue 3.4 + TypeScript 5.3
- **测试日期**: 2025-10-02
- **测试人员**: [填写]

### 测试结果统计

| 级别 | 总数 | 通过 | 失败 | 跳过 | 通过率 |
|------|------|------|------|------|--------|
| P0 | 23 | - | - | - | -% |
| P1 | 10 | - | - | - | -% |
| P2 | 4 | - | - | - | -% |
| **总计** | **37** | **-** | **-** | **-** | **-%** |

### 严重问题清单

| 编号 | 问题描述 | 严重级别 | 发现时间 | 状态 |
|------|----------|----------|----------|------|
| 1 | [示例] 前端使用mock API | 🔥 严重 | 2025-10-02 | ✅ 已修复 |
| 2 | ... | - | - | - |

### 改进建议

1. **功能改进**:
   - [ ] 添加代码预览功能
   - [ ] 添加模板市场
   - [ ] 支持自定义模板

2. **性能优化**:
   - [ ] 优化大模块生成速度
   - [ ] 添加生成进度推送
   - [ ] 实现增量生成

3. **用户体验**:
   - [ ] 改进错误提示
   - [ ] 添加操作引导
   - [ ] 支持生成历史

---

## ✅ 测试完成签字

- **测试负责人**: __________________
- **审核人**: __________________
- **日期**: __________________

---

**附录**: 
- 自动化测试脚本: `scripts/testing/run-e2e-tests.sh`
- 测试代码: `src/SmartAbp.Vue/tests/e2e/lowcode-generator.e2e.test.ts`
- 测试报告目录: `docs/testing/e2e-reports/`

