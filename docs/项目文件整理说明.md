# SmartAbp 项目文件整理说明

## 整理时间

**整理日期**: 2025-09-28 14:38:00

## 整理原则

1. 保留根目录必要配置文件
2. 迁移非必要的目录和文件
3. 更新 .gitignore 文件排除规则
4. 记录整理策略和文件位置

## 根目录必要配置文件

以下配置文件必须保留在根目录：

- `.codebuddy/` - 工具配置文件
- `.kiro/` - 工具配置文件
- `.specstory/` - 工具配置文件
- `.serena/` - 工具配置文件

## 文件迁移记录

### 项目根目录

- `demo/` → `docs/tooling/demo/examples/`
- `test/` → `src/tests/backend/`
- `k8s/` → `deployment/k8s/`
- `etc/abp-studio/` → `tools/abp-studio/`
- `.git-backups/` → `tools/git/backups/old-backups/`

### SmartAbp.Vue 根目录

- `test-login.html` → `docs/examples/vue-tests/`
- `test-theme.js` → `docs/examples/vue-tests/`
- `update-deps.bat` → `scripts/tools/vue/`
- `update-types.bat` → `scripts/tools/vue/`
- `stories/` → `tests/manual/stories/`

## .gitignore 更新

已更新 .gitignore 文件，添加以下排除规则：

- `tools/git/backups/old-backups/`
- `tools/git/backups/*.txt`
- `test-login.html`
- `test-theme.js`
- `/tests/manual/`
- `/docs/examples/vue-tests/`
- `/stories/`

## 脚本引用更新

已更新以下脚本中的路径引用：

- `scripts/git/git-safe-sync.ps1`
- `scripts/git/git-safe-sync.sh`
- `scripts/git/git-safe-sync.bat`

## 注意事项

1. 原始文件已保留在原位置，迁移采用复制方式，以确保不影响现有功能。
2. 后续可根据实际使用情况，决定是否删除原始文件。
3. 脚本文件迁移后，需要更新相关调用路径。
