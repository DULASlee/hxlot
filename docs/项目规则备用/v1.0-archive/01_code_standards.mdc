---
alwaysApply: true
---
# AI架构铁律同心圆模型 - L1: 代码形态层 (Code Standards)

## 文件说明
- **文件标识**: `01_code_standards.mdc`
- **层级**: L1 - 代码形态层
- **作用**: 提供具体、可执行的编码标准，所有代码必须严格符合此规范。

## 一、通用代码形态 (General Code Style)

1.  **命名规范 (Naming)**:
    - **清晰性**: 完整、无歧义的英文单词。
    - **一致性**: `camelCase` (变量/函数), `PascalCase` (类/接口/类型), `UPPER_SNAKE_CASE` (常量)。
    - **特定类型**: `is/has/can` (布尔), 复数 (数组), `动词+名词` (函数)。

2.  **样式与格式化 (Formatting)**:
    - **自动化**: 必须遵循项目 Prettier 和 ESLint 规则。
    - **核心样式**: 2空格缩进, 单引号, 使用分号。

3.  **注释规范 (Commenting)**:
    - **目的**: 解释“为什么”，而非“做什么”。
    - **场景**: 复杂业务逻辑, `TODO`标记, 非常规用法。

## 二、类型安全与代码质量铁律 (Type Safety & Quality Tenets)

### 🚨 严禁使用的危险语法 (Forbidden Syntax)
1.  **类型检查绕过**: 严禁使用 `@ts-ignore` 或 `as any`。
    - **替代方案**: 使用正确的类型定义、接口或类型守卫。
2.  **架构引用违规**: 严禁在 `packages/` 内使用 `@/` 引用主应用。
    - **替代方案**: 使用 `@smartabp/` 别名进行包间通信。

### ✅ **【集成自05】修复类型错误的唯一正确方式**
- **核心原则**: 只修复类型定义，绝不改变业务逻辑。
- **优先级**:
  1. 添加缺失的 `import`
  2. 定义缺失的 `interface/type`
  3. 修复类型注解
  4. 使用类型断言 (as) (仅在100%确定类型时)
  5. 重构代码 (需用户确认)

### 📋 强制执行规则
- **零容忍**: 出现禁忌语法，必须立即重构。
- **高优先级**: 此规则高于任何功能实现需求。
- **自动化检测**: 代码提交前必须通过自动化检查。

## 三、低代码生成器专项编码标准 (LowCode Generator Standards)

### 1. `packages` 核心原则
- **黑盒原则**: 每个包都是独立的、可复用的单元。严禁通过相对路径 (`../`) 或主应用别名 (`@/`) 进行跨包引用。
- **独立性要求**: 每个包必须能够独立编译和发布 (独立的`package.json`, `tsconfig.json`)。

### 2. `packages` 架构层级规范
- **严格依赖层级** (只能向下依赖):
  - `lowcode-shared`: 基础层 (层级0) - 零依赖
  - `lowcode-core`/`lowcode-api`/`lowcode-tools`: 核心层 (层级1) - 只依赖shared
  - `lowcode-designer`: 应用层 (层级2) - 依赖shared+core
- **违规检测命令**:
  ```bash
  grep -r "from '@smartabp/lowcode-designer'" src/SmartAbp.Vue/packages/lowcode-core/
  grep -r "from '@smartabp/lowcode-core'" src/SmartAbp.Vue/packages/lowcode-shared/
  grep -r "from '@smartabp/lowcode-api'" src/SmartAbp.Vue/packages/lowcode-core/
  ```

### 3. `packages` TypeScript严格模式
- **强制要求**: `packages` 必须启用最严格的TypeScript配置 (`"strict": true`, `"noImplicitAny": true`, etc.)。
- **配置示例**:
  ```json
  {
    "compilerOptions": {
      "strict": true,
      "noImplicitAny": true,
      "strictNullChecks": true,
      "noUncheckedIndexedAccess": true,
      "noUnusedLocals": true,
      "noUnusedParameters": true
    }
  }
  ```