---
alwaysApply: true
---
# AI架构铁律 - L2: 开发工作流协议

## 核心原则: 架构先行、强制检索、边界清晰、用户主导

---

## 🚀 阶段0：架构飞行前检查 (Architecture Pre-flight Protocol)

**任何代码编写前，必须强制执行并通过所有检查。**

### 0.1 JavaScript污染检测
```bash
find src/SmartAbp.Vue/src -name "*.js" -type f | grep -v "node_modules\|vite.config\|.eslintrc"
grep -r "<script>" src/SmartAbp.Vue/src --include="*.vue" | grep -v "lang=\"ts\""
```
- **失败**: 立即停止，要求迁移为.ts或添加`lang="ts"`

### 0.2 代码重复度检测 (DRY)
```bash
find src/SmartAbp.Vue/packages -name "*.vue" | xargs basename -a | sort | uniq -d
grep -r "export function" src/SmartAbp.Vue/packages --include="*.ts" | awk '{print $3}' | sort | uniq -d
grep -r "export interface\|export type" src/SmartAbp.Vue/packages --include="*.ts" | awk '{print $3}' | sort | uniq -d
```
- **失败**: 立即停止，展示已有实现，提供复用建议

### 0.3 依赖层级验证
```bash
grep -r "@smartabp/lowcode-designer" src/SmartAbp.Vue/packages/lowcode-core/
grep -r "@smartabp/lowcode-" src/SmartAbp.Vue/packages/lowcode-shared/
```
- **层级规则**: `shared(0) → core/api/tools(1) → designer(2)`，严禁逆向依赖
- **失败**: 立即停止，解释违规并提供正确方案

---

## 🔧 阶段1：标准开发协议 (Standard Development Protocol)

1.  **需求分析 (Analyze)**: 理解业务目标，自动查阅`docs/architecture/adr`识别约束。
2.  **技术设计 (Design)**:
    - **强制代码检索**: 必须全局搜索可复用组件/服务/函数。
    - **强制模板匹配**: 必须搜索`templates/`目录并优先使用。
3.  **编码实现 (Implement)**: 遵循`01_code_standards.mdc`，执行原子化操作。
4.  **自我验证 (Verify)**: 内部模拟静态检查，审查逻辑与需求一致性。

---

## ⚡ 阶段2：特殊任务协议 (Specialized Protocols)

### BUG修复协议 (Bug-Fixing)
- **严禁降级修复**: 严禁通过删除/注释代码、或使用`any`/`@ts-ignore`来“解决”问题。
- **根源分析**: 修复必须针对问题根源，而非表面症状。
- **最小化影响**: 修复应尽可能本地化，避免副作用。

### 模板自动化协议 (Template Automation)
1.  **意图映射**: 根据关键词（"CRUD", "Store"）自动匹配`templates/`。
2.  **信息提取**: 从用户请求中自动提取并填充所有占位符。
3.  **强制命令**: 必须使用以下非交互式命令执行代码生成：
    ```bash
    node scripts/templates/apply-template.js --templateId <template_id> --outputPath <output_path> --placeholders '{"key1":"value1"}'
    ```

### 🔴 **【集成自05】破坏性操作协议 (Destructive Action Protocol)**
- **适用场景**: 任何代码的**删除**或**注释**。
- **核心禁令**: **严禁未经用户三次确认，执行任何破坏性操作。**
- **强制三次确认流程**:
  1.  **第一次确认 (意图与影响)**: AI必须提出请求，清晰说明“为什么必须删除/注释？影响范围？替代方案？风险评估？”。必须得到用户明确的“同意删除/注释”回复。
  2.  **第二次确认 (代码审查)**: AI必须展示将要删除或注释的**完整代码**，并再次提问：“您确定要执行此操作吗？这是第二次确认”。必须得到用户明确的“确认删除/注释”回复。
  3.  **第三次最终确认 (最终警告)**: AI必须发出最终警告：“⚠️ 这是最后一次确认！操作后可能无法恢复！是否需要先备份？”。必须得到用户明确的“最终确认删除/注释”回复。
- **前置/后置检查**: 在执行任何修改（尤其是BUG修复）前后，AI都必须在内部完成`05`规则定义的“修复前6问检查”和“修复后自动验证”，确保逻辑完整性。

---

## 🛡️ 阶段3：边界与交接协议 (Boundary & Hand-off Protocol)

### 服务器管理与终端使用铁律
- **核心禁令**: **AI绝对禁止以任何方式（`npm run dev`, `dotnet run`）启动开发服务器。**
- **AI职责**: 仅限于代码编写、修复、编译检查、规范检查。
- **用户主导**: 服务器的启停完全由用户手动控制。
- **【集成自06】终端优化原则**:
    - **目标**: 保持聊天框清爽，避免大量输出阻塞对话。
    - **策略**:
        - **后端编译**: 必须使用 `--verbosity minimal`，优先编译 `.csproj` 而非 `.sln`。
        - **前端命令**: 必须使用 `--silent` 或 `--quiet`。
        - **Git操作**: 必须使用 `--quiet` 和 `--short`。
    - **Mac ARM优化**: 检测到`arm64`架构时，自动应用`DOTNET_TieredCompilation=1`等环境变量。

### 工作完成标准与交接
- **完成标志**:
  - **前端**: `npm run type-check` (0错误) + `npm run lint` (0错误)
  - **后端**: `dotnet build` (0错误)
- **强制交接模板**:
  ```
  ✅ 代码修改完成
  ✅ 编译与规范检查通过
  
  🎯 下一步操作：
  请您手动启动服务器进行测试
  
  📋 启动命令参考：
  前端: cd src/SmartAbp.Vue && npm run dev
  后端: cd src/SmartAbp.Web && dotnet run
  ```
