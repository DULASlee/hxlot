---
alwaysApply: true
---

# AI架构铁律 - L4: 代码质量禁止层

## 核心原则: 零容忍、强制修复、自动检测

---

### 1. 零级禁令：JavaScript污染 (严禁破坏类型安全)

- **禁止**:
  - 创建 `.js` 文件 (应为 `.ts`)
  - Vue组件未使用 `<script setup lang="ts">`
  - 缺少类型定义 (函数参数/返回值, API DTOs)
  - 使用 `as any` 或 `@ts-ignore`
- **检测**:
  ```bash
  find src/ -name "*.js" -type f | grep -v "\.generated\." | grep -v node_modules
  grep -r "as any\|@ts-ignore" src/
  ```

---

### 2. 一级禁令：架构完整性 (严禁破坏模块边界)

- **禁止**:
  - `packages` 目录中使用 `@/` 引用主应用
  - `packages` 目录中使用 `../` 跨包引用
- **检测**:
  ```bash
  grep -r "@/" src/SmartAbp.Vue/packages/
  grep -r "'../'" src/SmartAbp.Vue/packages/
  ```

---

### 3. 二级禁令：`packages` 依赖 (严禁破坏低代码核心)

- **禁止: 逆向/循环依赖**
  - **层级规则**: `shared(0) → core/api/tools(1) → designer(2)`
  - **检测**:
    ```bash
    grep -r "@smartabp/lowcode-designer" src/SmartAbp.Vue/packages/lowcode-core/
    grep -r "@smartabp/lowcode-" src/SmartAbp.Vue/packages/lowcode-shared/
    grep -r "@smartabp/lowcode-api" src/SmartAbp.Vue/packages/lowcode-core/
    grep -r "@smartabp/lowcode-core" src/SmartAbp.Vue/packages/lowcode-api/
    ```

- **禁止: 主应用耦合**
  - **检测**:
    ```bash
    grep -r "from '@/'" src/SmartAbp.Vue/packages/
    grep -r "from '.*src/SmartAbp.Vue/src'" src/SmartAbp.Vue/packages/
    ```
---

### 4. 三级禁令：构建与依赖

- **禁止**:
  - **构建产物污染**: 手动修改 `dist/`, 提交 `dist/` 到 Git
  - **未经审核的第三方依赖**: (体积>50KB, 更新>6个月, 下载量<10万/周)

---

### ⚖️ 修复原则与自动化

- **修复原则**:
  - **类型安全**: 使用具体类型、`unknown`、类型守卫
  - **架构通信**: 使用 `@smartabp/` 别名、`props/emit`
- **自动化**:
  - **检测脚本**: `scripts/quality-gate.sh` 在提交前自动执行所有检测