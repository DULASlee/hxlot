{
  "$schema": "../../template.schema.json",
  "id": "EntityDto",
  "version": "1.1.0",
  "name": "Entity DTO",
  "description": "为实体生成标准的Data Transfer Object (DTO)，包含审计字段和常用属性。",
  "tags": ["Backend", "Contracts", "DTO", "ABP"],
  "author": "SmartAbp Core Team",
  "type": "C#",
  "placeholders": [
    {
      "name": "{{entityName}}",
      "description": "实体名称 (PascalCase)，例如：'Product'。",
      "required": true
    },
    {
      "name": "{{primaryKeyType}}",
      "description": "实体主键类型，例如：'Guid' 或 'long'。",
      "defaultValue": "Guid",
      "required": false
    }
  ],
  "relatedTemplates": [
    "CrudAppService"
  ]
}
